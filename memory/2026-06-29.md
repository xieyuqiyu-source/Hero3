# 2026-06-29 项目记忆

- 记录物品与背包系统后续开发共识：当前背包入口在武将页侧边，但需要改为格子形展示。
- 背包容量限制定为 9999。
- 物品堆叠上限需要真正生效，超过上限要拆分堆叠。
- 物品品质分 5 个等级，参考钓鱼品质体系，从普通到史诗，史诗为金色。
- 只有史诗物品使用时需要二次确认。
- 玩家交易暂缓。
- 玩法掉落应先配置物品，再由玩法掉落/奖励配置引用，不应玩法硬编码发物品。
- Admin 后台需要补物品管理能力。
- 物品流水需要补齐，记录获得、消耗、来源、数量、玩家和时间。
- 物品配置细节后续继续讨论，暂不一次性扩展大量正式物品。
- 确认物品品质参考钓鱼但扩展为 5 档：普通、稀有、史诗、传说、神话；钓鱼当前是普通、稀有、史诗、传说 4 档。
- 确认史诗以上物品使用需要二次确认。
- 物品效果确认走 Effect Pipeline。
- 物品掉落确认通过物品配置和掉落池引用，玩法不硬编码发放具体物品。
- 已新增 `docs/系统设计/物品配置开发文档.md` 作为第一版配置设计文档。
- 物品分类讨论补充：碎片 `fragment` 暂时只预留，不进入第一版正式配置。
- 装备需要纳入物品体系，并且后续要单独设计装备套装系统模块；套装效果应通过装备穿戴状态和 Modifier/Effect 管线接入，不塞进普通物品使用流程。
- GM 新增物品流程确认：先选分类，再选品质，系统按分类自动补全 ID 前缀，GM 填写名称和 ID 后缀；保存后物品 ID 锁定，不使用随机 ID。
- 确认物品配置校验要做，并写入验收标准：校验物品 ID、分类、品质、堆叠、效果引用、掉落池引用、权重、数量和史诗以上确认规则。
- 补充确认验收标准需要明确 Admin 可新建、编辑、校验、发放物品，并可查看和筛选物品流水。
- 轮回绝境玩法定稿并写入 `docs/系统设计/轮回绝境玩法开发文档.md`：玩家主动开启，默认 2 小时，18 波，奇数进攻、偶数防守；层级决定每波规模，例如 1 万、10 万、100 万到 100 亿；每波随机敌方阵营、兵种搭配、己方加成和敌方加成；进攻波可多次进攻打光本波，防守波点击防御就绪后 3 秒倒计时并一次结算，防守失败本次结束；真实损耗；每波展示奖励池，通过波次后奖励累计，失败/超时/通关时发放已累计奖励；奖励只引用 itemId 和 dropPoolId，走 Reward/Effect Pipeline；战报走标准副本战报。

## 物品配置系统第一轮落地

- 按 `docs/系统设计/物品配置开发文档.md` 推进物品系统：`items.json` 切到 `category`、`quality`、`confirmOnUse` 标准字段，并兼容旧 `type`、`rarity`。
- 新增 5 档物品品质校验：普通、稀有、史诗、传说、神话；史诗及以上前端默认二次确认。
- 背包从按物品总数升级为真实格子模型：`InventorySlots` / `inventorySlots`，容量 9999 格，获得物品按 `maxStack` 自动拆分。
- `player_inventory` 表升级为 `slot_id` 主键，旧聚合接口 `inventory` 继续作为兼容视图返回。
- 新增 `item_ledger` 物品流水，奖励发物品和使用消耗都会记录玩家、物品、变化量、前后数量、来源和时间。
- 新增 `drop_pools.json` 和掉落池配置校验，后续玩法可引用掉落池或物品 ID，不应硬编码发放。
- Admin 配置页新增物品配置 JSON 校验/保存、玩家背包格子查询、物品流水筛选；原 GM 发物品继续可用。
- Admin 物品配置页继续补齐表单式新增/编辑流程：先选分类、品质，系统按分类展示 ID 前缀，GM 填后缀、名称、描述、堆叠、确认规则和效果 JSON；已有物品编辑时锁定 ID。
- 玩家端武将页背包改为格子展示，按品质显示颜色，使用高品质物品弹二次确认。
- 用户确认背包格子只显示小正方形本体，暂时使用 icon 占位；物品名称、品质、描述、使用和取消操作统一放到点击格子后的弹窗中，后续每个物品会单独设计图片。
- OpenAPI、README、Go README 已同步；`make openapi-bundle` 已重新生成打包文档。
- 修复存储层核心建筑投影和显式修复事务比较基准，旧玩家补齐核心建筑后会持久化到 `player_buildings`，此前阻碍全量存储测试的建筑测试已通过。
- 验证通过：`go test ./internal/app/game ./internal/transport/api ./internal/infrastructure/storage ./cmd/dbtool ./cmd/server`、`pnpm --dir web build`、`pnpm --dir admin build`、`make openapi-bundle`。

## 将领面板空间优化

- 用户确认将领调整需要节省空间：洗点放到将领标题右侧，换将按钮紧挨洗点，不再在主界面常驻选择框。
- 换将改为点击按钮后弹窗选择将领，再确认更换；规则仍为保留等级和经验，四维加点重置。
- 四维加点从两列小卡改成长条进度表现，属性加成直接嵌入对应四维长条内高亮展示，取消单独属性网格以减少高度占用。
- 四维条进一步确认采用单行展示：名称、加点按钮和加成高亮都放在同一条内，背景条本身就是进度，不再显示四维数字和“暂无加成”占位。

## 轮回绝境第一轮开发

- 按 `docs/系统设计/轮回绝境玩法开发文档.md` 开始落地地图-副本玩法：新增玩法模块注册 `reincarnation_abyss`，通过核心兵力、奖励和战报入口接入。
- 新增 `go/config/reincarnation.json`，配置 7 个层级、18 波奖励、敌方阵营池、加成倍率和 3 秒防守倒计时。
- 后端新增轮回绝境模型、配置校验、服务、玩家接口、GM 接口、内存仓储和 MySQL 仓储；MySQL 自动建 `reincarnation_runs`、`reincarnation_waves`、`reincarnation_battles`。
- 玩家接口支持获取配置/当前实例、开启副本、进攻波出兵、防守波就绪结算、主动结算、查看副本战报。
- GM 接口支持读取/保存配置、查看实例列表和详情、强制结算异常实例、幂等修复累计奖励。
- 玩家前端“地图 -> 副本”已把轮回绝境从占位改为可操作面板，支持选层级、查看当前波次/加成/奖励、配置兵力、进攻、防守倒计时和结算奖励。
- Admin 副本页新增轮回绝境管理面板，支持 JSON 配置保存、实例筛选、强制结算和奖励修复；原副本图片参数继续保留。
- 已补基础服务测试 `TestReincarnationStartAndAttack`，验证开启、首波进攻、真实兵力结算、战报和副本战斗记录。
- 当前已验证：`go test ./internal/app/game ./internal/transport/api ./cmd/server`、`pnpm --dir web build`、`pnpm --dir admin build`。
- 继续补齐轮回绝境接口契约：新增 OpenAPI `Dungeon` tag、玩家/GM 轮回绝境路径、Schema，并重新生成 `docs/接口文档/openapi打包.yaml`。
- 为进攻波和防守波补 `clientActionId` 幂等支持：同一波次同一动作 ID 重复提交会返回原战报，避免网络重试重复扣兵。
- MySQL 轮回结算从“玩家资产事务 + 副本快照事务 + 战报事务”增强为同一事务内锁定副本、更新玩家资产、保存波次/战斗和写战报。
- 新增验证：`make openapi-bundle`、`go test ./internal/app/game ./internal/transport/api ./cmd/server`、`pnpm --dir web build`、`pnpm --dir admin build` 均通过。
- 发现存储包旧用例 `TestMySQLResourcePackItemGrantsPlayerResources` 按当前 `go/config/items.json` 会失败：测试仍期望木材 2200，但当前小资源包配置和容量结算结果为 4800，后续应单独同步测试预期或固定测试配置。
- 修正轮回绝境连续进攻展示：当前波面板展示敌军剩余/初始进度，战报敌方兵力改为本次战斗前数量，避免误看成每次按满兵打。
- 修正防守失败奖励同步：防守失败会结束副本并发放前面已通过波次奖励，轮回接口现在返回 `inventory` 和 `inventorySlots`，前端立即同步武将页背包；新增测试覆盖第 1 波通过、第 2 波防守失败后奖励入背包。
- 按用户反馈继续优化轮回绝境：前端不再展示敌军精确剩余兵力，只保留敌军态势进度条；每波敌方阵营、兵种组合、兵力规模和双方加成改为副本生成时随机；出兵改为点击兵种自动按剩余上限填满并高亮，再点取消；战报按钮可直达战报详情，返回时回到地图副本页。

## 金币军事建筑与轮回加成重置

- 新增核心建筑 `siege_camp` 攻城武器营、`special_camp` 特殊建筑营，旧玩家通过核心建筑补齐入口自动拥有，新玩家初始创建也包含。
- 两个新建筑只消耗账户金币升级，并配置为高额金币成本；升级后分别提高攻城/特殊兵种征兵速度，并降低对应类别征兵资源消耗。
- 征兵资源消耗从简单“单价 x 数量”改为统一计算入口，接入 Modifier 管线，当前消耗减免上限为 80%，避免免费征兵。
- 军事招募页新增“金币训练强化”区域，高亮展示攻城武器营和特殊建筑营；兵种卡片和征兵弹窗同步显示折扣后的实际消耗，并按折扣后消耗计算最大可征兵数量。
- 轮回绝境配置新增 `bonusResetGoldCost`，当前为 10 金币；玩家可在当前波未产生战斗记录前通过 `bonus-reset` 消耗金币重置己方/敌方随机加成。
- 轮回加成重置记录账户金币流水 `reincarnation_bonus_reset`，返回扣费后的 `accountGold` 和本次 `cost`。
- 已同步 OpenAPI 源文件与打包文档、README、Go README 和轮回绝境玩法文档。
- 验证通过：`go test ./internal/app/game ./internal/transport/api ./cmd/server`、`pnpm --dir web build`、`pnpm --dir admin build`、`make openapi-bundle`。
- 额外检查 `go test ./internal/infrastructure/storage` 可编译到测试运行阶段，但仍失败在既有旧用例 `TestMySQLResourcePackItemGrantsPlayerResources`：期望木材 2200，当前配置结算为 4800，非本次改动新增问题。

## 战斗无损阈值与购买加成续订修正

- 本地测试库已从线上 `new_hero3` 重新拷贝到本机 `test_hero3`，恢复后执行迁移和权威表健康检查，关键数据量与线上一致，临时 dump 已清理。
- 战斗规则新增可配置字段 `noLossPowerRatioThreshold`，默认 `0.001`，含义为弱强实力比低于等于该阈值时胜方无损；官方攻击和官方掠夺规则都已写入默认值。
- Admin 战斗规则面板新增“无损比”输入，GM 可直接调整 `noLossPowerRatioThreshold`。
- 战斗规则设计文档、根 README 和 Go README 已同步说明悬殊战力无损阈值。
- 修正左侧菜单产量/容量购买加成规则：同倍率续订只叠加剩余时间；不同倍率购买按新倍率和新时长重新计算，不再倍率累乘。
- 玩家端产量加成和仓库扩容按钮文案已从“叠加倍率”改为“续订/切换”，避免误导。
- 新增测试覆盖战斗无损阈值、阈值收紧后仍按公式损失、同倍率加成续时、不同倍率重算。
- 本轮验证通过：`go test ./internal/core/combat`、`go test ./internal/app/game -run 'TestPurchase.*Boost|TestBoostOptions'`、`go test ./internal/app/game`、`go test ./internal/app/game ./internal/transport/api ./cmd/server`、`pnpm --dir admin build`、`pnpm --dir web build`。

## 武将参战规则与战报修正

- 明确并统一武将参战规则：NPC、PVP、轮回绝境副本真实战斗均以本次显式携带/在家守城的武将为准；不带将只享受家里基础加成，不享受武将加成、经验和武将特性。
- NPC 攻击请求新增 `generalIds`，后端按指定武将构建战斗加成和特性，杀敌后给参战武将经验；前端 NPC 出征面板新增参战武将选择，默认不带将。
- 轮回绝境进攻/防守请求新增 `generalIds`，副本页新增参战武将选择，结算后同步 `general/generals` 并弹出战斗结果；副本战报显示将领经验。
- PVP 防守修正：守城军只吃在家防守武将加成和经验，援军按自己的武将快照加成并补发援军武将经验；被打时武将不在家则不享受加成和经验。
- 标准战报修正防守视角武将归属：别人打我时，攻击方栏显示攻击者武将，防守方栏显示我的守城武将；读取旧错位 `detail` 时会自动校正两侧武将。
- 轮回绝境防守波的武将快照改写到防守方字段，避免防守副本战报继续出现武将栏错位。
- OpenAPI 源文件和打包文档已同步 `generalIds` 字段；README 已补充武将参战规则说明。
- 本轮验证通过：`go test ./internal/app/game -run 'TestNormalizeBattleReport|TestPvp|TestReincarnationSelectedGeneralGainsExp|TestReincarnationStartAndAttack'`、`go test ./internal/app/game ./internal/transport/api ./cmd/server`、`pnpm --dir web build`、`make openapi-bundle`、`git diff --check`。
