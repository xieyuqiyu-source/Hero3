# Hero3 Memory - 2026-06-26

## 核心地基补强：建筑生命周期

- 用户确认核心地基已完成后，继续追问建筑长期状态是否应算入地基。
- 共识：建筑是玩家长期资产，建筑状态变化应进入核心地基，而不是后续玩法临时补丁。
- 已新增 `internal/core/building`，定义建筑标准状态和通用变更类型。
- 建筑标准状态包括：`normal`、`constructing`、`upgrading`、`locked`、`damaged`、`destroyed`、`repairing`、`disabled`、`protected`、`occupied`。
- 已新增应用层统一入口 `MutateBuilding(playerID, mutation)`，建筑摧毁、损坏、修复、锁定、占领等长期状态变化应通过该入口或后续 Effect Pipeline 调用。
- 现有升级流程接入建筑状态：开始升级为 `upgrading`，完成升级恢复 `normal`。
- `destroyed`、`disabled` 建筑不再提供资源产量、容量和建筑 Modifier。
- 建筑状态变化会进入核心资产事件快照，通过 `building.upgraded` 事件的 `statusChanges` 载荷发布。
- 已更新核心地基文档、总体架构文档和 Go README。
- 本轮验证目标：`go test ./...`、`git diff --check`。

## 建筑系统整理第一步

- 用户确认下一步不是继续讨论，而是进入重构系统整理。
- 当前大目标包括：建筑系统、武将系统、兵种/军事系统、道具/Buff 系统、玩法模块边界和最终 Effect Pipeline。
- 本轮先整理建筑系统，原则是不改变数据库、不改变业务行为，先把建筑相关代码归口。
- 已新增 `building_registry.go`，归口建筑配置查询、建筑类型判断和老存档核心建筑补齐。
- 已新增 `building_effects.go`，归口建筑资源产量、容量和建筑 Modifier 来源。
- 已新增 `building_registry_test.go`，覆盖建筑注册表、安全复制和核心建筑补齐。
- 已从 `balance.go`、`service.go`、`service_resource.go`、`modifier.go` 中移出建筑专属逻辑，减少通用文件继续膨胀。
- 文档已补充“建筑系统归口”：核心层只管稳定状态/变更校验，应用层建筑系统管配置、效果和服务编排。
- 本轮验证目标：`go test ./...`、`git diff --check`。

## 武将系统整理第一步

- 本轮继续重构系统整理，进入武将系统。
- 共识保持不变：武将特性总线属于稳定核心能力，武将配置、成长、属性装配和玩家操作属于应用层武将系统。
- 已新增 `general_registry.go`，归口武将配置查询、注册判断和全部武将配置列表。
- 已新增 `general_growth.go`，归口武将等级、经验、属性点和战斗经验计算。
- 已新增 `general_effects.go`，归口武将配置装配、属性拆解和武将 Modifier 来源。
- 已新增 `general_combat.go`，归口武将特性接入战斗特性总线和战报触发结果合并。
- 已新增 `general_registry_test.go`，覆盖武将注册表和配置安全复制。
- 已从 `service_general.go`、`model.go`、`modifier.go`、`service_combat.go` 中移出武将专属 helper，减少通用文件继续膨胀。
- 文档已补充“武将系统归口”：新增普通武将走配置注册表，固定加成走 Modifier，战斗特性走核心 general traits；涉及新长期资产变更时先检查核心是否已有通用入口。
- 本轮验证目标：`go test ./...`、`git diff --check`。

## 兵种/军事系统整理第一步

- 本轮继续重构系统整理，进入兵种/军事系统。
- 共识保持不变：核心战斗引擎只管稳定战斗计算和规则绑定；兵种配置、兵力状态、征兵队列和战斗适配属于应用层军事系统。
- 已新增 `unit_registry.go`，归口兵种配置查询、注册判断、按名称查找兵种，并对返回配置做安全复制。
- 已新增 `army_state.go`，归口玩家兵力增加、兵力切片和 map 转换、战斗后兵力合并。
- 已新增 `recruit_timing.go`，归口征兵时长、通用征兵速度加成和兵种分类征兵速度加成。
- 已新增 `military_combat.go`，归口出兵扣兵、非战斗兵种限制、模拟战斗单位构建和 PVE 场景规则解析。
- 已新增 `unit_registry_test.go`，覆盖兵种注册表、安全复制和兵种名称别名。
- 已从 `units.go`、`model.go`、`service_combat.go`、`service_recruit.go`、`service_resource.go` 中移出军事专属 helper，减少通用文件继续膨胀。
- 文档已补充“兵种/军事系统归口”：新增普通兵种走配置注册表，兵力奖励走统一奖励/兵力入口，玩法专属临时兵种由玩法控制产出和下线。
- 本轮验证目标：`go test ./...`、`git diff --check`。

## 道具/Buff 系统整理第一步

- 本轮继续重构系统整理，进入道具/Buff 系统。
- 共识保持不变：奖励契约和 Modifier 计算属于稳定核心；道具配置、背包状态、道具效果、Buff/DeBuff 状态和具体来源属于应用层系统。
- 已新增 `item_registry.go`，归口道具配置查询、注册判断和全部道具配置列表，并对返回配置做安全复制。
- 已新增 `inventory_state.go`，归口玩家背包道具增加和消耗。
- 已新增 `item_effects.go`，归口道具使用后的效果执行，包括武将经验、资源和按阵营发兵力。
- 已新增 `buff_effects.go`，归口 Buff/DeBuff 状态、过期清理、Modifier scope 校验和 Modifier 来源。
- 已新增 `item_registry_test.go` 和 `buff_effects_test.go`，覆盖道具配置安全复制、Buff scope 校验、过期过滤和状态清理。
- 已从 `items_config.go`、`service_item.go`、`modifier.go`、`service_buff.go` 中移出道具/Buff 专属 helper，减少通用文件继续膨胀。
- 文档已补充“道具/Buff 系统归口”：新增普通道具走配置注册表，道具产出长期资产必须复用已有核心资产入口，Buff/DeBuff 必须先校验 stat key 和 modifier mode 再进入 Modifier 管线。
- 本轮验证目标：`go test ./...`、`git diff --check`。

## 玩法模块边界整理第一步

- 本轮继续重构系统整理，进入玩法模块边界。
- 共识保持不变：信函、万象幻境、后续活动/副本都不是核心地基本身，但必须通过标准核心入口修改长期资产。
- 已新增 `gameplay_module_registry.go`，归口玩法模块边界声明。
- 已登记 `mail` 模块：拥有信函列表、阅读、删除、发送和附件领取规则；附件必须转换为标准 Reward，通过 `ApplyRewardsToStateWithContext` 发放；账号金币附件必须走信函 + 账号 + 玩家组合事务。
- 已登记 `minigame` 模块：拥有小游戏记录、库存和兑换规则；兑换产出必须转换为标准 Reward，通过 `ApplyRewardsToStateWithContext` 发放；跨阵营或不可识别产出不能绕过兵种注册表直接写入玩家兵力。
- `Bootstrap` 模块列表已改为核心模块 + `ListGameplayModuleIDs()`，让玩法模块在服务启动信息里显式出现。
- 已新增 `gameplay_module_registry_test.go`，覆盖默认模块登记、边界声明安全复制和 Bootstrap 模块列表。
- 文档已补充“玩法模块边界”：后续活动/副本应先登记模块边界，再通过奖励、事件、Modifier、建筑变更等核心入口接入长期资产。
- 本轮验证目标：`go test ./...`、`git diff --check`。

## Effect Pipeline 第一版

- 本轮进入最后一块：Effect Pipeline 设计与接入。
- 共识保持不变：玩法模块、武将特性、道具效果、建筑效果不能各自直接改长期资产，应提交标准效果，由统一管线校验并执行。
- 已新增 `internal/core/effect`，定义稳定效果契约，不直接读写玩家存档。
- 当前标准效果类型包括：`reward`、`building_mutation`、`modifier`。
- 已新增 `effect_pipeline.go`，应用层执行器 `ExecuteEffectsOnState` 和服务级事务入口 `Service.ExecuteEffects`。
- `reward` 效果内部复用 `ApplyRewardsToStateWithContext`。
- `building_mutation` 效果内部复用建筑生命周期校验和 `applyBuildingMutation`。
- `modifier` 效果会转换为标准 Buff 奖励，再进入 Modifier 管线。
- `MutateBuilding` 已改为提交 `building_mutation` 效果，由 `Service.ExecuteEffects` 执行。
- `item_effects.go` 已把道具效果转换为标准 `reward` 效果，武将经验、资源、兵力产出不再由道具系统直接写长期资产。
- `ClaimMailAttachments` 已把信函附件转换为标准 `reward` 效果，通过 Effect Pipeline 发放。
- `RedeemMiniGameReward` 和 `RedeemAllFactionMiniGameRewards` 已把万象幻境兑换产出转换为标准 `reward` 效果，通过 Effect Pipeline 发放。
- 已新增 `effect_pipeline_test.go` 和 `internal/core/effect/effect_test.go`，覆盖奖励效果、建筑变更效果、Modifier 效果、服务级资产事件和核心效果 helper。
- 文档已补充“Effect Pipeline”：后续武将特性、活动、副本如果要影响长期资产，应优先提交标准 Effect；如果需要新长期资产容器，先讨论核心是否不完整。
- 本轮验证目标：`go test ./...`、`git diff --check`。

## GM 后台配置补齐

- 本轮目标：根据 Go 后端已暴露的 admin / GM 业务能力，补全 admin 前端配置和操作入口。
- 已补齐 admin API 类型契约：战斗配置、阵营配置、兵种配置、将领配置、特性注册表等不再使用粗泛 `object`。
- 已新增前端扣减城金封装并接入玩家操作页和账号玩家列表页，对齐后端 `/api/v1/admin/gold/deduct`。
- 建筑数值页补齐：加速/兑换因子、升级成本、升级时间、建筑 modifier 等级表；军事建筑这类无产量/容量数组的配置不再被空表漏掉。
- NPC 城池页补齐：tier 随机权重、兵种类型范围、恢复档案 ID、城池名池等字段。
- 战斗规则页补齐：规则新增、删除、名称和模式编辑；被场景引用的规则不能直接删除。
- 阵营配置页补齐：阵营名称、描述、图标、阵营将领列表可编辑。
- 兵种配置页补齐：名称、描述、分类、角色、图标、属性、训练消耗、训练秒数和解锁条件可编辑。
- 将领配置页补齐：等级通用加成、将领基础信息、固定 buff 增删、特性增删和特性切换。
- 钓鱼配置页补齐：稀有度视觉字段、鱼饵 ID/增删、鱼池奖励编辑，并保留高级 JSON 兜底。
- 移动端 GM 导航改为完整横向滚动，配置/接口/审计页在窄屏可达；开发切换按钮避开底部导航。
- 已移除多个 GM 面板中的符号化成功/失败文案，后台状态表达改为纯文本。
- 验证结果：`pnpm lint` 通过；`pnpm build` 通过；本地 Vite smoke 打开 `http://localhost:5176/admin/` 正常渲染，未配置 admin token 时接口返回 HTTP 403 并进入稳定错误态。

## 数据库测试库与迁移工具

- 本轮继续执行目标：新建 `test_` 前缀测试库，本地开发连接测试库，并开始处理未达标架构项。
- 远程 `hero3_user` 没有 `CREATE DATABASE` 权限，直接创建远程 `test_hero3` 返回 MySQL 1044；服务器 SSH 密码登录也不可用。
- 已改为使用本机 MariaDB，通过 socket `/tmp/mysql.sock` 创建本地 `test_hero3`。
- `go/.env` 已切换到本机测试库 DSN：`xieyuqiyu@unix(/tmp/mysql.sock)/test_hero3?...`，该文件不提交。
- `test_hero3` 已通过 `MigrateMySQL` 迁移出 6 张表：`accounts`、`players`、`battle_reports`、`mails`、`minigame_records`、`gold_ledger`。
- 当前测试库是干净结构库，`players` 行数为 0；远程全量数据复制未完成。
- 已新增 `go/cmd/dbtool`：
  - `migrate`：迁移当前 DSN。
  - `create-test-db`：创建当前库对应的 `test_` 前缀库。
  - `migrate-test`：创建并迁移 `test_` 前缀库。
  - `print-test-dsn`：输出当前 DSN 对应的测试库 DSN。
  - `clone-data`：从 `HERO3_SOURCE_DATABASE_DSN` 复制数据到当前 `test_` 目标库，拒绝非 `test_` 目标库和源/目标相同场景。
- `make migrate` 已修正为调用 `dbtool migrate`，不再误启动 HTTP 服务；新增 `make migrate-test`、`make print-test-dsn`、`make clone-data`。
- `HERO3_ENV=development` 时，后端默认拒绝连接非 `test_` 前缀数据库；如确需临时绕过，必须显式设置 `HERO3_ALLOW_DEVELOPMENT_DATABASE=true`。
- 已用临时端口启动 Go 服务验证当前测试库连接，`/healthz` 和 `/api/v1/meta` 正常。
- 已按模块拆分 `transport/api/router.go` 的路由注册函数，降低继续堆长路由列表的风险。
- 已在 MySQL `UpdatePlayerState` 中增加“状态 JSON 未变化则跳过整包写回”的轻量优化，减少无效 `state_json` 更新。
- 本轮验证结果：`go test ./...` 通过；`git diff --check` 通过；`clone-data` 本机空库复制验证通过；同源同目标复制会拒绝执行。

## MySQL 仓储职责拆分

- 本轮继续处理未达标项：原 `internal/infrastructure/storage/mysql.go` 超过 1500 行，账号、玩家存档、战报、信函、小游戏和流水 SQL 全部混在一起，不利于后续拆高频资产表。
- 已将 MySQL 仓储按业务归口拆分：
  - `mysql.go`：连接、DSN 工具和轻量迁移。
  - `mysql_account.go`：账号、账号金币和玩家列表。
  - `mysql_player_state.go`：玩家主状态读写和账号 + 玩家组合事务。
  - `mysql_report.go`：战报归档。
  - `mysql_mail.go`：信函归档和信函 + 玩家 + 账号组合事务。
  - `mysql_minigame.go`：小游戏记录和小游戏 + 玩家组合事务。
  - `mysql_ledger.go`：金币/城金流水审计。
- `mysql.go` 从 1564 行降到约 319 行，后续新增资产表或模块表时应放入对应归口文件，不再把 SQL 堆回单文件。
- 本轮验证结果：`go test ./...` 通过；`git diff --check` 通过。

## 玩家资源影子表第一版

- 本轮开始处理 `players.state_json` 写入偏重的问题，先选择资源作为第一批高频资产拆表地基。
- 已新增 MySQL 表 `player_resources`：
  - `player_id`
  - `resource_type`
  - `amount`
  - `capacity`
  - `updated_at`
- 当前阶段 `player_resources` 是影子表，不是权威读源；玩家接口仍从 `players.state_json` 返回状态。
- 已在这些 MySQL 事务中同步资源快照：
  - `CreatePlayer`
  - `UpdatePlayerState`
  - `UpdateAccountPlayerState`
  - `UpdateMailPlayerState`
  - `UpdateMiniGamePlayerState`
- 同步规则：玩法模块不允许直接写 `player_resources`，必须通过核心玩家状态事务或组合事务同步。
- 已在本地 `test_hero3` 迁移出 `player_resources` 表。
- 已用临时端口启动 Go 服务，创建测试账号和玩家，确认 `player_resources` 写入 4 行资源快照；随后删除测试账号，测试库 `players` 和 `player_resources` 均清理回 0。
- 本轮验证结果：`go test ./...` 通过；`git diff --check` 通过；`make migrate` 对 `test_hero3` 成功。

## 资源影子表回填与一致性校验

- 本轮补齐资源影子表运维入口，避免旧存档或复制来的数据缺少 `player_resources`。
- `go/cmd/dbtool` 新增：
  - `backfill-resources`：从 `players.state_json.resources` 重建 `player_resources`。
  - `verify-resources`：校验 `player_resources` 与 `state_json.resources` 的资源数量和容量是否一致。
- Makefile 新增：
  - `make backfill-resources`
  - `make verify-resources`
- `backfill-resources` 默认要求目标库为 `test_` 前缀；如需非测试库必须显式传 `--allow-non-test`。
- 已验证空测试库回填和校验通过。
- 已用临时账号创建玩家，确认 `verify-resources` 在正常同步状态下通过。
- 已手动篡改一条 `player_resources` 的 wood 数量，确认 `verify-resources` 会失败并报告 1 条不一致。
- 已执行 `make backfill-resources` 修复不一致，再次 `make verify-resources` 通过。
- 已删除临时账号，`test_hero3.players` 与 `test_hero3.player_resources` 均回到 0。
- 本轮验证结果：`go test ./...` 通过；`git diff --check` 通过；`make verify-resources` 通过。

## clone-data 复制后自动刷新资源影子表

- 本轮继续收口测试库迁移流程，避免复制数据后忘记回填资源影子表。
- `go/cmd/dbtool clone-data` 现在默认在复制完成后执行：
  - `backfillPlayerResources`
  - `verifyPlayerResources`
- 如果确实只想复制原始表数据，可以显式传 `--skip-resource-refresh`。
- 已用本机临时库 `test_hero3_clone` 验证：从 `test_hero3` 克隆后输出包含“资源影子表已刷新”，随后清理临时库，只保留 `test_hero3`。
- 本轮验证结果：`go test ./...` 通过；`git diff --check` 通过。

## dbtool 长文件拆分

- 本轮继续处理“未达标项”：新建的 `go/cmd/dbtool/main.go` 已增长到 713 行，虽然功能正确，但重新出现了单文件堆积问题。
- 已按职责拆分数据库维护工具：
  - `main.go`：命令分发和用法输出。
  - `config.go`：读取当前数据库 DSN。
  - `migrate.go`：迁移、创建测试库、输出测试库 DSN。
  - `clone.go`：从稳定库复制数据到 `test_` 目标库。
  - `resources.go`：`player_resources` 回填和一致性校验。
  - `mysql_helpers.go`：MySQL 标识符转义和通用辅助函数。
- 拆分后单文件最大约 290 行，后续新增数据库维护命令应继续按职责放入对应文件，不再把所有逻辑堆回 `main.go`。
- 本轮验证结果：`go test ./...` 通过；`make verify-resources` 通过；`go run ./cmd/dbtool print-test-dsn` 正常输出 `test_hero3` DSN。

## 账号级 Effect Pipeline 入口

- 本轮继续处理核心接入缺口：原 `ExecuteEffects` 只能在玩家状态事务里执行效果，遇到账号金币奖励会拒绝，后续副本/活动如果产出账号金币就需要绕到其他入口。
- 已新增 `ExecuteEffectsWithAccount`，在账号 + 玩家组合事务里执行标准 Effect：
  - 支持标准 Reward Effect 产出账号金币。
  - 账号金币写入和玩家状态变更在同一事务中完成。
  - 金币流水继续复用 `GoldLedgerEntry` 和 `flushRewardSideEffects`。
- 无账号上下文的 `ExecuteEffects` 行为保持不变，仍拒绝账号级奖励。
- 已新增测试覆盖账号金币效果：执行 Reward Effect 后账号金币增加、结果返回正确、持久化账号金币正确、金币流水正确。
- 本轮定向验证结果：`go test ./internal/app/game` 通过。

## 提交前全量验证

- 本轮提交前复核：
  - `go test ./...` 通过。
  - `make verify-resources` 通过，当前 `test_hero3` 玩家 0、资源行 0。
  - `npm run build` 在 `admin/` 通过。
  - `npm run build` 在 `web/` 通过，仅保留 Vite chunk 体积提示。
  - `git diff --check` 通过。
- 当前仍未把资源表切为权威读写源，`player_resources` 仍是影子表；后续应继续推进资源表原子更新和读源切换。

## GM 后台输入框自适应优化

- 用户要求：admin 的 UI 优化，让各种输入框能够自适应。
- 已在 `admin/src/App.css` 增加表单控件兜底规则：普通 input、select、textarea 默认 `min-width: 0`、`max-width: 100%`，并按父容器宽度收缩。
- 已重点改造高密度 GM 配置页：
  - 建筑数值页：基础参数自适应网格、金币兑换参数自动换行、建筑等级表输入从固定宽度改为按列收缩，modifier 表格列改为 `minmax()`。
  - NPC 页：tier 卡片、词条池、恢复档案、侦查消耗改为响应式网格，小数字输入不再固定挤压。
  - 阵营、兵种、将领、战斗、钓鱼、信函、流水、操作、账号页：固定列宽改为 `minmax()`，关键输入改为最小宽度 + 填充可用空间。
- 保留复选框、图标按钮、移动底部导航按钮等需要稳定尺寸的控件，不做无意义拉伸。
- 验证结果：
  - `pnpm lint` 通过。
  - `pnpm build` 通过。
  - Playwright mock admin 配置接口后，在 390px 宽度展开全部配置页，文档 `scrollWidth=clientWidth=390`，140 个 input/select/textarea 未发现越界。
  - 桌面 1280px 检查无页面横向溢出；仅复选框因固定尺寸被粗筛规则标记，符合预期。

## player_resources 同步写入优化

- 本轮继续推进数据库写入性能方向：原 `syncPlayerResourcesTx` 每次同步玩家资源影子表都会先删除该玩家全部资源行，再重新插入。
- 已改为：
  - 从 `resources.items` 和 `resources.capacity` 合并资源类型。
  - 对仍存在的资源类型执行 `INSERT ... ON DUPLICATE KEY UPDATE`。
  - 删除主状态中已经不存在的过期资源类型。
- 这个改动不改变当前权威读源：`players.state_json` 仍是权威，`player_resources` 仍是影子表。
- 已新增 `mysql_player_state_test.go`，覆盖资源类型合并、排序、忽略空 key。
- 本轮定向验证结果：`go test ./internal/infrastructure/storage ./internal/app/game` 通过。

## player_resources 无效同步写入跳过

- 本轮继续压缩无效数据库写：此前玩家状态事务、账号组合事务、信函领取事务、小游戏兑换事务都会调用 `syncPlayerResourcesTx`，即使资源数量和容量没有变化也会 upsert 资源影子表。
- 已在这些入口增加资源快照比较：
  - `UpdatePlayerState`
  - `UpdateAccountPlayerState`
  - `UpdateMailPlayerState`
  - `UpdateMiniGamePlayerState`
- 只有资源数量或容量变化时才同步 `player_resources`；纯信函状态、小游戏记录、账号金币等不改变资源的动作不会额外写资源影子表。
- 由于 Go map 是引用类型，比较逻辑使用 update 前生成的资源快照，避免原地修改 map 导致旧值被污染。
- 已补充测试覆盖原地 map 修改能被检测、等价资源不会误判变化。
- 本轮定向验证结果：`go test ./internal/infrastructure/storage ./internal/app/game` 通过。

## 组合事务无效 UPDATE 跳过

- 本轮继续减少数据库主表无效写入。
- `UpdateAccountPlayerState` 现在只在账号金币变化时更新 `accounts`，只在玩家 JSON 变化时更新 `players`。
- `UpdateMailPlayerState` 现在只在账号金币变化时更新 `accounts`，只在玩家 JSON 变化时更新 `players`；信函领取状态仍按业务需要更新 `mails`。
- `UpdateMiniGamePlayerState` 现在只在玩家 JSON 变化时更新 `players`；小游戏记录库存仍按业务需要更新 `minigame_records`。
- 该策略不改变事务锁定、返回值和模块业务表更新，只减少没有实际数据变化的主表写入。
- 本轮定向验证结果：`go test ./internal/infrastructure/storage ./internal/app/game` 通过。

## 小游戏记录库存无效 UPDATE 跳过

- 本轮继续减少数据库写入：原 `UpdateMiniGamePlayerState` 会遍历玩家全部小游戏记录并执行 `UPDATE minigame_records SET remaining_amount = ?`。
- 已改为只更新 `remaining_amount` 发生变化的记录。
- 对 update 回调中新返回的未知记录 ID，仍保留尝试更新行为，避免改变既有兼容性。
- 已新增 `mysql_minigame_test.go`，覆盖未变化跳过、变化更新、未知记录保持更新兼容。

## 战报单条操作无效 UPDATE 跳过

- 本轮继续收缩无效写入：`MarkSingleReportRead` 只更新未读且未删除的战报；`DeleteReport` 只更新未删除的战报。
- 这不改变当前接口返回语义，因为 MySQL 战报单条操作原本不依赖 affected rows 判断业务错误。
- 根 `.gitignore` 增加 `.playwright-mcp/`，避免浏览器验证临时文件污染后续数据库提交范围。
