# 2026-06-26 记忆

## 驻防/增援系统

- 当前阶段完成玩家之间互相增援基础闭环，不支持对 NPC 城池增援。
- `GameState` 新增 `garrisonArmy`，用于存放驻防军队；主军队 `army` 和驻防军队不得混用。
- 非增援渠道获得兵种时：
  - 本阵营兵种进入 `army`。
  - 非本阵营兵种进入 `garrisonArmy`。
- 玩家主动增援时，发起方扣除对应兵力；抵达目标玩家后，无论本阵营或非本阵营兵种，全部进入目标玩家 `garrisonArmy`。
- 增援接口生成 `reinforce` 类型军情/战报，并分别给发起方和目标方保存记录。
- 新增玩家互攻基础接口，防守方 `army` 与 `garrisonArmy` 共同参与防守计算。
- PVP 防守结算时内部使用带来源的战斗单位 ID 区分主军队和驻防军队；战损按来源分别扣回 `army` 或 `garrisonArmy`，避免同兵种粗暴扣错池子。
- 钓鱼库存兑换规则已更新：非本阵营兵种不再拒绝，兑换后进入驻防军队。

## 前端与文档

- 玩家侧边栏在“军队”下方新增“驻防军队”展示框，桌面端和移动端均可查看。
- 驻防展示只读，空状态显示“暂无驻防兵力”。
- 前端类型、API 客户端和钓鱼库存弹窗已同步 `garrisonArmy` 与新兑换规则。
- OpenAPI 已补充 `garrisonArmy`、玩家攻击、玩家增援、驻防战损字段，并重新生成 `docs/openapi.bundle.yaml`。

## 验证

- `cd go && go test ./...` 通过。
- `cd web && pnpm build` 通过。
- `cd admin && pnpm build` 通过。

## 待办

- 后续可扩展增援行军时间、驻防来源记录、召回、遣返、驻防容量上限和联盟增援加成。
- 当前玩家互攻是基础闭环，尚未做前端主动发起玩家攻击/增援的完整操作界面。

## 公告系统

- 公告系统按纯公告实现，不支持附件、不支持奖励领取、不支持玩家回复。
- 公告和信函系统保持独立：
  - 后端使用 `announcements` 与 `announcement_reads` 独立表。
  - 内存仓储使用独立公告 map 和已读 map。
  - 玩家端公告不进入 `mail` 页面，不产生信函记录。
- 公告支持 `system`、`maintenance`、`event`、`update` 类型。
- 公告支持 `draft`、`published`、`archived` 状态。
- 玩家只看到 `published` 且处于 `startsAt` / `endsAt` 展示窗口内的公告。
- 公告排序规则为置顶优先、优先级高优先、展示/创建时间新优先。
- 玩家端新增 `/notice` 页面和侧边栏公告入口，未读数走独立公告接口。
- GM 后台新增独立“公告管理”页面，支持列表、新建、编辑、发布、下架和软删除。
- OpenAPI 新增公告 paths 和 schemas，并挂载到入口文件。

## 提交前记录

- 本次准备提交并推送 `main` 分支，包含驻防/增援基础闭环和公告系统闭环。
- 保留本地临时端口代理改动不纳入提交：
  - `web/vite.config.ts`
  - `admin/vite.config.ts`

## NPC 甄宓俘虏归属修复

- 明确稳定规则：只有玩家对玩家增援具有“增援归属”语义；增援只发生在 PVP，抵达后全部进入目标玩家 `garrisonArmy`。
- 除增援外，任何渠道获得兵种都按兵种自身阵营分流：本阵营进入 `army`，非本阵营进入 `garrisonArmy`；未知阵营保守进入驻防。
- 已把该规则写入根目录 `PROJECT_RULES.md`。
- NPC 甄宓俘虏原先沿用 PvE `SameFaction=true`，导致非本阵营 NPC 守军被俘虏后进入主军队。
- NPC 结算改为 trait 只计算俘虏数量，应用到玩家状态时按兵种阵营分流；非本阵营 NPC 俘虏进入驻防，并同步从 NPC 守军扣除。
- 已补测试覆盖魏国甄宓攻击蜀国 NPC，俘虏蜀步兵进入 `garrisonArmy`、战报写 `capturedToGarrison`。
- 本次验证：
  - `cd go && go test ./...` 通过。

## v0.1.0 发布

- 准备发布第一个正式版本 `v0.1.0`。
- 后端默认版本已为 `0.1.0`；同步玩家端和 GM 后台 `package.json` 版本到 `0.1.0`。
- 发布范围包含驻防/增援基础、PVP 行军攻击、地图玩家 tab、钓鱼库存分页与兑换归属、PVP/NPC 甄宓俘虏归属修复，以及线上历史数据修复记录。

## PVP 行军攻击基础

- 玩家攻击玩家改为创建 PVP 行军，不再立即结算；出征兵力从攻击方主军队扣除，到达后由 `SettleDueMarches` 统一结算。
- 行军记录支持出征方/防守方可见；防守方 incoming 行军不暴露敌方兵力。
- 行军时间由最慢出征兵种速度、最短/最长时长和速度倍率计算，并在行军创建时固化配置。
- 行军支持城金加速；每次按固化配置扣城金并减少剩余时间，不能低于固化最短剩余时间。
- PVP 到达结算时防守方 `army` 与 `garrisonArmy` 共同防守，主军与驻防战损分别扣除并写入战报字段。
- 防守方无守军时按攻击方胜利处理，攻击方不损兵，双方生成战报。
- MySQL 新增 `pvp_marches` 表，内存仓储同步支持行军创建、查询、到期领取和更新。
- 玩家端 PVP 入口调整到地图页“玩家”tab，不使用独立征战页；玩家目标按 NPC 城池一致的卡片式展示，可选择主军兵力出征、查看 outgoing/incoming 行军并加速 outgoing 行军。

## PVP 待办

- 已补 GM 后台独立“行军配置”面板，可维护最大/最短行军时间、速度倍率和加速规则。
- 已补 OpenAPI 的 PVP paths/schemas、GM 行军配置接口、PVP 战报字段和旧玩家攻击兼容入口说明。
- 已补 PVP 战报详情展示：双方信息、主军/驻防参战和损失、掠夺资源、防守方无守军、将领特性结果。
- 已补后端 PVP 测试：自己/同账号/兵力不足/非战斗兵校验、行军配置固化、incoming 隐藏兵力、加速固化配置、无守军幂等结算、掠夺上限和特性/经验写战报。
- PVP 结算已接入现有将领特性事件管线和将领经验计算；甄宓跨阵营俘虏进入攻击方驻防。
- PVP 出征新增仓储组合事务：保存攻击方状态和创建 march 一起提交。
- PVP 到达结算新增仓储组合事务：保存双方状态、双方战报和 march resolved 一起提交。

## PVP 提交前验证

- `cd go && go test ./...` 通过。
- `cd web && pnpm build` 通过。
- `cd admin && pnpm build` 通过。
- `make openapi` 通过，已重新生成 `docs/openapi.bundle.yaml`。
- 保留本地临时端口代理改动不纳入提交：
  - `web/vite.config.ts`
  - `admin/vite.config.ts`

## 甄宓俘虏与钓鱼库存修复

- 甄宓 PVP 俘虏原先按攻防双方玩家阵营分流；同为魏国玩家时，防守方驻防里的蜀兵会被错误写入攻击方主军队。
- PVP 到达结算改为按被俘虏兵种自身阵营分流：攻击方本阵营兵种进入 `army`，非本阵营兵种进入 `garrisonArmy`；未知兵种保守进入驻防。
- 钓鱼库存弹窗原先分页读取全部钓鱼历史记录；历史达到数百页且前面多为已兑换记录时，库存页会显示空，导致玩家看不到可兑换记录，也看不到全部兑换入口。
- 小游戏记录接口新增 `stockOnly=true` 查询开关，库存弹窗只分页 `remainingAmount > 0` 且有奖励兵种的可兑换记录；后台默认仍可查全部历史。
- 钓鱼库存文案同步为：本阵营奖励进入军队，非本阵营奖励进入驻防军队。
- 本次验证：
  - `cd go && go test ./...` 通过。
  - `cd web && pnpm build` 通过。
  - `cd admin && pnpm build` 通过。
  - `make openapi` 通过，已重新生成 `docs/openapi.bundle.yaml`。
  - `git diff --check` 通过。
- 保留本地临时端口代理改动不纳入提交：
  - `web/vite.config.ts`
  - `admin/vite.config.ts`
