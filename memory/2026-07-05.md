# 2026-07-05 记忆

## 黄巾起义第一版开发

- 按 `docs/系统设计/黄巾起义最终开发文档.md` 开发黄巾起义第一版，保持“稳定核心 + 玩法模块”边界。
- 新增军事建筑“千帐营”，老存档和新存档都会拥有 1 级千帐营；千帐营使用金币升级，当前配置扩展到 25 级，满级口粮上限 100 亿，最高级升级费用 3000 金币。
- 新增 `go/config/yellow_turban.json` 和 `game_configs.yellow_turban` 配置入口，GM 后台可编辑完整黄巾配置并手动触发一次全服检测。
- 玩家当前口粮按现有兵种配置 `stats.upkeep` 计算，只统计战斗兵种，不统计商人/运输/非战斗单位。
- 军事视图返回 `foodPressure`，玩家左侧军队卡片展示“当前口粮 / 千帐营上限”，超限时红色显示风险等级和压力百分比。
- 黄巾城池在世界地图中以 `yellow_turban` 假玩家势力展示，不创建真实账号或玩家，不进入排行榜、联盟、普通 PVP 目标身份。
- 黄巾来袭使用独立 `yellow_turban_marches` 队列保存来源城池、压力等级、玩家口粮、千帐营上限和黄巾兵力快照。
- 黄巾派兵按玩家当前总口粮乘以压力等级倍率生成，不按超出部分计算；黄巾兵种从魏/蜀/吴对应阵营战斗兵种中生成，排除侦察和非战斗单位。
- 黄巾行军按世界地图距离和黄巾速度倍率计算，到达后复用战斗引擎结算玩家防守兵力损耗，黄巾不掠夺、不返程、战斗后消失。
- 黄巾战报通过统一战报入口保存为防守战报，来源为 `yellow_turban`。
- 当前已提供玩家状态/检测/结算接口和 GM 全服检测入口；常驻每 10 分钟自动分片扫描 daemon 尚未启用，后续应基于现有检测服务接调度器。
- 验证通过：`go test ./...`、玩家前端 `npm run build`、GM 后台 `npm run build`、`python3 scripts/openapi_bundle.py`。

## 黄巾起义发布与迁移

- 用户要求发布并迁移线上数据库。
- 发布前确认当前分支为 `main-core`，远程仓库为 `https://github.com/xieyuqiyu-source/Hero3.git`。
- 本次线上数据库结构变更通过后端启动迁移创建 `yellow_turban_marches`，不手工直接改线上库。
- 首次发布提交 `4b01af6` 已推送并触发 Actions，但线上服务启动失败，日志显示 `yellow turban config load failed`，原因是校验误把风险等级第一档 `minPressure = 1.0` 判定为无效。
- 已修复黄巾配置校验，允许文档要求的 100% 起步阈值，并补充默认配置校验测试；验证通过 `go test ./...`。
- 发现生产环境 `hero3-server` 默认跳过启动迁移；为满足本次“发布并迁移”要求，发布脚本改为安装新 `hero3-dbtool` 后从 `/etc/hero3/hero3.env` 读取数据库地址并显式执行 `/opt/hero3/go/bin/hero3-dbtool migrate`，再重启服务。
- 迁移脚本首次尝试直接 source `/etc/hero3/hero3.env`，因线上 DSN 含 `tcp(...)` 且 env 文件未按 shell 语法加引号导致失败；修正为只提取 `HERO3_DATABASE_DSN=` 行并导出给 `hero3-dbtool migrate`。
- 线上 DSN 行存在反斜杠续行，单行读取会把 `parseTime=true\` 传给 MySQL 驱动；发布脚本继续修正为支持 DSN 续行拼接。
- DSN 续行解析需兼容 CRLF 行尾，否则 `\r` 会导致反斜杠续行判断失效。
- 为避免发布脚本自解析线上 env 文件继续偏离 systemd 语义，迁移改为通过 `systemd-run --property=EnvironmentFile=/etc/hero3/hero3.env` 执行临时服务，让 systemd 读取同一份环境文件。
- systemd-run 已成功进入真实迁移阶段，但首次连接本机 MySQL 时出现 `i/o timeout`；发布脚本增加迁移 3 次重试，降低线上库瞬时繁忙导致发布失败的概率。
- 三次迁移重试仍然在连接本机 MySQL 时超时，判断为运行中服务对数据库压力较高；发布脚本改为迁移前短暂停止 `hero3` 服务，迁移完成后再启动，迁移失败时也尝试重启服务避免中途停服。
- 停止服务后 MySQL 仍在约 5 秒读超时，说明迁移命令沿用了服务短超时 DSN；已改造 `hero3-dbtool migrate`，迁移时自动将 MySQL 连接超时提升到 30 秒、读写超时提升到 60 秒，并补充 DSN 超时单元测试。
- 后续迁移进入等待阶段后触发 dbtool 自身 30 秒 context deadline；继续将 `hero3-dbtool migrate` 总时限提高到 5 分钟，发布脚本外层迁移超时提高到 360 秒。
- 全量 `hero3-dbtool migrate` 在生产热库会碰到老表元数据锁，不适合为单个玩法表执行；已新增 `hero3-dbtool migrate-yellow-turban`，只创建 `yellow_turban_marches`。黄巾队列表取消数据库外键，保留 `target_player_id` 应用层关联，以符合玩法模块独立队列边界并避免锁住 `players` 热表。
- 最终发布 run `28731804750` 成功，日志确认线上库 `new_hero3` 已完成黄巾定向迁移，服务健康检查通过。

## 黄巾前端千帐营入口修复

- 用户指出前端城池建筑页缺少“千帐营”升级入口。
- 原因是城池军事建筑列表为前端手写清单，后端已创建 `thousand_tent_camp`，但前端未加入建筑卡片。
- 修复方向：在城池军事建筑页新增“千帐营”金币升级卡，显示安全口粮上限当前值和下一级变化；黄巾据点页“升级千帐营”直接跳转到城池军事建筑页并高亮千帐营。

## 黄巾据点归属与来袭提醒调整

- 用户明确黄巾起义应归入“据点”体系，表现上和副本入口类似，不应作为地图顶层独立页签。
- 黄巾来袭属于玩家被攻击提醒，应复用左侧菜单行军提醒中的“被攻击”样式，而不是放在黄巾起义界面内部。
- 前端调整方向：地图顶层移除“黄巾起义”页签，新增据点页承载黄巾面板；黄巾面板去掉来袭队列列表；左侧 `MarchAlertTags` 拉取黄巾来袭并统一展示为“被攻击”倒计时。

## 黄巾自动触发和口粮字段修复

- 用户反馈线上出现 `Error 1264 (22003): Out of range value for column 'player_food'`，原因是 `yellow_turban_marches.player_food` / `food_capacity` 使用 `INT`，无法承载亿级以上口粮快照。
- 修复方向：黄巾来袭表口粮快照字段改为 `BIGINT`，定向迁移 `migrate-yellow-turban` 会修复已有线上表。
- 用户确认黄巾起义应自动触发，不应由玩家手动检测；玩家侧“立即检测”按钮和玩家检测路由移除。
- 后端服务启动黄巾自动调度器，启动后短延迟执行首次全服检测，之后按黄巾配置 `checkIntervalMinutes` 周期自动扫描；GM 全服手动检测入口继续保留给运营排查。

## 黄巾防守武将规则补齐

- 用户询问 PVP 和黄巾起义的玩家防守是否有武将加成，并要求没有则补上，战报显示防守方武将和经验值。
- 确认 PVP 防守已有主将加成、防守武将快照和经验发放；黄巾防守此前只扣兵和写战报，未接防守主将加成与经验。
- 修复方向：黄巾防守复用 PVP 守城主将规则，防守主将提供守城 modifier 和防守侧特性；按黄巾损失发放防守武将经验，并在黄巾防守战报写入 `pvpDefenderGenerals`、`generalExpGained` 和等级变化字段。

## 黄巾防守协防规则补齐

- 用户追问黄巾打玩家时协防是否触发。
- 确认 PVP 防守已触发 `stationed` 驻防援军；黄巾防守此前没有读取驻防援军，只结算玩家本城兵力。
- 修复方向：黄巾到达结算改为事务内同时锁定黄巾来袭、防守玩家和已驻防可参战援军；协防援军加入防守阵列，按 PVP 防守来源分摊规则写回损耗。
- 战报规则：防守方黄巾战报显示协防快照和协防损失；协防派出方在实际参战并产生损耗时获得独立协防战报，来源和战斗类型均标记为 `yellow_turban`。

## 千帐营 25 级配置调整

- 用户要求千帐营可升级到 25 级，满级口粮上限调整为 100 亿。
- 按“新增 21-25 五档分别为 20/40/60/80/100 亿”的口径落地；保留 1-20 级原有曲线，最高级金币升级费用从 2000 顺延到 3000。
- 线上黄巾配置优先读取 `game_configs.yellow_turban`，因此新增 `hero3-dbtool sync-yellow-turban-config`，发布流程在迁移后自动把随版本发布的 `go/config/yellow_turban.json` 同步到数据库配置。

## 协防武将战报和经验修复

- 用户反馈协防武将没有出现在协防战报中，且协防战报出现不适用的 25% 展示。
- 修复方向：标准战报在 `reinforcement` 视角从 `pvpReinforcements.generals` 生成主侧武将列表；协防视角不再填充敌方损失暴露阈值，避免显示默认 25%。
- 黄巾协防战报生成后，会按协防战报中的 `generalExpGained` 给实际参战援军携带武将补发经验；PVP 协防继续复用同一套补发函数。

## 左侧行军提醒自动刷新

- 用户询问左侧菜单是否必须刷新页面才会更新“被进攻”提醒。
- 确认 `MarchAlertTags` 已有 12 秒轮询和到达后刷新，但黄巾状态返回的 `foodPressure` 未同步进全局状态，且浏览器切回页面时没有立即刷新。
- 修复方向：行军提醒轮询成功读取黄巾状态后同步 `foodPressure` 和 `serverTime` 到全局状态；窗口重新聚焦或标签页恢复可见时立即刷新行军提醒。

## 驻防武将展示强化

- 用户追问驻防部队是否不显示将领。
- 确认真实增援驻防记录和战斗快照均带 `generals`；获得驻防/俘虏驻防通常没有携带武将。
- 修复方向：左侧驻防卡片增加醒目的“驻防武将”区域和武将图标；战斗结果弹窗的参战信息展开驻防/援军明细，显示兵力和携带武将。
