<!-- 本文件记录 2026-07-23 的关键开发决策、实现事实、验收结果和待办。 -->

# 2026-07-23 项目记忆

## 许褚特性更新

### 决策

- 虎痴冲阵仅在许褚主动进攻战斗前判定，默认 50% 概率使敌方全军步兵、骑兵防御降低 30%；概率和比例可由 GM 配置。
- 虎虎生威是永久被动，使许褚所率虎豹骑每兵固定增加 12 点攻击和 5 点移动；主动进攻、防守和增援持续生效，不进行概率判定，不进入战斗触发时间线。
- 旧破敌防御不再是当前特性。存量旧 ID 只允许在配置载入时迁移为虎虎生威，不得继续注册或执行旧破防逻辑。
- 两项可在主动进攻中同时生效：被动攻击先进入核心输入，虎痴命中后修改敌军防御，再用最终攻防计算真实兵损。

### 实现

- 正式配置、特性目录和配置迁移已切换到虎痴 `50% / -30%` 与虎虎生威虎豹骑 `+12 / +5`；当前 GM 显式配置值保留。
- 通用指定兵种固定攻击、移动 Modifier 同时接入 PVP 战斗、出征行军和增援速度；许褚没有新增专属核心分支。
- `web` 和 `web-wlsg` 同步当前元信息、将领特性列表和战报被动分栏；虎痴只按后端真实结果进入触发时间线。
- 新增许褚真实 PVP、配置迁移、被动属性、NPC、方向、概率和前端战报测试，并清理旧双破防断言。
- 新增 `docs/系统设计/许褚特性更新开发文档.md`，同步项目索引、文档目录、现有特性盘点、平衡设计和短期治理文档。

### 当前验收

- 真实 PVP 固定矩阵：无被动时攻击 `3000`、守军损失 `247`、行军 `2054` 秒；被动生效且虎痴未命中时攻击 `4200`、守军损失 `400`、行军 `1450` 秒；虎痴命中后防御战力 `8000 -> 6000`、守军损失 `602`。
- 虎痴命中战报准确记录步防 `-3`、骑防 `-2`；双方库存、经验、最终死伤和战报已对账。
- 许褚专项 Go `-race -count=3` 通过；`web` 236 项、`web-wlsg` 287 项测试和两个生产构建通过。
- Go 全量首次运行发现一条旧 `-3/-3` 测试文字，已按当前整数结果修正为 `-3/-2` 并定向连续 3 次通过；修正后的 `go test ./... -count=1` 全量通过。
- 本机 Chrome 使用同一许褚存档完成两端验收：`web` 准确显示虎痴 `50% / -30%` 与虎虎生威虎豹骑 `+12 / +5`，`web-wlsg` 正常进入许褚军事页；两端均无控制台警告、错误或横向溢出。
- 许褚改动将与工作区中已验收的夏侯渊、张辽、典韦改动合并为同一版本提交并推送到 `main-core`。

## 典韦特性更新

### 决策

- 护主血战仅在典韦守城或作为援军时于战斗前生效，默认必定使所率禁卫甲士步兵防御、骑兵防御各增加 20 点；主动进攻无效，概率和固定值可由 GM 配置。
- 死战到底仅在典韦主动进攻战斗前判定，默认 60% 概率使所率步兵攻击增加 35%；防守和增援无效，概率和比例可由 GM 配置。
- 两项按方向互斥，命中后必须先修改攻防，再由现有核心重新计算战力、真实兵损、经验和权威库存。
- 已废弃的典韦战败返兵规则不再注册或执行；旧 ID 只作为存量配置迁移输入，不保留旧结果条件、返兵比例或上限。

### 实现

- 正式配置、特性目录和配置迁移已切换到 `huzhu_xuezhan` 与当前 `sizhandaodi`；GM 当前显式自定义值保留。
- 护主血战复用通用固定防御修正，只修改 `jinWeiSoldier` 的步防和骑防；死战到底复用通用步兵攻击比例修正，没有改动稳定核心公式。
- PVP、NPC、黄巾、轮回和真实增援已同步当前方向、概率、战前输入、战报逐项实际变化和权威状态校验。
- `web` 与 `web-wlsg` 已清除旧名称和旧返兵展示，改为当前名称、阶段、概率、数值和后端真实触发结果。
- 新增 `docs/系统设计/典韦特性更新开发文档.md`，同步项目索引、文档目录、现有特性盘点、平衡设计和短期治理文档。

### 当前验收

- 主动 PVP：死战未命中时战力 `1000/10000`、双方损失 `96/36`；命中时战力 `1400/10000`、双方损失 `94/57`，实际步兵攻击 `+4`。
- 守城 PVP：护主关闭时防御战力 `1300`、双方损失 `108/100`；生效时防御战力 `3300`、双方损失 `200/49`，实际步防、骑防各 `+20`。
- NPC 命中/未命中固定验证敌军损失 `120/74`；黄巾护主生效固定验证防御战力 `3399`、来袭方全损 `200`、禁卫甲士损失 `47`；真实增援验证防御战力 `1010 -> 3010` 和三方权威状态。
- 典韦专项 Go `-race -count=3`、Go 全量、`web` 236 项、`web-wlsg` 287 项测试和两个生产构建均通过。
- 本机 Chrome 使用同一典韦存档完成两端验收：React 将领页准确显示护主 `100% / 双防 +20` 与死战 `60% / 攻击 +35%`，Vue 正常进入典韦军事页；两端均无控制台警告、错误或横向溢出。
- 2026-07-23 用户要求提交推送；夏侯渊、张辽、许褚、典韦四位将领的已验收改动将合并为同一版本推送到 `main-core`。
