# 2026-06-30 项目记忆

## NPC 扫荡死锁排查与修复

- 用户反馈 NPC 扫荡时出现 `Error 1213 (40001): Deadlock found when trying to get lock; try restarting transaction`，前端表现为“扫荡完成：成功 3 场，失败 1 场”。
- 定位为 NPC 城池扫荡逐场调用 `/api/v1/map/npc-cities/attack`，其中某一场攻击在 MySQL 资产事务写入玩家状态、资源、兵力、武将经验、NPC 快照等数据时遇到 InnoDB 死锁。
- 判断原因：近期战报、资产权威表、武将经验和奖励流水接入后，单场 NPC 攻击事务触达表和索引增多；扫荡连续快速攻击时更容易暴露数据库并发锁冲突。
- 修复方案：在 `Service.AttackNpc` 的核心 `UpdateCombatState` 事务外增加可恢复数据库冲突短重试，识别 `deadlock`、`Error 1213`、`40001`、`lock wait timeout`、`Error 1205`，最多重试 3 次。
- 重试前会清空本次尝试的临时战报、奖励、俘虏驻防等变量，确保只重试未提交的核心事务，避免重复扣兵、重复发奖或重复攻击。
- 验证通过：`go test ./internal/app/game -run 'TestAttackNpc|TestStorageConflict' -count=1`，以及 `go test ./internal/app/game ./internal/transport/api ./cmd/dbtool`。
- 发布提交 `a1ad02f` 触发 Actions `Deploy Hero3 #64`，失败点仍在服务器 `Build and deploy on server`；本地复验通过 `go build ./cmd/server`、`go test ./internal/app/game ./internal/transport/api ./cmd/dbtool`、`pnpm --dir web build`、`pnpm --dir admin build`，准备追加重试发布提交。

## 将领、军队提示和 NPC 配置版本

- 本轮准备提交剩余工作区改动：将领更换消耗账号金币并记录流水、将领加点支持一次分配多个点、军事视图补充将领冷却与账号金币字段。
- 前端军事将领面板同步支持批量加点、更换将领消耗展示、账号金币刷新和冷却提示。
- 左侧行军提示调整优先级与样式，被攻击优先于出征、被侦查和增援展示。
- NPC 配置调整小型/大型/金色城池保底数量，并保留各层级掉落池配置。
- 新增 `docs/系统设计/将领军队背包需求排查方向.md`，并同步文档目录。

## 联盟入口占位

- 用户要求左侧菜单原“增援”按钮改为“联盟”，点击跳转联盟占位页。
- 已将桌面侧边栏和移动侧边栏菜单项从 `reinforcements / 增援` 改为 `alliance / 联盟`，并使用联盟图标；现有 `/alliance` 路由与占位页可直接承接。
- 验证通过：`pnpm --dir web build`。

## 内部工具入口可见性

- 用户要求左侧菜单“帮助”按钮和“战斗模拟”只允许 `xieyuqi` 账号查看。
- 实现保持为前端轻量可见性控制：复用登录后缓存和状态中的账号名，只有账号名为 `xieyuqi` 时显示桌面/移动端帮助入口，并渲染设置页战斗模拟模块。
- 直接访问帮助页时，非 `xieyuqi` 账号显示暂无权限提示，不加载帮助文档。
- 验证通过：`pnpm --dir web build`。

## PVP 返程时长与提示修正

- 用户确认 PVP 战后返程规则：从出征开始到实际抵达目标花了多久，战后返程就花多久；如果中途使用加速缩短去程，返程也按缩短后的实际去程时间计算。
- 后端 `ResolvePvpMarch` 不再在战后返程时重新按兵种速度和加成计算，而是使用 `ArrivesAt - StartedAt` 作为返程秒数；旧数据时间异常时回退原 `DurationSeconds`。
- 新增回归测试覆盖两次行军加速后，战后返程时间等于实际加速后的去程时间。
- 前端左侧行军提示修正：自己的 PVP 行军状态为 `returning` 时显示绿色 `返程` 标签，并使用 `returnsAt` 倒计时，不再显示红色 `出征`。
- 验证通过：`go test ./internal/app/game`、`pnpm --dir web build`、`git diff --check`。

## 线上启动迁移异常

- 用户反馈服务器异常，检查最新部署 Actions 发现后端安装成功后健康检查 `127.0.0.1:8081/healthz` 连接失败，公网 `/healthz` 返回 502。
- 线上 `journalctl -u hero3` 显示后端反复在启动迁移阶段报 MySQL `i/o timeout` 和 `database migration failed: invalid connection`。
- MySQL 进程列表确认多个重启进程堆积同一条 `UPDATE battle_reports SET view_type = type WHERE view_type = '' OR view_type = 'attack'`，生产战报约 7.6 万条，其中绝大多数正常攻击战报都会被这条条件反复命中。
- 已先在线上 `systemctl stop hero3` 并杀掉堆积的旧 UPDATE，避免重启循环继续拖住数据库。
- 修复方向：战报兼容回填改为小批次执行，并只修复 `type` 与 `view_type` 不一致或空字段的旧数据；不再把所有 `view_type='attack'` 的正常战报纳入启动迁移。
- 验证：生产库按新条件检查待修复战报为 0；`go test ./internal/infrastructure/storage -run 'TestMySQLBattleReport|TestMySQLBattleReportStateID' -count=1` 和 `go test ./internal/app/game -count=1` 通过。

## 经验包使用死锁

- 用户截图反馈一次性使用 999 个“将领经验包”时报 `Error 1213 (40001): Deadlock found when trying to get lock; try restarting transaction`。
- 线上日志显示同一时间大量 `/api/v1/map/npc-cities/attack` 在 NPC 扫荡事务内遇到死锁并重试，期间也有 `/api/v1/items/use` 请求；MySQL 进程列表可见同一玩家多个 `SELECT ... FOR UPDATE` 和 `DELETE FROM player_inventory` 并发。
- 判断原因：NPC 攻击之前只有数据库死锁重试，但没有按玩家进入 `playerLocks` 串行；道具使用有玩家锁但缺少数据库死锁重试，因此在高频扫荡和使用经验包同时发生时，错误会直接透给前端。
- 修复：`UseItem` 的 `UpdateItemState` 主事务增加和 NPC 攻击一致的可恢复死锁短重试，重试前清空本次临时效果、资产快照和道具数量，避免失败尝试污染最终返回。
- 修复：`AttackNpc` 进入玩家资产事务前按 `playerId` 加 `playerLocks`，让同一玩家的 NPC 攻击、扫荡和道具使用串行进入核心资产写入，减少同玩家并发事务互锁。
- 验证通过：`go test ./internal/app/game -run 'TestUseItem|TestGrantItem|TestAttackNpc|TestStorageConflict' -count=1`、`go test ./internal/app/game -count=1`、`git diff --check`。
