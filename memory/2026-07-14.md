# 2026-07-14 项目记忆

## 本次版本

- 军情页面接入真实后端接口，保留最新、攻击、防守、军事增援、侦查五个标签；未读情报进入最新，阅读后按类型归档。
- 世界地图行军状态区分进攻、增援和被增援，多支队伍逐条展示，并接入加速行军与召回能力；小地图可展示同账号多个存档城池。
- 战报详情按官网结构完善参战方、武将身份、经验、特性触发说明、兵种出动与阵亡、资源、掉落和城金；NPC 防守方没有武将时保留标准占位结构，并修正战损阈值与内部状态码直出问题。
- 地图新增独立 NPC 页面，12 座真实 NPC 城池按小型、中型、大型、超大四色同页展示；点击城池打开官方风格战争命令弹窗。
- NPC 攻击、掠夺、侦查接入现有后端接口并即时结算，不产生行军时间或右侧行军队列。
- NPC 手动刷新改为每次消耗 100 账户金币；扣费和重新生成在同一后端资产事务内完成，余额不足时金币与 NPC 状态均不改变。
- 生产部署工作流的玩家前端构建源由旧 `web/` 切换为 `web-wlsg/`，产物继续部署到 `/var/www/hero3/web/`，保持现有域名和 Nginx 配置不变。

## 验证结果

- Go NPC 服务与 API 定向测试通过。
- `web-wlsg` 共 14 个测试文件、83 项测试通过。
- `web-wlsg` TypeScript 校验与 Vite 生产构建通过。
- OpenAPI 校验、打包与 `git diff --check` 通过。
- NPC 真实接口页面已在 1280×800 和 1440×900 两档视口完成截图验证。

## 发布规则

- 推送 `main-core` 会触发 `.github/workflows/deploy.yml` 的生产发布。
- workflow 在服务器构建 `web-wlsg/`，再将 `dist/` 同步到 `/var/www/hero3/web/`；GitHub Actions 页面也可对 `main-core` 手动运行 `Deploy Hero3`。
