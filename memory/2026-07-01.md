# 2026-07-01 记忆

- 线上玩家 `xiaomimi` 反馈 NPC 扫荡偶发 `Error 1213 Deadlock found when trying to get lock`。排查 `hero3.service` 日志，确认集中在同一存档高频 NPC 攻击/扫荡时出现，符合手机和电脑同时操作同一存档导致事务抢锁的表现。
- 架构处理共识：对同一 `player_id` 的 NPC 攻击/扫荡做串行保护；如果同一存档已有 NPC 操作正在执行，新的 NPC 操作不排队等待，直接返回友好错误 `操作太快，请稍后再试`，HTTP 状态为 `429 Too Many Requests`，减少数据库错误直接暴露给玩家。
- 实现调整：`AttackNpc` 和 `SweepNpc` 在公开入口尝试获取玩家锁，内部 `attackNpc` 不再重复拿锁，避免批量扫荡期间被另一端同存档 NPC 操作插队。
- 测试补充：新增同一存档扫荡执行中再次 NPC 攻击会返回 `ErrOperationTooFast` 的测试，确保不会进入第二个存储事务。
- 前端体验优化：一键扫荡弹窗和军情战报详情的宝物掉落按物品标识和品质聚合展示，避免同一物品掉落多次时弹窗过长。
- 验证：后端通过 `go test ./internal/app/game ./internal/transport/api`；前端通过 `npm run build`。
- 存档删除保护：删除存档改为 1 小时冷静期，首次删除只写入 `deleteRequestedAt/deleteScheduledAt`，存档列表显示倒计时并提供恢复按钮；到期后刷新存档列表触发真正硬删除。前端云同步弹窗和账户页都展示倒计时/恢复，删除入口加两次确认。
- 新增接口：`DELETE /api/v1/players/{playerId}` 负责申请删除或到期硬删除；`POST /api/v1/players/{playerId}/restore-delete` 负责取消待删除状态。
- 验证：延迟删除版本通过 `go test ./internal/app/game ./internal/transport/api` 和 `npm run build`；额外跑 storage 集成包时有 3 个既有 MySQL 更新时间断言失败，未作为本次阻塞项。
- 线上误删恢复：`xiaomimi` 账号的 `笑眯眯` 存档被旧接口硬删除后，用 `old_hero3` 旧快照恢复基础资产，再从 binlog 删除前战报和流水校准兵力、张辽等级与经验。最终确认张辽为 82 级，常规兵力按误删前普通战报恢复，小游戏记录原本仍保留。
- 恢复校正：一键扫荡战报中的兵力是多场聚合展示，不能作为实际存档兵力；恢复实际兵力时以误删前最后普通 NPC 战报为准。发现恢复时遗留 `current_general` 和 `main` 双占用，已清理多余 `current_general`，只保留 `main -> zhangliao`。
- 手机端体验：修复移动端左侧菜单缺少将领卡的问题，手机菜单现在显示当前将领、等级和忙碌状态，点击进入 `军事 -> 将领`。
- GM 配置体验：后端和 `balance.json` 已支持 `goldUpgradeCostByLevel`，但 GM 建筑数值面板此前未显示账号金币升级费用。本次补齐金币费用列，并补全攻城武器营、特殊建筑营、建造司等中文建筑名及攻城/特殊征兵加成文案。
- 当前配置测算：攻城武器营从 0 升满 25 级约需 `37,746` 金币，特殊建筑营约需 `56,623` 金币，两者合计 `94,369` 金币。
- 存档删除到期体验修复：前端云同步弹窗和账户页会识别 `deleteScheduledAt` 到期状态，自动刷新并把按钮从“恢复”切换为最终“删除”；后端补充到期后再次删除会硬删除的测试。
- 将领经验包修复：后端将普通物品使用结果中的 `cityGold` 改为可选字段，只有真实查询/变更城金的物品结果返回；将领经验包不再返回 `cityGold:0`，避免前端左侧城金被误覆盖为 0。
- 赌场开放记录与兑换：赌场记录使用 `minigame_records`，胜利记录会产生 `remainingAmount`，可像钓鱼库存一样分组、分页、单项兑换或全部兑换；旧赌场胜利记录通过一次性 MySQL 迁移回填可兑换库存，避免重启反复恢复已兑换库存。
- 赌场押注扣兵：新增 `POST /api/v1/minigame/gambling/resolve`，赌场每局由后端事务统一完成校验兵种、扣除押注兵力、服务器掷骰、判定输赢、写入记录、返回最新军队；旧 `/minigame/record` 禁止 `gameType=gambling`，防止伪造中奖记录。
- 前端赌场改为调用后端结算接口，动画播放后端返回的骰子和结果；胜利奖励进入赌场库存，输局只记录历史且库存为 0。
- 本批验证：通过 `go test ./internal/app/game -run 'Test.*Gambling|Test.*MiniGame|TestUseItemConsumesInventoryAndAppliesGeneralExp|TestDeletePlayer|TestListPlayersPurgesDueDeletedPlayers' -count=1`、`go test ./internal/infrastructure/storage -count=1`、`go test ./internal/transport/api -count=1`、`npm run build`、`git diff --check`。
- 天机轮转小游戏开发：按 `docs/系统设计/天机轮转老虎机系统设计.md` 接入万象幻境第三个小游戏，后端新增 `POST /api/v1/minigame/slot/resolve`，使用 `gameType=slot` 复用 `minigame_records`，押注先扣兵，服务器加密随机抽取三轴图案，三同才中奖，中奖库存进入小游戏兑换体系。
- 天机轮转配置：新增 `go/config/slot.json` 和 `slot_config.go`，配置最小押注 1000、单局固定上限 1000000、当前兵种 5% 比例上限，以及玄铜符/白银符/赤金符/玉玺/虎符/天命令权重和倍率。
- 天机轮转前端：玩家端 `地图 -> 万象幻境` 新增“天机轮转”卡片、老虎机三轴动画、押注兵种/数量选择、后端落点播放、最近记录和“天机库存”兑换弹窗；GM 后台万象幻境记录面板新增 slot 记录展示。
- 接口与文档同步：OpenAPI 新增 slot 结算接口、slot 枚举和 Slot 配置/响应 schema；根 README、Go README、Web README 补充万象幻境和天机轮转说明。
- 黄巾起义设计共识：黄巾起义放在 `地图 -> 黄巾起义`，不作为副本、据点或赛季；玩法核心是玩家口粮压力超过阈值后，系统按等级和波次生成黄巾军进攻玩家城池。
- 黄巾起义实现方向：采用方案 B，不创建真实系统玩家，不污染 `accounts/players`、排行榜、联盟或玩家目标列表；黄巾攻击方用虚拟 `GameState` 快照，后端新增黄巾专用事务，只锁防守玩家、驻防、黄巾事件和波次，复用 PVP 战斗结算核心。
- 已新增开发文档 `docs/系统设计/黄巾起义系统设计.md`，明确虚拟攻击方、起义等级、波次、表结构、接口、GM 能力、战报展示、触发冷却、幂等和测试标准；后续开发应先做只读风险和 GM 手动触发，再开启自动触发。
