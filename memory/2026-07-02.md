# 2026-07-02 记忆

## 本次发布前记录

- 用户确认将服务器 MySQL 数据目录迁移到新硬盘后，要求提交发布一个新版本。
- 当前数据库已经通过绑定挂载让 `/var/lib/mysql` 落到新硬盘 `/root/data/disk/mysql`，binlog 也随 MySQL 数据目录走新硬盘。
- 本次代码改动聚焦删除存档清理：`DeletePlayer` 改为事务内显式清理存档关联表，降低历史库缺少级联外键时删除失败或残留数据的风险。
- 补充集成测试，覆盖战报、战报状态、分享链接、信函、小游戏记录、PVP 状态、金币流水和物品流水等关联数据清理。
- 发布前仍需保持 `main-core` 为主发布分支，提交并推送到远程触发后续发布流程。

## 本次提交前记录

- 用户要求补上天机轮转老虎机 GM 调整入口；后台新增“配置 -> 天机轮转配置”，支持调整每线押注、线数、免费旋转、图案权重、倍率、Scatter 免费次数和 Bonus 宝匣倍率。
- 后端新增 GM 天机轮转配置读取与保存接口，配置仍落到 `go/config/slot.json`；同步更新 README、admin README 和 OpenAPI。
- 用户提供并要求按《增援系统修复开发方案》补完增援未完善项。
- 增援修复完成：新增 `POST /api/v1/reinforcements/{reinforcementId}/accelerate`，派出方可对行军中援军加速，每批最多 2 次，每次消耗 10 城金并写 `reinforcement_accelerate` 流水。
- 增援发送时服务端忽略客户端 `speedMultiplier`，避免免费加速；路上召回/遣返按已走时间返程，驻扎后按实际去程时间返程。
- 前端左侧行军提示和增援页补齐增援加速、召回、遣返入口；驻防面板按“自己 / 增援 · 玩家名”展示，并聚合显示兵力和携带武将。
- 已验证：`go test ./internal/app/game ./internal/transport/api -count=1`、`web npm run build`、`admin npm run build`、`python3 scripts/openapi_bundle.py`、`git diff --check` 均通过。
- 已知情况：`web npm run lint` 和 `admin npm run lint` 仍有既有 lint 问题，定点检查本次增援改动文件通过。

## 扫荡弹窗优化提交前记录

- 用户反馈一键扫荡后的战斗结果弹窗内容过多，会把屏幕撑大，要求弹窗简化，完整内容去战报查看。
- 前端 `BattleResultModal` 针对 `battleType = sweep` 或扫荡聚合数据改为摘要展示：成功/请求数量、失败数量、击溃总数、完成/中止状态，并提示逐城明细去战报详情。
- 扫荡结果弹窗不再展开每个 NPC 防守方战损、触发特性、俘虏和复活等长明细；普通单场战斗弹窗保持原展示。
- 弹窗容器增加最大高度和内部滚动，避免未来奖励或摘要过长时撑出屏幕。
- 已验证：`cd web && npx eslint src/pages/map/components/BattleResultModal.tsx`、`cd web && npm run build` 通过。

## 天机轮转提交前记录

- 天机轮转规则调整：用户明确“天机令不是大小王吗，可以代替一切”。后端规则改为天机令既可替代普通图案，也可补齐星陨 Scatter 和宝匣 Bonus 的 3 个触发条件；至少仍需要 1 个真实星陨/宝匣，纯 3 个天机令继续按天命令三连赔付。
- 对角线 `天机令 + 天机令 + 宝匣` 现在会触发宝匣奖励局；`bonusRewards` 增加 `positions`，前端用这些坐标参与滚轴高亮闪烁和结果弹窗高亮。
- 文档同步：玩法说明弹窗、系统设计文档和 OpenAPI schema 已改为“天机令是大小王，可补齐普通/星陨/宝匣”；`make openapi` 已重新生成打包文档。
- 验证：通过 `go test ./internal/app/game -run 'TestResolveSlotRound|TestRedeemSlot|TestRedeemAllSlot|TestSaveMiniGameRecordRejectsSlot' -count=1` 和 `cd web && npm run build`。
- 用户追问“满天星”后确认可以补齐：已新增满天星/All Pay 结算，普通图案在任意位置加天机令达到 5/6/7/8/9 个时获得额外奖励，倍率系数为 2/4/8/15/30；同一局只取收益最高的一组满天星，避免天机令被多组重复放大奖励。
- 前端结果弹窗和滚轴高亮已接入 `allPayRewards`，满天星命中格会和连线、宝匣一样闪烁约 3 秒，并在结算明细中显示数量、倍率和奖励。
- 押注口径调整：用户要求“押注单线就行，能连五条线”。后端 `lineBet` 保留字段名但语义改为单次押注，`totalBet = lineBet`，不再按 5 条线倍乘扣兵；赔付仍固定结算 5 条线、满天星、宝匣和免费旋转。前端文案已改为“单次押注 / 可中奖线 / 本局扣除”。

## 世界地图玩家城池系统开发记录

- 用户要求按 `docs/系统设计/世界地图玩家城池系统开发文档.md` 开发真正世界地图，不再继续把 PVP 玩家目标列表当地图。
- 第一版世界地图按文档收敛为 `100 x 100`、坐标 `0-99`、固定 `world_1`，每个玩家城池占一个格子，地图距离使用曼哈顿距离。
- 后端新增世界地图模型、权威坐标仓储接口、内存实现、MySQL `player_world_positions` 表、`GET /api/v1/world-map/view` 接口；坐标缺失时懒创建并保证同世界坐标唯一。
- 新玩家通过服务创建后会写入世界坐标；历史玩家打开地图时会懒补坐标，旧 PVP targets 兼容接口也改为读取权威坐标。
- PVP 攻击/掠夺和增援发起改为读取双方权威坐标，按 `distance * 300 / 最慢兵种速度` 接入行军时间，并继续走 `marchSpeedBonus` 加成。
- 前端玩家地图数据源改为 `/world-map/view`，补充格子化地图、坐标查找、自己/同盟/其他玩家筛选、点击空格显示空地信息，详情与卡片不再显示兵力。
- OpenAPI 新增世界地图视图接口和 `WorldPosition`、`WorldMapTarget`、`WorldMapViewResponse` schema，并重新生成 `openapi打包.yaml`。
- 剩余待办：迁移工具/后台坐标管理、独立拆分 WorldMap 组件、目标详情接口、前端自动化测试和更完整的线上老坐标批量迁移能力仍需继续完善。

## 世界地图继续开发记录

- 补充世界地图目标详情能力：后端新增 `GetWorldMapPlayerCityTarget` 和 `GET /api/v1/world-map/targets/player_city/{playerId}?viewerId=...`，返回查看者视角下的单个玩家城池目标详情，不暴露兵力、资源、武将或驻防。
- 补充老玩家世界坐标迁移能力：服务层新增 `MigrateWorldPositions`，dbtool 新增 `backfill-world-positions` 命令，可重复执行，输出 total/created/skipped/failed 和失败明细。
- 修正 MySQL 世界坐标手动分配逻辑，避免 `ON DUPLICATE KEY` 在坐标唯一键冲突时误更新其他玩家坐标。
- 前端继续按文档拆分世界地图模块：新增 `WorldMapFilters.tsx`、`WorldMapCoordinateSearch.tsx`、`WorldMapTargetPanel.tsx`，主地图组件改为复用这些控件。
- OpenAPI 补充目标详情接口与 `WorldMapMigrationResult` schema，并重新打包。
- 已验证：`go test ./internal/app/game ./internal/transport/api ./cmd/dbtool -count=1 -timeout=60s`、`cd web && npm run build`、`python3 scripts/openapi_bundle.py`、`git diff --check` 通过。

## 世界地图精准格子和 GM 坐标入口记录

- 用户强调草地也必须对应玩家格子大小，不能只是背景视觉；世界地图需要精准做到一格等于距离 1。
- 前端新增 `WorldMapGrid.tsx`，每个可见坐标都渲染为真实草地格，玩家城池和自己的城池占用同一套 CSS grid 单元；拖动地图也按格子步长移动，避免坐标和视觉错位。
- GM 后台新增玩家世界坐标查询和调整能力：后端新增 `GET/PUT /api/v1/admin/world-map/positions/{playerId}`，服务层通过 `AdminGetWorldPosition` 和 `AdminAssignWorldPosition` 读取/保存权威坐标。
- GM 玩家详情弹层新增“世界坐标”面板，可查看并调整玩家 X/Y；坐标范围固定 0-99，目标格已被其他玩家占用时后端拒绝。
- OpenAPI 补充 GM 坐标接口并重新打包；新增测试覆盖 GM 坐标调整和占用格拒绝。
- 已验证：`go test ./internal/app/game ./internal/transport/api ./cmd/dbtool -count=1 -timeout=60s`、`cd web && npm run build`、`cd admin && npm run build`、`python3 scripts/openapi_bundle.py`、`git diff --check` 通过。

## 世界地图迁移语义和入口命名记录

- 继续对照《世界地图玩家城池系统开发文档》推进，补实老玩家迁移原则：默认坐标优先使用旧 PVP 伪随机坐标映射后的 0-99 世界坐标，避免老玩家位置无意义变化。
- 前端地图主入口从 `PlayerTargetsTab.tsx` 重命名为 `WorldMapTab.tsx`，`MapPage` 改为加载真正的世界地图组件，减少继续把世界地图当 PVP 目标列表维护的风险。
- 后端世界地图测试补充：新玩家通过 `CreatePlayer` 后会写入权威坐标；迁移会优先保留旧伪随机坐标；地图 10000 格满时返回 `ErrWorldMapFull`。
- 已验证：`go test ./internal/app/game ./internal/transport/api ./cmd/dbtool -count=1 -timeout=60s`、`cd web && npm run build`、`cd admin && npm run build`、`python3 scripts/openapi_bundle.py`、`git diff --check` 通过。

## 世界地图单一入口和前端逻辑测试记录

- 按文档“只保留一个世界地图视图”继续收敛前端入口：`MapPage` 移除旧 NPC/玩家/据点/副本/小游戏页签，打开地图页直接进入 `WorldMapTab`。
- 抽出 `worldMapGridLogic.ts` 作为世界地图网格纯逻辑，`WorldMapGrid` 复用该逻辑，避免 UI 和测试各维护一套格子规则。
- 补充 `web/tests/worldMapGridLogic.test.ts`，覆盖中心视野草地格、边界裁剪、视野判断、阵营颜色、方向文本和行军时间格式；同时修正前端方向文本为“东南/西北”等与后端一致的横向优先格式。
- 已验证：`cd web && npm test`、`cd web && npm run build`、`cd go && go test ./internal/app/game ./internal/transport/api ./cmd/dbtool -count=1 -timeout=60s` 通过。

## 世界地图目标面板操作入口记录

- 按文档“点选玩家城池后显示可执行操作”继续优化：`WorldMapTargetPanel` 不再只是定位摘要，选中城池后直接展示坐标、国家、等级、状态、距离、预计时间和侦查/增援/攻击/掠夺按钮。
- 目标面板按钮复用 `WorldMapTab` 现有侦查、PVP 出征和增援流程，按钮可用性继续以后端返回的 `canScout/canAttack/canPlunder/canReinforce/reason` 为准。
- `worldMapGridLogic.ts` 新增 `buildWorldMapActionStates`，并补充测试覆盖自己城池禁用全部按钮、忙碌时禁用全部按钮但只标记当前动作。
- 已验证：`cd web && npm test`、`cd web && npm run build`、`cd go && go test ./internal/app/game ./internal/transport/api ./cmd/dbtool -count=1 -timeout=60s` 通过。

## 世界地图主布局收敛记录

- 继续去掉旧 PVP 玩家目标列表形态：`WorldMapTab` 移除旧的玩家卡片列表和重复操作按钮，页面操作入口收敛为地图点选后的目标侧栏。
- 世界地图主体改为桌面端“地图主区域 + 目标侧栏”布局；移动端仍按上下堆叠展示，未选择格子时侧栏显示点选提示。
- 已验证：`cd web && npm test`、`cd web && npm run build`、`cd go && go test ./internal/app/game ./internal/transport/api ./cmd/dbtool -count=1 -timeout=60s`、`git diff --check` 通过。

## 世界地图顶部信息和国家图例记录

- 继续弱化世界地图的 PVP 页感：`WorldMapTab` 移除头部 PVP 积分、排名、胜场、复仇展示，同时停止加载 `getPvpState` 和 `listPvpRankings`。
- 世界地图头部改为展示当前可见/已加载玩家城池数量、我的坐标和世界尺寸。
- 新增 `WorldMapLegend.tsx` 和 `WORLD_MAP_FACTION_LEGEND`，在工具条展示魏/蜀/吴/其它国家颜色图例，补齐文档要求的国家图例。
- 补充前端测试覆盖国家图例固定展示魏、蜀、吴、其它及颜色类。
- 已验证：`cd web && npm test`、`cd web && npm run build`、`cd go && go test ./internal/app/game ./internal/transport/api ./cmd/dbtool -count=1 -timeout=60s`、`git diff --check` 通过。

## 世界地图视野加载防抖记录

- 优化世界地图拖动体验：`WorldMapTab` 对视野变化后的地图请求增加 180ms 防抖，首次加载仍立即请求，减少拖动/缩放过程中每格都打后端请求导致的抖动。
- `WorldMapTab` 增加请求序号保护，避免旧视野响应晚返回时覆盖新视野数据。
- `worldMapGridLogic.ts` 新增 `WORLD_MAP_VIEWPORT_LOAD_DELAY_MS` 和 `worldMapViewportLoadDelay`，并补充前端测试覆盖首次加载和后续视野变化的防抖规则。
- 已验证：`cd web && npm test`、`cd web && npm run build`、`cd go && go test ./internal/app/game ./internal/transport/api ./cmd/dbtool -count=1 -timeout=60s` 通过。

## 世界地图精准草地格和行军预览记录

- 用户再次明确：草地也必须对应玩家格子大小，地图视觉上一块草地就是一个坐标格，一格必须等于距离 1。
- 已核对前端 `WorldMapGrid`：可见坐标逐格渲染为真实草地按钮，玩家城池和自己的城池使用同一套 `gridColumn/gridRow` 落位，城池尺寸为所在格的完整宽高，不跨格、不漂浮。
- `worldMapGridLogic.ts` 抽出 `WORLD_MAP_SECONDS_PER_GRID_AT_SPEED_ONE = 300` 和 `estimateWorldMarchSeconds`，统一前端行军预览：速度 1 时每格 5 分钟，兵种速度越高时间按比例缩短。
- 增援、攻击、掠夺的选兵弹窗都显示坐标、方向、距离、最慢兵种速度和预计时间，避免只有增援能看到行军预览。
- 补充前端测试覆盖“一格距离 1、速度 1 五分钟、三小时上限”规则。
- 已验证：`cd web && npm test`、`cd web && npm run build`、`cd go && go test ./internal/app/game ./internal/transport/api ./cmd/dbtool -count=1 -timeout=60s` 通过。

## 世界地图全量加载缓存记录

- 按用户“目前玩家不多，地图页面可以全部加载并缓存，拖动时本地读取”的要求继续优化世界地图。
- 后端世界地图视野半径上限从 30 放宽到 100，第一版 100x100 地图可通过 `radius=100` 一次返回全图玩家城池；屏幕实际显示范围仍由前端当前视野半径逐格裁剪。
- 前端 `WorldMapTab` 进入地图和手动刷新时使用全量半径加载玩家城池缓存，拖动/缩放地图不再触发后端视野请求，只在本地根据中心坐标和半径筛选当前可见城池。
- `worldMapGridLogic.ts` 用 `WORLD_MAP_FULL_LOAD_RADIUS = 100` 取代旧视野防抖规则，测试改为覆盖全量缓存半径。
- 开发文档和 OpenAPI 同步更新 `radius` 最大 100 及全量缓存说明，并重新生成 `openapi打包.yaml`。
- 已验证：`python3 scripts/openapi_bundle.py`、`cd web && npm test`、`cd web && npm run build`、`cd go && go test ./internal/app/game ./internal/transport/api ./cmd/dbtool -count=1 -timeout=60s` 通过。

## 世界地图格子可读性记录

- 继续优化地图粗糙问题：草地格在默认密度下直接显示小坐标，玩家不需要只靠悬停提示判断当前格子。
- 玩家城池和自己的城池改为格子内的正方形小城标识，草地仍完整对应一个坐标格，避免整格被城池色块盖住。
- 点击空地后会把选中的草地格在地图上高亮，目标面板继续显示空地坐标和“第一版暂无操作”。
- `worldMapGridLogic.ts` 新增坐标标签密度判断和同格判断，前端测试覆盖默认显示坐标、密集视野隐藏坐标、空格选中判断。
- 已验证：`cd web && npm test`、`cd web && npm run build` 通过。

## 世界地图关系和状态标识记录

- 继续补齐开发文档要求的“自己、同盟、其他玩家有关系标识”：地图城池小方块新增关系角标，自己显示“己”、同盟显示“盟”、其他玩家显示“他”。
- 保护、免战、不可操作等状态在城池小方块上新增短角标，目标面板也显示关系短标识，减少只靠颜色和面板文字辨识的成本。
- 点击玩家城池时会清理旧空地高亮，避免先点空地再点城池后地图上残留错误选中格。
- `worldMapGridLogic.ts` 新增关系角标、关系样式和状态角标函数，并补充前端测试覆盖。
- 已验证：`cd web && npm test`、`cd web && npm run build` 通过。

## 世界地图视野移动规则记录

- 继续收敛世界地图拖动、方向按钮、坐标查找和缩放规则：前端不再在页面组件里分散手写坐标边界裁剪。
- `worldMapGridLogic.ts` 新增 `clampWorldMapCenter`、`moveWorldMapCenter`、`clampWorldMapRadius`，统一把地图中心限制在 `0-99` 世界坐标内，视野半径限制在 `5-30`。
- `WorldMapTab` 的拖动/方向移动、缩放和坐标查找都改为复用同一套纯逻辑，避免中心点越界或不同入口行为不一致。
- 前端测试补充覆盖越界移动、非整数中心裁剪、缩放上下限。
- 已验证：`cd web && npm test`、`cd web && npm run build` 通过。

## 世界地图全局概览记录

- 继续改善玩家在 100x100 地图中的方位感：`WorldMapGrid` 右下角新增只读全局概览，显示当前视野框、自己的城池位置和当前选中城池/空地位置。
- 概览不会拦截拖拽或点击，主地图仍然是一格一坐标的实际操作区域。
- `worldMapGridLogic.ts` 新增 `worldMapOverviewPointStyle` 和 `worldMapOverviewViewportStyle`，用全图百分比计算概览点位和视野框。
- 前端测试补充覆盖概览点位边界、视野框在中心和左上角时的百分比计算。
- 已验证：`cd web && npm test`、`cd web && npm run build` 通过。

## 世界地图草地格等距记录

- 用户进一步澄清：草地不是大背景装饰，草地也必须和玩家城池对应同一套格子大小；每个草地格就是一个坐标格，一格严格等于距离 1。
- 前端 `WorldMapGrid` 移除外层大块草地背景表达，地图底图改为由 `visibleCells` 逐坐标生成的草地格组成；城池仍按同一套 `gridColumn/gridRow` 占用对应格。
- 地图头部新增“一格=距离1”提示，强化玩家对坐标、方向和行军距离的理解。
- 前端测试补充约束，防止后续把草地重新做成和坐标格脱节的背景层。
- 已验证：前端世界地图测试 65 条、前端类型检查、前端构建和 `git diff --check` 通过；构建仍保留既有大 chunk 提示。

## 世界地图本地缓存筛选规则记录

- 继续收敛全量缓存后的本地地图逻辑：`WorldMapTab` 不再直接在组件里手写视野筛选和格子查找。
- `worldMapGridLogic.ts` 新增 `filterWorldMapTargetsInViewport` 和 `findWorldMapTargetAtCell`，统一从全量缓存目标中按当前视野、关系筛选，并按格子查找玩家城池。
- 坐标搜索和点击草地格都复用同一套格子查找逻辑，减少搜索到城池、点击到城池行为不一致的风险。
- 前端测试补充覆盖全量缓存目标按视野裁剪、关系过滤、指定格子查找。
- 已验证：`cd web && npm test`、`cd web && npm run build` 通过。

## 世界地图 GM 占用率统计记录

- 继续补齐开发文档 Admin 能力中的“统计地图占用率”：后端新增 `WorldMapOccupancyStats`，返回总格子、已占用、可用格子和占用率。
- 仓储新增 `CountWorldPositions`，内存仓储和 MySQL 仓储均已实现；服务层新增 `AdminWorldMapOccupancy`。
- HTTP 新增 `GET /api/v1/admin/world-map/occupancy`，OpenAPI 源文件和打包文件已同步。
- GM 玩家世界坐标面板读取坐标时同步展示地图占用率，保存坐标后刷新统计，便于判断 100x100 地图容量健康。
- 后端测试补充覆盖占用率统计；已验证：`python3 scripts/openapi_bundle.py`、`cd go && go test ./internal/app/game ./internal/transport/api ./cmd/dbtool -count=1 -timeout=60s`、`cd admin && npm run build` 通过。

## 世界地图旧坐标映射一致性记录

- 继续核对老玩家坐标迁移语义：发现 MySQL 仓储曾在自身包内重复实现 FNV 坐标 fallback，没有直接复用应用层旧 PVP 坐标映射，存在内存仓储和线上 MySQL 语义漂移风险。
- `LegacyWorldCoordinateForPlayer` 导出为应用层统一旧坐标映射入口，内存仓储和 MySQL 仓储都复用同一套旧 PVP 坐标映射。
- 移除 MySQL 仓储本地重复 FNV fallback，避免 playerId trim 等细节不一致。
- 后端测试补充覆盖公开旧坐标映射与旧 PVP 坐标一致、带空格 playerId 和 trim 后结果一致。
- 已验证：`cd go && go test ./internal/app/game ./internal/transport/api ./cmd/dbtool -count=1 -timeout=60s` 通过。

## 世界地图点选目标详情刷新记录

- 继续接入开发文档中的单个玩家城池目标详情接口：前端点击城池后会调用 `getWorldMapPlayerCityTarget` 刷新该目标状态和按钮权限。
- 全量缓存仍用于地图拖动和本地裁剪，单点详情用于保证选中城池面板的保护状态、可操作原因和操作按钮尽量实时。
- `worldMapGridLogic.ts` 新增 `mergeWorldMapTargetCache`，把刷新后的单个目标合并回全量缓存；坐标搜索命中城池和点击城池都复用同一刷新流程。
- 前端测试补充覆盖单目标详情刷新替换已有缓存、缺失时插入缓存。
- 已验证：`cd web && npm test`、`cd web && npm run build` 通过。

## 世界地图 MySQL 玩家存在性记录

- 继续核对世界地图线上一致性：MySQL 仓储在分配世界坐标前补充玩家存在校验，避免旧库外键缺失时给不存在玩家写入幽灵坐标。
- `EnsureWorldPosition` 和 `AssignWorldPosition` 在进入占位、冲突检测和写入前先复用 `GetAccountIDByPlayerID`，保持和内存仓储一致的玩家不存在错误语义。
- 补充静态测试锁定 MySQL 世界坐标写入入口必须检查玩家存在。
- 已验证：`cd go && go test ./internal/infrastructure/storage -run TestMySQLWorldMapWritesCheckPlayerExists -count=1 -timeout=60s`、`cd web && npm test -- --run worldMapGridLogic` 通过；完整存储集成测试仍有 3 个既有战斗快照时间断言失败，和本次地图改动无关。

## 世界地图正方形格子画布记录

- 继续响应用户“草地也是对应玩家格子大小，一格等于距离 1”的要求：地图主画布改为正方形承载层，避免宽屏容器把草地格拉成长方形。
- `WorldMapGrid` 外层只负责背景和居中，真正承载草地格、坐标轴、城池和概览的区域使用 `aspect-square`，横向和纵向都由同一个 `gridSize` 切分。
- 前端测试补充锁定地图主画布必须保留正方形格子结构，避免后续布局改动破坏“一格视觉距离 = 一格地图距离”。
- 已验证：`cd web && npm test -- --run worldMapGridLogic`、`cd web && npm run build` 通过。

## 世界地图新玩家随机坐标记录

- 对照开发文档继续核对：老 PVP 坐标当前代码已是 `0-99`，所以老玩家迁移继续保留旧 PVP 稳定坐标；新玩家创建则不应继续用 playerId 稳定坐标作为起点。
- `CreatePlayer` 在写入新存档后会生成随机世界坐标起点，并以 `assigned_by = create` 写入权威坐标；如果随机格被占用，仓储仍按现有规则就近找空格。
- 迁移和懒加载兜底仍然不传随机起点，继续复用旧 PVP 坐标，保证老玩家位置尽量不变。
- 补充后端测试锁定新玩家创建会使用随机起点，同时保留老玩家迁移重复执行和旧坐标一致性测试。
- 已验证：`cd go && go test ./internal/app/game -run 'Test(CreatePlayerAssignsWorldPosition|CreatePlayerUsesRandomWorldCoordinateStart|MigrateWorldPositionsIsRepeatable|LegacyWorldCoordinateForPlayerUsesPvpMapping|WorldMap)' -count=1 -timeout=60s`、`cd go && go test ./internal/transport/api ./cmd/dbtool -count=1 -timeout=60s` 通过。

## 世界地图兵种速度配置校验记录

- 继续补齐开发文档“兵种缺少速度配置时应提示”的要求：正式兵种配置加载后新增 `ValidateUnitsConfig`，参与世界地图行军的兵种必须配置正数 `stats.speed`。
- 校验范围限定为有战斗/行军属性的兵种；运输或纯非战斗配置不强制，避免把未来非行军单位误判为错误。
- 后端行军计算仍保留速度缺失时按 `1` 兜底的运行时保护，但配置加载会尽早提示具体阵营和兵种，方便上线前发现配置问题。
- 补充测试覆盖缺少 `stats.speed` 的战斗兵种会报错、运输兵种无速度可通过。
- 当前终端 PATH 缺少 `/usr/local/bin`，因此用 `/usr/local/bin/go` 和 `/usr/local/bin/gofmt` 验证；已通过：`/usr/local/bin/go test ./internal/app/game -run 'Test(UnitRegistry|LoadUnitsConfigRequiresSpeedForMarchUnits|ValidateUnitsConfigAllowsTransportWithoutSpeed|CalculateWorldMarchSeconds|ReinforcementTravelUsesSlowestSelectedUnitSpeed|PvpMarchTravelUsesWorldMapDistanceAndUnitSpeed)' -count=1 -timeout=60s`、`/usr/local/bin/go test ./internal/app/game -run TestResolveSlotRound -count=1 -timeout=60s`。

## 世界地图坐标轴刻度记录

- 继续改善用户反馈的“分不清方向和坐标”：在原有东西南北边缘标签基础上，世界地图主画布新增顶部 X 轴和左侧 Y 轴坐标刻度。
- `worldMapGridLogic.ts` 新增 `buildWorldMapAxisTicks`，按当前视野、世界边界和真实格子中心生成刻度；边缘视野会保留越界裁剪后的真实位置。
- `WorldMapGrid` 在正方形地图画布内渲染刻度，刻度位置与草地格中心对齐，拖动地图时玩家可以更直接判断当前格子坐标。
- 前端测试补充覆盖中心视野刻度、边缘视野刻度和组件使用刻度函数。
- 当前终端 PATH 缺少 `npm`，因此用 Codex Node 直接执行测试和构建；已验证：`node --experimental-strip-types --test "tests/**/*.test.ts" --run worldMapGridLogic`、`node node_modules/typescript/bin/tsc -b && node node_modules/vite/bin/vite.js build` 通过。

## 世界地图空地信息记录

- 继续补齐文档“点击空格显示空地信息”：空地面板不再只显示坐标，新增相对自己城池的方位和曼哈顿距离。
- `worldMapGridLogic.ts` 新增 `distanceFrom`，和 `directionFrom` 一起作为前端地图格子距离/方位的统一计算入口。
- `WorldMapTargetPanel` 接收自己的世界坐标，点击草地空格时展示坐标、方位、距离和“第一版暂无操作”，与城池目标面板的信息结构更一致。
- 前端测试补充覆盖空地面板必须显示方位和距离。
- 已验证：`node --experimental-strip-types --test "tests/**/*.test.ts" --run worldMapGridLogic`、`node node_modules/typescript/bin/tsc -b && node node_modules/vite/bin/vite.js build` 通过。

## 世界地图选中城池标牌记录

- 继续改善地图点位识别：玩家点击城池后，地图格子上会显示一个浮动目标标牌，包含玩家昵称和距离。
- 标牌只在当前选中城池显示，不给全部城池铺文字，避免玩家变多后地图被昵称覆盖。
- 右侧目标面板仍保留完整坐标、国家、等级、状态和操作按钮；地图标牌只承担快速识别当前目标的作用。
- 前端测试补充覆盖选中城池必须在地图格子上显示昵称和距离。
- 已验证：`node --experimental-strip-types --test "tests/**/*.test.ts" --run worldMapGridLogic`、`node node_modules/typescript/bin/tsc -b && node node_modules/vite/bin/vite.js build` 通过。

## 世界地图行军角标记录

- 继续增强世界地图的操作反馈：地图页已有 PVP 行军数据，现将正在出征、返程、结算和被攻击状态映射到目标城池格子的短角标。
- `worldMapGridLogic.ts` 新增 `buildWorldMapMarchBadges`，按当前玩家视角生成目标玩家 ID 到短标识的映射：出、返、结、袭。
- `WorldMapGrid` 在玩家城池小方块左下角显示行军角标，玩家可直接在地图上看到哪些城池与当前行军有关。
- 前端测试补充覆盖当前玩家视角下的出征、返程、被攻击和已结束行军过滤。
- 已验证：`node --experimental-strip-types --test "tests/**/*.test.ts" --run worldMapGridLogic`、`node node_modules/typescript/bin/tsc -b && node node_modules/vite/bin/vite.js build` 通过。

## 世界地图行军图例记录

- 继续完善地图可读性：在国家颜色图例后补充行军角标图例，解释“出、返、袭、结”的含义。
- `WORLD_MAP_MARCH_BADGE_LEGEND` 固定维护行军角标文案，`WorldMapLegend` 同时展示国家颜色和行军状态，不需要玩家猜角标。
- 前端测试补充覆盖行军角标图例顺序、文案和组件接入。
- 已验证：`node --experimental-strip-types --test "tests/**/*.test.ts" --run worldMapGridLogic`、`node node_modules/typescript/bin/tsc -b && node node_modules/vite/bin/vite.js build` 通过。

## 世界地图草地格与城池格精确对应记录

- 根据用户澄清，“草地”不是背景纹理，而是和玩家城池同一套坐标格：一格草地等于距离 1，城池占据其中一个完整格子。
- `WorldMapGrid` 中玩家城池视觉从原来的格内 68% 小方块改为占满坐标格，移除 hover 放大效果，避免视觉上破坏“一格 = 距离 1”的判断。
- 前端测试补充锁定城池不能再缩成小图标，也不能通过 hover 缩放改变格子占用感。

## 世界地图行军三小时上限记录

- 继续修正地图距离和行军时间规则：世界地图保持真实曼哈顿距离，最终行军时间超过三小时时统一封顶，而不是把远距离提前截断成 36 格。
- `CalculateWorldMarchSeconds` 新增三小时上限，PVP 攻击/掠夺通过该通用计算自然生效。
- 增援距离计算移除 `pvpMaxViewRadius` 距离截断，速度较快的远距离增援会按真实距离计算，只有结果超过三小时才封顶。
- 前端世界地图预估时间新增同一上限，攻击、掠夺、增援弹窗与后端规则保持一致。

## 世界地图坐标冲突搜索顺序记录

- 继续补齐开发文档“冲突时就近找空格、搜索顺序稳定”的要求：坐标候选顺序抽到应用层 `WorldMapCoordinateCandidates`。
- 候选顺序固定为原格、上、右、下、左，再补同半径外圈其他合法格子；边界坐标会跳过越界格。
- 内存仓储和 MySQL 仓储都改为复用同一候选顺序，避免测试环境和线上数据库在同样冲突数据下分配不同坐标。
- 补充应用层顺序测试和 MySQL 静态一致性测试，锁定线上仓储不能保留单独的曼哈顿搜索实现。

## 世界地图 PVP 兼容入口坐标记录

- 继续核对开发文档“PVP 目标列表从权威表读取坐标”：旧 PVP 目标列表测试改为显式写入权威坐标，再断言列表、距离和视野筛选都按权威表返回。
- 修复旧 PVP 兼容接口把 `centerX=0&centerY=0` 当作未传的问题；后端统一用 `-1` 表示未指定中心，`0,0` 保持为合法地图坐标。
- 前端 `listPvpTargets` API 封装改为用 `!== undefined` 判断参数，避免查询 0 坐标时漏掉查询参数。
- 补充前端静态测试锁定 PVP 兼容目标接口必须允许查询 0 坐标中心。

## 世界地图迁移冲突统计记录

- 继续补齐开发文档“迁移工具输出冲突数量”的要求：`WorldMapMigrationResult` 新增 `conflicts` 字段。
- 老玩家迁移时先计算旧伪随机坐标；如果最终分配坐标与旧坐标不同，说明旧坐标被占用并触发就近找空格，计入冲突数量。
- `backfill-world-positions` 命令行输出新增 `conflicts`，上线回填时可以直接看到有多少玩家被挪到附近格。
- OpenAPI schema 同步新增 `conflicts` 字段，并补充迁移冲突计数测试。

## 世界地图前端验收测试补强记录

- 继续补齐开发文档前端验收要求：新增测试锁定地图页入口只渲染 `WorldMapTab`，不再回退到旧玩家目标列表或 NPC 分类页。
- 新增测试锁定世界地图目标面板不展示目标兵力、资源或驻防详情，只展示等级、状态、坐标、距离和预计时间等基础信息。
- 新增测试锁定目标面板四类操作入口：侦查、增援、攻击、掠夺分别调用现有对应流程。
- 已验证世界地图前端测试 34 条通过，前端构建通过。

## 世界地图拖拽规则测试记录

- 继续补齐开发文档“拖拽地图会改变视野”的前端验收要求：将地图拖拽像素换算为格子偏移的逻辑抽为 `worldMapDragGridDelta`。
- `WorldMapGrid` 拖拽事件改为复用该纯计算函数，确保测试覆盖的规则就是实际组件使用的规则。
- 拖拽换算按正方形地图画布的格子大小计算，小于一格的移动不会触发视野偏移；负向小幅移动会规范为普通 `0`，避免出现 `-0`。
- 前端测试新增真实跨格拖拽、小幅拖拽不移动和组件接入断言；已验证世界地图前端测试 35 条通过，前端构建通过。

## 世界地图坐标查找规则测试记录

- 继续补齐开发文档“输入坐标能跳转”的前端验收要求：将坐标输入解析抽为 `parseWorldMapCoordinateSearch`。
- 坐标查找明确允许 `0` 和 `99` 等边界合法坐标，空输入、负数和超过世界范围的坐标返回统一提示。
- `WorldMapTab` 改为复用统一解析规则，合法坐标会移动地图中心；命中玩家城池时选中目标，否则选中空地格。
- 前端测试新增坐标查找边界校验和组件接入断言；已验证世界地图前端测试 37 条通过，前端构建通过。

## 世界地图后端视图验收测试记录

- 继续补齐开发文档后端验收要求：新增测试验证历史玩家缺失世界坐标时，打开世界地图会为自己和范围内玩家自动写入 `lazy_create` 坐标。
- 新增 JSON 响应测试，锁定世界地图视图不返回目标兵力、资源、武将、驻防或部队字段。
- 保留世界地图目标只展示基础城池信息、等级、状态、坐标、距离和操作权限；兵力与防守详情必须继续通过侦查获取。
- 已验证 `go test ./internal/app/game -run 'Test(WorldMap|MigrateWorldPositions|CalculateWorldMarchSeconds)'` 通过。

## 世界地图后端半径查询测试记录

- 继续补齐开发文档“地图视图按半径返回目标”的后端验收要求：新增测试验证半径不足时不会返回目标，移动中心后默认半径能返回目标。
- 测试同时锁定请求中心坐标会反映到响应 `centerX/centerY`，目标 `distance` 仍以自己城池到目标城池的真实曼哈顿距离计算。
- 已验证世界地图相关后端测试通过。

## 世界地图 GM 坐标边界测试记录

- 继续补齐开发文档 Admin 能力验收：新增 GM 坐标边界测试，覆盖越界坐标不能保存、缺失玩家不能查询或分配坐标。
- 既有 GM 测试继续覆盖占用坐标拒绝、合法坐标保存、查询回读和地图占用率统计。
- 已验证 `go test ./internal/app/game -run 'Test(WorldMap|AdminWorld|MigrateWorldPositions|CalculateWorldMarchSeconds)'` 通过。

## 世界地图关系筛选控件测试记录

- 继续补齐开发文档“自己、同盟、其他玩家筛选生效”的前端验收要求：新增测试锁定 `WorldMapFilters` 固定提供自己、同盟、其他玩家三类选项。
- 测试同时锁定筛选控件分别绑定 `self`、`ally`、`other` key，并且 `WorldMapTab` 默认三类筛选全部开启。
- 已验证世界地图前端测试 38 条通过，前端构建通过。

## 世界地图格子点击交互测试记录

- 继续补齐开发文档“点击玩家城池显示详情、点击空格显示空地信息”的前端验收要求：新增测试锁定草地格点击调用 `handleGrassClick(cell.x, cell.y)` 并进入空地选择。
- 新增测试锁定玩家城池点击会阻止事件冒泡，并调用 `onFocusTarget(target.playerId)`，避免城池点击被底层草地格误处理。
- 已验证世界地图前端测试 39 条通过，前端构建通过。

## 世界地图返回本城入口记录

- 继续补齐开发文档“坐标查找控件包含返回本城按钮”的前端要求：`WorldMapCoordinateSearch` 新增“我的城”按钮。
- `WorldMapTab` 将现有 `focusSelf` 传入坐标搜索控件，玩家可以在顶部筛选/搜索区域直接回到自己城池。
- 前端测试新增坐标查找控件 X/Y、查找和返回本城入口断言；已验证世界地图前端测试 40 条通过，前端构建通过。

## 世界地图 MySQL 坐标表约束测试记录

- 继续补齐开发文档“坐标必须唯一、分配依赖数据库唯一键防并发冲突”的上线可靠性要求：新增 MySQL schema 静态测试。
- 测试锁定 `player_world_positions` 必须保留玩家主键、`world_id/x/y` 唯一键、世界坐标查询索引、世界玩家索引和玩家外键级联删除。
- 已验证 `go test ./internal/infrastructure/storage -run 'TestMySQLWorldMap'` 通过。

## 世界地图国家主体颜色记录

- 继续改善世界地图可读性：玩家城池主体色改为按国家区分，不再只是浅色城池加边框。
- 新增 `factionCityClass`，魏为蓝、蜀为绿、吴为红、其它为灰，和国家图例保持一致。
- `WorldMapGrid` 城池方格改为直接使用国家主体色，玩家缩放/拖动时更容易看清不同国家分布。
- 已验证世界地图前端测试 40 条通过，前端构建通过。

## 世界地图格子精准对应测试记录

- 根据用户再次澄清，草地也必须对应玩家城池格子大小，不能只是背景；一块草地就是一个世界坐标格，一格等于距离 1。
- 前端测试新增“草地格和城池格使用同一个坐标网格”断言，锁定草地按钮、自己城池和玩家城池都通过同一套 `gridColumn/gridRow` 坐标换算落格。
- 新增 OpenAPI 静态一致性测试，锁定世界地图玩家接口、GM 坐标接口、迁移冲突字段和目标详情不暴露兵力/资源/武将/驻防的文档约束。
- 已验证世界地图前端测试 41 条通过，`go test ./internal/transport/api -run TestOpenAPIWorldMapStaticConsistency` 通过。

## 世界地图距离辅助环记录

- 继续改善世界地图粗糙和方向距离判断困难的问题：在正方形地图主画布中新增中心横纵轴和 5 格倍数距离辅助环。
- 新增 `buildWorldMapDistanceGuides`，按当前视野半径生成 5、10、15 等距离环，位置和尺寸都按真实格子百分比计算，不改变一格一坐标规则。
- `WorldMapGrid` 在草地格之上、城池之下渲染距离辅助线，叠层禁用指针事件，不会抢草地或城池点击。
- 前端测试补充覆盖距离环计算、组件接入和中心轴展示；已验证世界地图前端测试 43 条通过，前端类型检查和构建通过。

## 世界地图自己筛选修复记录

- 对照开发文档“自己默认始终显示，除非用户显式关闭”发现前端存在筛选绕过：关闭“自己”筛选后，`WorldMapGrid` 仍会通过我的城兜底层把自己城池画出来。
- `WorldMapTab` 现在把 `relationFilters.self !== false` 显式传给 `WorldMapGrid`，地图兜底显示自己的城池时也遵守自己筛选状态。
- 前端测试补充锁定自己城池兜底显示受 `showSelf` 控制，避免后续关系筛选再次失效。
- 已验证世界地图前端测试 44 条通过，前端类型检查和构建通过。

## 世界地图选中目标筛选一致性记录

- 继续核对关系筛选：发现选中某个城池后再关闭对应关系筛选时，地图上的城池点会隐藏，但右侧目标详情仍可能保留这个已隐藏目标。
- 新增 `isWorldMapRelationVisible` 作为统一关系可见性判断，地图视野筛选和右侧选中目标都复用同一规则。
- `WorldMapTab` 的 `focusedTarget` 现在会在目标关系被筛掉时返回空，不再展示隐藏关系的城池详情。
- 已验证世界地图前端测试 44 条通过，前端类型检查和构建通过。

## 世界地图隐藏城池点击一致性记录

- 继续修复筛选后的交互边角：关闭某类关系后，隐藏城池所在格视觉上是草地，但点击或坐标搜索仍会从全量缓存命中该隐藏城池，导致空地面板不出现。
- 新增 `findVisibleWorldMapTargetAtCell`，点击草地格和坐标搜索都只命中当前筛选可见的城池。
- 如果目标城池被当前关系筛选隐藏，该格按空地处理，视觉、点击和右侧面板保持一致。
- 已验证世界地图前端测试 44 条通过，前端类型检查和构建通过。

## 世界地图缩放控件边界记录

- 继续改善世界地图操作可读性：缩放按钮原先使用 Plus/Minus，但提示写成“放大视野/缩小视野”，容易和实际半径变化混淆。
- `WorldMapGrid` 现在按视野半径上下限禁用缩放按钮：最小半径时不能继续放大格子，最大半径时不能继续缩小格子。
- 缩放按钮提示改为“放大格子”和“缩小格子”，更贴近地图格子视觉变化。
- 前端测试补充锁定缩放边界禁用和提示文案；已验证世界地图前端测试 45 条通过，前端类型检查和构建通过。

## 世界地图接口鉴权文档记录

- 对照开发文档“玩家只能以自己的 playerId 查询视野、后端必须校验登录用户拥有该玩家”：后端 `WorldMapView` 和 `WorldMapPlayerCityTarget` 已调用 `requireOwnership` 校验玩家归属。
- OpenAPI 补充 `bearerAuth` JWT 安全方案，并在两个玩家端世界地图接口上声明 `security: bearerAuth`，避免联调时误认为世界地图玩家接口公开可查。
- 静态测试补充锁定世界地图处理器必须保留 `requireOwnership`，OpenAPI 源文件和打包文件必须保留 `bearerAuth`。
- 已验证 `python3 scripts/openapi_bundle.py` 和 `go test ./internal/transport/api -run TestOpenAPIWorldMapStaticConsistency` 通过。

## 世界地图鉴权错误响应文档记录

- 继续补齐世界地图玩家接口安全契约：两个玩家端世界地图接口新增 `401` 和 `403` 响应说明，对应未登录/凭证无效以及当前账号不拥有玩家存档。
- `GET /api/v1/world-map/view` 的 `403` 描述为当前账号不拥有该玩家存档。
- `GET /api/v1/world-map/targets/player_city/{playerId}` 的 `403` 描述为当前账号不拥有查看者玩家存档。
- 已重新生成 OpenAPI 打包文件，并验证 `go test ./internal/transport/api -run TestOpenAPIWorldMapStaticConsistency` 通过。

## 世界地图真实路由鉴权测试记录

- 继续补强开发文档“玩家只能以自己的 playerId 查询视野、后端必须校验登录用户拥有该玩家”的验收证据。
- 新增真实 HTTP 路由测试 `TestWorldMapHandlersRequireOwnedViewer`，使用 JWT 中间件、内存仓储和真实 `NewRouter` 发起世界地图请求。
- 测试覆盖未登录访问世界地图视野返回 `401`、他人 token 查询你的地图返回 `403`、本人 token 查询返回 `200`。
- 同时覆盖目标详情接口：他人 token 使用你的 `viewerId` 返回 `403`，本人 token 返回 `200`。
- 已验证 `go test ./internal/transport/api -run 'Test(WorldMapHandlersRequireOwnedViewer|OpenAPIWorldMapStaticConsistency)'` 通过。

## 世界地图概览玩家格记录

- 根据用户澄清“草地也是对应玩家格子大小，要精准对应，一格等于距离 1”，继续补齐前端地图可读性。
- 主地图已保持草地格、自己城池、玩家城池共用同一套 `gridColumn/gridRow` 坐标换算；本次新增概览图玩家城池点，按同一 100x100 世界坐标百分比映射。
- `WorldMapTab` 将当前关系筛选可见的全量玩家城池传给概览图，关闭“自己/同盟/其他玩家”时主地图和概览同步隐藏对应点。
- 概览图玩家点使用小方块并按魏蜀吴阵营着色，和扁平化正方形城池视觉保持一致。
- 已验证世界地图前端测试 46 条通过，前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图草地格纹理记录

- 继续按用户“草地也是对应玩家格子大小，一格等于距离 1”的要求改善地图粗糙感，草地不再是整张地图统一背景。
- `worldMapGridLogic.ts` 新增 `worldMapGrassCellClass`，按格子坐标生成稳定草地纹理变体；同一坐标刷新或拖动后保持同一纹理，相邻坐标会有轻微变化。
- `WorldMapGrid` 的每个草地按钮继续占用独立 CSS grid 单元，只替换该格自身背景纹理，不改变点击、距离、坐标或城池占格规则。
- 前端测试新增草地纹理稳定性和组件接入断言；已验证世界地图前端测试 47 条通过，前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图城池关系描边记录

- 对照开发文档“主体颜色表示国家，边框或徽标表示关系，自己使用高亮描边，同盟使用友方描边”的展示规则，继续增强地图可读性。
- 新增 `worldMapRelationRingClass`，自己城池使用蓝色高亮描边，同盟城池使用绿色友方描边，其他玩家使用轻描边；主体颜色仍由国家决定。
- `WorldMapGrid` 中玩家城池和自己的城池兜底层都接入关系描边，不改变城池占格、点击、筛选或行军角标逻辑。
- 前端测试补充关系描边函数和组件接入断言；已验证世界地图前端测试 47 条通过，前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图指向格读数记录

- 继续改善玩家判断坐标、方向和距离的体验：`WorldMapGrid` 顶部新增当前指向格读数。
- 鼠标指向草地格、玩家城池或自己的城池时，会显示该格坐标、相对自己城池的方位和曼哈顿距离；没有悬停时回落到当前选中空地或城池。
- 该读数只复用现有坐标和 `directionFrom`/距离计算，不改变一格一坐标、点击、筛选、行军或目标详情逻辑。
- 前端测试补充指向格读数的状态、草地/城池/自己城池接入断言；已验证世界地图前端测试 48 条通过，前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图移动端目标抽屉记录

- 对照开发文档“移动端顶部筛选和坐标搜索、中间地图全屏、底部目标信息抽屉”的前端结构要求，继续补齐移动端地图体验。
- `WorldMapTab` 在移动端将 `WorldMapTargetPanel` 包装为底部固定抽屉，限制最大高度并允许内部滚动；桌面端仍恢复为右侧静态目标栏。
- 地图页移动端新增底部留白，避免底部抽屉遮住地图后续内容；目标面板内容和侦查/增援/攻击/掠夺逻辑不变。
- 前端测试补充移动端底部抽屉结构断言；已验证世界地图前端测试 49 条通过，前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图坐标查找键盘体验记录

- 继续补齐开发文档“输入坐标能跳转、坐标超出范围时提示”的查找体验，减少玩家查找坐标时的额外操作。
- `WorldMapCoordinateSearch` 现在接收 `worldSize`，X/Y 输入框设置 `min=0`、`max=worldSize-1`、数字输入模式、范围提示和 aria label。
- 任一坐标输入框按回车会直接触发查找，按钮查找和“我的城”入口保持不变；越界校验仍由统一 `parseWorldMapCoordinateSearch` 处理。
- 前端测试补充世界大小传入、输入边界、回车查找和范围提示断言；已验证世界地图前端测试 49 条通过，前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图增援行军验收测试记录

- 对照开发文档“增援发起时读取双方权威坐标、按地图距离和部队最低速度计算行军时间”的后端验收要求，补强服务级测试证据。
- 新增 `TestSendReinforcementUsesWorldMapDistanceAndUnitSpeed`：显式分配派出方 `(10,10)`、目标方 `(13,14)` 权威世界坐标，设置兵种速度 5，断言增援发起得到 420 秒去程和 420 秒返程。
- 测试中将派出方驿站等级归零，隔离 `marchSpeedBonus`，只验证地图距离 7 与兵种速度 5 的基础公式；其它测试仍覆盖行军加成接入。
- 已验证：`go test ./internal/app/game -run 'Test(WorldMap|MigrateWorldPositions|LegacyWorldCoordinateForPlayer|CalculateWorldMarchSeconds|SendReinforcementUsesWorldMapDistanceAndUnitSpeed|ReinforcementTravel|PvpMarchDurationUsesSlowestUnitSpeed)' -count=1 -timeout=60s` 通过，`gofmt` 和 `git diff --check` 通过。

## 世界地图草地坐标格口径记录

- 用户明确修正地图视觉口径：草地也必须对应玩家城池同样大小的坐标格，不是整张地图背景；每一块草地就是一个可点选坐标，一格等于距离 1。
- 当前 `WorldMapGrid` 已按 `visibleCells` 逐格渲染草地按钮，草地、自己城池、玩家城池共用同一套 CSS grid 坐标换算，城池占满对应格子。
- 前端测试继续补强“草地格和城池格使用同一个坐标网格”：锁定草地逐格生成、坐标标题、点击坐标、城池同格定位，并禁止退回到背景尺寸铺法。
- 已验证世界地图前端测试 49 条通过。

## 世界地图概览跳转记录

- 继续改善世界地图粗糙体验：右下角概览图从纯展示改为可点击的小地图。
- 新增 `worldMapOverviewCoordinateFromPoint`，把概览图点击位置换算成真实世界坐标，并裁剪到世界范围内。
- `WorldMapGrid` 的概览图点击后触发 `onJump`；`WorldMapTab` 复用现有选格逻辑，有玩家城池则选中城池，无玩家则显示空地信息。
- 前端测试补充概览点击坐标换算、组件接入和页面跳转逻辑断言；已验证世界地图前端测试 49 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图目标操作顺序记录

- 对照开发文档“侦查、攻击、掠夺、增援”的玩家城池操作顺序，调整目标信息面板按钮顺序，避免原先“侦查、增援、攻击、掠夺”与文档不一致。
- `buildWorldMapActionStates` 现在按文档顺序返回操作状态；`WorldMapTargetPanel` 改为按 `scout/attack/plunder/reinforce` key 读取按钮状态，不再依赖数组下标。
- 概览图补充 `onPointerDown` 阻止冒泡和 `aria-label`，避免点击小地图时误触发主地图拖拽，并改善可访问性。
- 已验证世界地图前端测试 49 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图行军预览弹窗记录

- 对照开发文档“出征和增援选择兵力后，需要显示距离、最慢速度、预计行军时间”，优化世界地图出征/增援弹窗。
- 原先行军预览是一整段描述文本，移动端可读性较弱；现在改为结构化短信息块，展示坐标、方位、距离、最慢速度、预计时间。
- 新增 `buildTravelPreviewItems` 统一构建攻击、掠夺、增援的地图行军预览；预计时间仍使用前端同公式预估，提交后以后端实际返回为准。
- 前端测试补充结构化行军预览断言，锁定不再退回长文本描述；已验证世界地图前端测试 50 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图关系筛选数量记录

- 继续改善玩家判断地图上“哪里有人”的体验：关系筛选项旁新增已缓存城池数量。
- 新增 `buildWorldMapRelationCounts`，从全量目标缓存统计自己、同盟、其他玩家数量；筛选关闭时数量仍保留显示，避免玩家误以为地图没有人。
- `WorldMapFilters` 接收 `counts` 并在自己、同盟、其他玩家旁显示数量；`WorldMapTab` 使用 `useMemo` 从全量目标缓存计算数量。
- 前端测试补充关系计数、筛选组件数量显示和页面传值断言；已验证世界地图前端测试 50 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图最近城池快捷跳转记录

- 继续改善玩家“看不到其他玩家、需要自己在格子里找人”的体验：地图筛选区下方新增“最近城池”快捷条。
- 新增 `buildNearestWorldMapTargets`，从当前关系筛选可见的全量目标中排除自己，按距离和坐标稳定挑选最近 5 个玩家城池。
- `WorldMapTab` 展示最近城池按钮，按钮显示玩家名、距离和坐标，点击后复用概览跳转逻辑：有城池则选中目标，无城池则显示空地。
- 前端测试补充最近城池排序、页面展示和点击跳转断言；已验证世界地图前端测试 51 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图最近城池方位记录

- 继续增强“最近城池”快捷条的信息密度，避免玩家只看到距离和坐标还要自己判断方向。
- 最近城池按钮现在展示关系短标识、玩家名、方位、距离和坐标；按钮 `aria-label` 也包含方位和距离。
- 方位优先使用后端目标方向字段，缺失时用当前自己坐标即时计算，不改变筛选、跳转或目标详情刷新逻辑。
- 前端测试补充关系标识、方位文本和可访问描述断言；已验证世界地图前端测试 51 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图行军状态摘要记录

- 继续改善地图行军可读性：城池格子已有出征/返程/被袭/结算角标，本次在地图顶部新增行军状态摘要。
- 新增 `buildWorldMapMarchSummary`，按当前玩家视角统计出征、返程、被袭、结算中数量；只统计活跃行军状态，已完成 `resolved` 不再显示。
- `WorldMapTab` 在存在活跃行军时展示“行军 出/返/袭/结”数量，帮助玩家不用逐格找角标也能知道当前地图动静。
- 前端测试补充行军摘要统计、`resolving` 与 `resolved` 区分、页面展示断言；已验证世界地图前端测试 52 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图同步时间记录

- 继续改善地图状态可信度：世界地图接口返回 `serverTime`，页面现在在顶部显示“已同步 HH:mm:ss”。
- 新增 `formatWorldMapSyncTime`，把服务端时间格式化为 24 小时时分秒，非法或缺失时间不显示。
- `WorldMapTab` 在加载世界地图视图后保存 `targetResult.serverTime`，刷新地图后同步时间会一起更新。
- 前端测试补充同步时间格式化和页面接入断言；已验证世界地图前端测试 53 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图可见坐标范围记录

- 继续改善玩家判断地图方向和坐标段的体验：主地图标题现在显示当前画面覆盖的 X/Y 坐标范围。
- `WorldMapGrid` 根据当前中心、半径和世界大小计算 `boundedMinX/boundedMaxX/boundedMinY/boundedMaxY`，在边缘视野会自动裁剪到合法坐标。
- 顶部信息从只有“中心、范围”扩展为同时显示 `X a-b · Y c-d`，拖动地图时能直接看出当前屏幕所在坐标段。
- 前端测试补充可见坐标范围计算和展示断言；已验证世界地图前端测试 53 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图国家图例数量记录

- 继续改善地图势力分布可读性：国家颜色图例现在展示魏、蜀、吴、其它各自缓存城池数量。
- 新增 `buildWorldMapFactionCounts`，从全量目标缓存统计国家数量，未知国家归入“其它”。
- `WorldMapLegend` 接收 `counts` 并在国家图例旁显示数量；`WorldMapTab` 使用 `useMemo` 计算并传入图例。
- 前端测试补充国家计数、图例数量显示和页面传值断言；已验证世界地图前端测试 53 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图格子占用记录

- 继续补齐地图规模感：顶部信息现在展示世界格子占用量，例如 `占用 12/10000 格`。
- 占用量使用当前已缓存玩家城池数量和 `worldSize * worldSize` 计算，贴合开发文档“100x100 共 10000 个格子”的容量口径。
- 该信息不改变地图筛选、拖拽、查找或目标缓存逻辑，仅帮助玩家和运营理解当前世界密度。
- 前端测试补充世界格子占用量展示断言；已验证世界地图前端测试 54 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图扩容阈值提示记录

- 对照开发文档“当有效玩家接近 8000 时，需要考虑扩容地图或开启新世界”，继续补齐地图容量提示。
- `WorldMapTab` 根据 `targets.length / (worldSize * worldSize)` 计算占用率，达到 80% 时在顶部显示“接近扩容线”。
- 该提示仅作为运营/玩家可读信息，不改变世界地图容量、坐标分配、筛选或查询逻辑。
- 前端测试补充占用率计算和 80% 扩容线提示断言；已验证世界地图前端测试 54 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图加载失败态记录

- 继续补齐世界地图作为主入口的稳定体验：首次加载世界地图失败时，不再停在空白或无提示状态。
- `WorldMapTab` 新增 `loadError`，非静默加载失败时记录错误并提示 toast；如果没有可用地图视图，会展示“世界地图加载失败”、错误原因和“重新加载”按钮。
- 静默刷新失败不会清空当前已加载地图，避免玩家操作中的地图突然消失。
- 前端测试补充加载失败、错误提示和重试入口断言；已验证世界地图前端测试 55 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图无存档空状态记录

- 继续补齐世界地图入口边界体验：如果当前没有选中的玩家存档，地图页不再一直停留在 loading。
- `WorldMapTab.load` 在没有 `activePlayerId` 时会结束非静默 loading 并返回；页面展示“请选择玩家存档”空状态。
- 该状态提示选择存档后可查看自己的世界地图坐标和附近玩家城池，不改变正常存档下的地图加载流程。
- 前端测试补充无玩家存档时结束 loading 和空状态展示断言；已验证世界地图前端测试 56 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图占用自城兜底记录

- 修正顶部世界格子占用口径：如果后端目标缓存里未包含自己的城池，前端占用量会额外把自己城池计入，避免低估世界占用。
- `WorldMapTab` 新增 `worldOccupiedCells`，优先检查 targets 中是否有 `relation=self` 或坐标等于自己的目标；没有则使用 `targets.length + 1`。
- 扩容阈值判断也改为基于 `worldOccupiedCells / worldCellCapacity`，保持和显示占用量一致。
- 前端测试补充自城兜底、占用率计算和展示断言；已验证世界地图前端测试 56 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图轻量刷新记录

- 改善地图刷新体验：已有地图内容后再次点击刷新，不再整页进入 loading 并清空地图。
- `WorldMapTab` 新增 `refreshing` 状态和 `hasLoadedMapRef`；首次加载仍显示 loading，已有地图后刷新只让刷新按钮进入“刷新中”并旋转图标。
- 刷新完成后恢复按钮状态；失败时沿用已有错误处理，但不会因为刷新而隐藏当前地图。
- 前端测试补充已有内容时刷新不清空地图、按钮禁用和旋转状态断言；已验证世界地图前端测试 57 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图键盘移动记录

- 继续改善地图操控体验：主地图现在可聚焦，并支持方向键按真实格子移动视野。
- `WorldMapGrid` 新增 `handleMapKeyDown`，方向键左/右/上/下分别调用 `onPan(-1,0)`、`onPan(1,0)`、`onPan(0,-1)`、`onPan(0,1)`。
- 主地图容器新增 `tabIndex=0` 和 `aria-label`，说明可用方向键按格移动；不改变原有拖拽、按钮平移或坐标点击逻辑。
- 前端测试补充键盘平移、可聚焦和可访问描述断言；已验证世界地图前端测试 58 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图键盘缩放记录

- 继续完善无鼠标地图操控：主地图聚焦后支持 `+`/`=` 放大格子，`-`/`_` 缩小格子。
- 键盘缩放复用现有 `onZoom(-5)` 与 `onZoom(5)`，并遵守 `canZoomIn`/`canZoomOut` 边界，避免越过最小/最大视野半径。
- 主地图 `aria-label` 更新为同时说明方向键移动和加减号缩放。
- 前端测试补充键盘缩放和可访问说明断言；已验证世界地图前端测试 58 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图切换存档清理记录

- 修正切换玩家存档时的地图隐患：世界地图属于玩家私有坐标视图，切换存档时不能继续展示上一位玩家的地图缓存。
- `WorldMapTab` 新增 `loadedPlayerRef`，加载时检测当前 `activePlayerId` 是否和已加载玩家一致；切换玩家时清空 targets、targetView、marches、mapServerTime，并重新进入 loading。
- 同一玩家的手动刷新仍保持轻量刷新，不清空当前地图。
- 前端测试补充切换玩家时清空旧地图和重新加载断言；已验证世界地图前端测试 59 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图键盘回城记录

- 继续完善地图键盘操作闭环：主地图聚焦后支持 `Home` 键返回自己的城池。
- `WorldMapGrid.handleMapKeyDown` 在按下 `Home` 时调用 `onFocusSelf()`，行为与“我的城”按钮一致。
- 主地图 `aria-label` 更新为说明 `Home 返回我的城`；不改变方向键移动、加减号缩放、拖拽或鼠标点击逻辑。
- 前端测试补充 Home 回城和可访问描述断言；已验证世界地图前端测试 59 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图精准格子记录

- 明确世界地图显示口径：草地格和玩家城池格必须精准对应同一套坐标网格，一格就是距离 1。
- `WorldMapGrid` 当前按坐标生成正方形草地格，玩家城池作为该坐标格的完整占位显示，不再把草地当成装饰背景或与城池比例错位。
- 主地图聚焦后支持 `Enter` 或空格选择当前中心格，选中逻辑复用原有 `onSelectCell`，城池格进入目标选择，空草地进入空地信息。
- 前端测试已覆盖“一格按距离 1 计算”“草地格和城池格使用同一个坐标网格”“城池视觉占满对应坐标格”；已验证世界地图前端测试 59 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图视野预设记录

- 继续改善世界地图粗糙和找人困难的问题：地图工具栏新增近景、中景、远景三个视野预设。
- `WORLD_MAP_VIEW_PRESETS` 统一维护预设半径：近景 5 格用于精确点选，中景 10 格对应默认操作视野，远景 30 格用于快速观察周边玩家。
- `WorldMapGrid` 渲染预设切换按钮并标记当前视野，`WorldMapTab` 新增 `setViewportRadius`，切换预设时保持当前中心坐标不变，只调整显示半径。
- 该功能不改变后端全量缓存、权威坐标、草地/城池同格规则或行军距离公式；前端测试新增视野预设断言，已验证世界地图前端测试 60 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图目标预计时间记录

- 继续补齐开发文档“右侧目标信息显示距离和预计行军时间”的要求：目标面板现在明确展示“速度1预计”基础行军时间。
- 空地面板也展示同口径基础预计时间，玩家点选草地格时可以直接理解距离与时间换算；正式出征/增援弹窗仍按已选兵种最慢速度重新预估。
- 前端 `estimateWorldMarchSeconds` 修正 0 距离边界，和后端 `CalculateWorldMarchSeconds` 保持一致：距离小于等于 0 时返回 0 秒，不再误按 1 格计算。
- 该改动不暴露兵力、资源、武将或驻防；已验证世界地图前端测试 60 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图按钮不可用原因记录

- 继续补齐开发文档“按钮不可用时显示原因”的要求：世界地图目标面板不再只把原因放在按钮 `title` 上。
- `WorldMapTargetPanel` 根据后端权限字段生成的 `action.reason` 汇总不可用原因，并在操作按钮下方以可见标签展示；重复原因会去重，避免自己城池等场景重复刷屏。
- 该改动不改变后端权限判断、不新增前端硬判，也不暴露兵力、资源、武将或驻防。
- 已验证世界地图前端测试 60 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图隐藏目标查找记录

- 修正坐标查找与关系筛选的交互：如果输入坐标命中了已缓存玩家城池，但该城池被当前关系筛选隐藏，不再把该格误当空地选中。
- `WorldMapTab.handleCoordinateSearch` 会先按当前筛选查找可见目标，再用全量缓存判断是否存在隐藏目标；命中隐藏目标时移动视野到该坐标并提示“该坐标有玩家城池，但已被当前筛选隐藏”。
- 地图点击和概览跳转仍按当前筛选显示规则处理，不改变“关闭关系后隐藏对应城池”的基础行为。
- 已验证世界地图前端测试 60 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图选格入口统一记录

- 继续修正隐藏城池被误判为空地的问题：坐标查找、点击草地格、概览跳转现在统一走 `selectMapPosition`。
- `selectMapPosition` 先按当前筛选查找可见城池，再用全量缓存判断是否存在被筛选隐藏的城池；只有两者都没有时才选中空地。
- 三种入口分别给出对应提示：坐标查找提示“该坐标有玩家城池”，地图格提示“该格有玩家城池”，概览跳转提示“目标坐标有玩家城池”。
- 已验证世界地图前端测试 60 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图迁移冲突明细记录

- 继续补强老玩家世界坐标迁移工具：迁移结果新增 `conflictDetails`，记录冲突玩家从旧坐标改分配到新坐标的明细。
- `MigrateWorldPositions` 在旧坐标被占用并就近挪格时，会追加 `playerId: (oldX,oldY) -> (newX,newY)`，便于线上回填后排查位置变化。
- `backfill-world-positions` 命令会逐条输出“冲突：...”明细；OpenAPI schema 和打包文档同步新增 `conflictDetails`。
- 已验证：`go test ./internal/app/game -run 'Test(WorldMap|MigrateWorldPositions|LegacyWorldCoordinateForPlayer|CalculateWorldMarchSeconds)'`、`go test ./internal/transport/api -run 'Test(OpenAPIWorldMapStaticConsistency|WorldMapHandlersRequireOwnedViewer)'`、`go test ./cmd/dbtool`、`python3 scripts/openapi_bundle.py` 通过。

## 世界地图筛选隐藏数量记录

- 继续改善关系筛选后的地图可读性：顶部状态现在会在筛选隐藏玩家城池时显示“已隐藏 N 个”。
- `WorldMapTab` 使用全量缓存数量减去当前关系筛选后的概览目标数量得到 `hiddenByFilterCount`，避免玩家关闭“自己/同盟/其他玩家”后误以为地图没人。
- 该提示和隐藏目标查找、选格提示保持一致，不改变筛选隐藏规则、地图点击规则或后端数据。
- 已验证世界地图前端测试 60 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图当前视野隐藏提示记录

- 继续改善筛选后的空视野误导：如果当前视野内存在玩家城池，但这些城池都被关系筛选隐藏，地图画布不再提示“当前视野没有玩家城池”。
- `WorldMapTab` 新增 `viewportTargets` 和 `hiddenInViewportCount`，用当前视野内全量城池数减去筛选后可见城池数；`WorldMapGrid` 在空视野覆盖层展示“当前视野有 N 个玩家城池已被筛选隐藏”。
- 该改动不改变筛选规则、坐标点击、概览跳转或后端数据，只让玩家知道当前格子范围里是否有人被隐藏。
- 已验证世界地图前端测试 61 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图当前视野统计口径记录

- 修正顶部“当前视野 X/Y 个玩家城池”的统计口径：Y 不再使用全图缓存总数，而是使用当前视野内未按关系筛选前的玩家城池数量。
- 顶部筛选隐藏提示文案改为“全图已隐藏 N 个”，和当前视野统计分开表达，避免把全图数量误读成当前屏幕范围数量。
- 该改动只调整显示口径，不改变地图筛选、拖动、点击、缓存或后端查询。
- 已验证世界地图前端测试 61 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图曼哈顿距离辅助线记录

- 修正距离辅助线视觉口径：世界地图距离规则是曼哈顿距离 `abs(dx)+abs(dy)`，原先方形辅助框容易让角落格子被误读成同距离。
- `buildWorldMapDistanceGuides` 现在生成菱形等距线点位，`WorldMapGrid` 使用 SVG `polygon` 渲染 5 格倍数的曼哈顿距离辅助线。
- 该改动只修正距离可视化，不改变格子坐标、点击、筛选或后端行军时间公式。
- 已验证世界地图前端测试 61 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图边界提示记录

- 继续改善玩家判断地图方向和边界的体验：当当前视野贴到世界边界时，地图标题会显示北界、南界、西界、东界。
- `WorldMapGrid` 复用已有 `boundedMinX/boundedMaxX/boundedMinY/boundedMaxY` 计算 `worldEdgeLabels`，不新增后端字段或额外状态。
- 该提示只说明当前视野已经到达 0-99 世界边界，不改变拖拽、缩放、坐标查找或点击规则。
- 已验证世界地图前端测试 61 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图边界方向按钮记录

- 继续改善地图边界操作反馈：当前视野已经贴到世界边界时，对应方向移动按钮会禁用。
- `WorldMapGrid` 根据 `boundedMinX/boundedMaxX/boundedMinY/boundedMaxY` 计算 `canPanWest/canPanEast/canPanNorth/canPanSouth`，分别控制左、右、上、下按钮状态。
- 该改动不改变键盘移动、拖拽或中心点边界裁剪，只让按钮状态和世界边界一致，减少玩家误以为按钮失效。
- 已验证世界地图前端测试 61 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图迁移失败错误记录

- 修正世界坐标迁移失败时的错误语义：`MigrateWorldPositions` 不再把所有失败都统一返回 `ErrWorldMapFull`。
- 迁移过程中会保留第一个真实失败错误；如果是满图仍返回满图，如果是存储或其它错误则返回原始错误，同时继续保留失败数量和失败明细。
- 补充测试模拟世界坐标读取存储错误，确认迁移结果返回真实错误且失败明细可读。
- 已验证：`go test ./internal/app/game -run 'Test(WorldMap|MigrateWorldPositions|LegacyWorldCoordinateForPlayer|CalculateWorldMarchSeconds)'`、`go test ./cmd/dbtool` 通过。

## 世界地图画布统计口径记录

- 修正地图画布标题中的“当前”统计口径：不再显示“可见数量/全图缓存数量”，改为“当前视野 可见数量/当前视野内总数量”。
- `WorldMapGrid` 用 `targets.length + hiddenInViewportCount` 计算当前视野总城池数，和页面顶部统计保持一致。
- `WorldMapTab` 不再向地图画布传 `totalTargets`，避免画布组件混用全图缓存数量和当前视野数量。
- 已验证世界地图前端测试 61 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图短行军时间显示记录

- 修正世界地图行军预览的短时间显示：文档明确速度 15 时一格为 20 秒，前端不应把 20 秒粗略显示成 1 分钟。
- `formatDuration` 现在对 60 秒以内的行军时间显示秒数，例如 `20秒`；分钟和小时显示保持原有口径。
- 该改动影响地图提示、目标面板和出征/增援预览中的时间展示，不改变后端行军时间计算。
- 已验证世界地图前端测试 61 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图草地格精准对应记录

- 明确用户口径：草地不是装饰背景，每个草地小方格都必须对应一个真实世界坐标格，一格等于距离 1。
- `WorldMapGrid` 继续保持草地格和城池格使用同一个 CSS grid 坐标；新增当前中心格内圈和小圆点标记，帮助玩家判断拖动后哪个格子是距离/时间计算中心。
- 该改动只增强地图可读性，不改变格子大小、城池占格、坐标点击、行军距离或后端计算。
- 已验证世界地图前端测试 61 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图边缘视野补齐记录

- 修正世界地图贴近边界时的粗糙体验：视野不再按负坐标留空，而是把窗口整体补齐到合法世界坐标范围内。
- 新增 `buildWorldMapViewportBounds` 统一计算当前显示格子边界；`WorldMapGrid`、当前视野目标筛选、坐标轴、距离辅助线和概览视野框都复用同一边界口径。
- 中心轴和曼哈顿距离辅助线会跟随真实中心格偏移，中心在 `(0,0)` 时显示窗口为 `x: 0-20`、`y: 0-20`，不会再出现左上空白格误导玩家。
- 开发文档同步补充边缘视野规则；已验证世界地图前端测试 62 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图目标距离显示统一记录

- 继续收敛世界地图前端显示口径：地图格标题、选中城池标牌、目标面板、攻击/掠夺/增援弹窗预览不再分别依赖缓存字段展示方位和距离。
- 新增 `buildWorldMapTargetMetrics`，统一从“我的坐标 + 目标坐标”计算方向、曼哈顿距离和预计行军秒数，保证显示层和一格一距离规则一致。
- 选择兵种后，出征/增援预览会按同一个坐标距离和所选部队最慢速度重新计算预计时间。
- 该改动只统一前端显示和预览口径，不改变后端行军结算、权限判断或坐标数据；已验证世界地图前端测试 62 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图目标缓存距离统一记录

- 继续收敛世界地图缓存数据口径：全图加载和单点详情刷新在把后端目标转成前端目标缓存时，会传入自己的世界坐标。
- `worldTargetToPvpTarget` 现在使用 `buildWorldMapTargetMetrics(self, position)` 重算 `distance`、`direction` 和 `reinforceSeconds`，不再直接信任后端返回字段作为前端缓存指标。
- 这样最近城池排序、快捷跳转标签、目标面板和出征/增援弹窗都会共享同一套“一格一距离”的坐标指标；后端仍负责真实行军结算和权限判断。
- 已验证世界地图前端测试 63 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图远景城池简洁显示记录

- 修正远景 30 格视野下城池格过于拥挤的问题：当格子密度高到不显示坐标标签时，城池格也自动隐藏“城/我”文字、关系角标、状态角标和行军角标。
- 远景仍保留国家主体颜色、关系描边、选中标牌和悬停标题，用于快速扫视全图玩家分布；近景和中景继续展示完整城池文字和角标。
- 该改动只影响地图视觉密度，不改变坐标、点击、筛选、目标缓存或行军计算。
- 已验证世界地图前端测试 64 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图目标面板取消选择记录

- 修正移动端目标面板选中后不易收起的问题：城池目标面板和空地面板右上角新增取消选择按钮。
- `WorldMapTargetPanel` 新增 `onClearSelection`，`WorldMapTab.clearMapSelection` 同时清除当前选中城池和空地格。
- 该改动只影响前端选中态，不改变地图坐标、筛选、目标缓存、操作权限或后端行军逻辑。
- 已验证世界地图前端测试 65 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图键盘取消选择记录

- 按用户强调的口径继续保持：草地格和城池格使用同一套真实世界坐标网格，一格等于距离 1。
- 主地图聚焦后支持按 `Escape` 清除当前选中城池或空地，和目标面板右上角取消按钮复用同一个 `WorldMapTab.clearMapSelection`。
- `WorldMapGrid` 新增 `onClearSelection`，键盘说明同步加入 `Escape 取消选择`，方便拖动、键盘移动和格子选择后快速回到未选中状态。
- 该改动只影响前端选中态，不改变地图坐标、草地/城池格子大小、筛选、目标缓存、操作权限或后端行军逻辑。
- 已验证世界地图前端测试 65 条、前端类型检查和构建通过；构建仍保留既有大 chunk 提示。

## 世界地图历史坐标迁移兼容记录

- 继续按世界地图开发文档收敛老玩家权威坐标迁移：新增历史坐标映射兜底，明显越界的旧坐标会按 `-100~100 -> 0~99` 公式映射进第一版 100x100 地图。
- 为保护线上已经落在 `0-99` 的随机坐标，完全合法的当前坐标保持原值，不做二次压缩，避免玩家位置被无故挪动。
- 开发文档补充线上兼容口径；测试覆盖当前合法坐标保留、负坐标边界、正向越界边界和混合历史坐标。
- 已验证：`go test ./internal/app/game -run 'Test(WorldMap|MigrateWorldPositions|LegacyWorldCoordinateForPlayer|MapLegacyWorldCoordinateToCurrent|CalculateWorldMarchSeconds)'` 通过。

## 世界地图后端边界视野补齐记录

- 继续对齐世界地图开发文档的贴边视野规则：后端 `GetWorldMapView` 不再简单把 `center-radius` 裁到 `center+radius`，而是和前端一样在世界边界处整体补齐合法坐标窗口。
- 新增 `worldMapViewBounds`，例如中心 `(0,0)` 半径 `10` 查询范围为 `x:0-20, y:0-20`，中心 `(99,99)` 半径 `10` 查询范围为 `x:79-99, y:79-99`。
- 这样即使后续前端不走全图缓存，或 GM/调试直接调用地图接口，后端返回范围也和一格一坐标地图显示规则一致。
- 已验证：`go test ./internal/app/game -run 'Test(WorldMap|MigrateWorldPositions|LegacyWorldCoordinateForPlayer|MapLegacyWorldCoordinateToCurrent|CalculateWorldMarchSeconds)'` 通过。

## 世界地图增援按钮权限记录

- 继续对齐文档“操作按钮可用性以后端为准”：世界地图目标不再简单对所有非自己城池返回 `canReinforce=true`。
- `buildWorldMapTargets` 现在复用真实增援槽位规则 `ensureReinforcementSourceSlot` 判断目标城池是否还能接收新的增援来源；目标来源已满时返回 `canReinforce=false`。
- 如果没有其它更具体原因，地图目标会给出 `目标增援来源已满`，避免玩家在地图面板看到增援按钮可用、提交后才失败。
- 已验证：`go test ./internal/app/game -run 'Test(WorldMap|MigrateWorldPositions|LegacyWorldCoordinateForPlayer|MapLegacyWorldCoordinateToCurrent|CalculateWorldMarchSeconds)'` 通过。

## 世界地图操作细分原因记录

- 继续优化地图目标面板可读性：一个目标可能同时因为保护期无法攻击、因为增援来源满无法增援，单一 `reason` 会让按钮禁用原因串台。
- `WorldMapTarget` 新增 `scoutReason`、`attackReason`、`plunderReason`、`reinforceReason`，同时保留旧 `reason` 作为兼容兜底。
- 前端 `buildWorldMapActionStates` 优先使用对应动作的细分原因；世界地图目标缓存转换也保留这些字段，目标面板按钮 tooltip 和原因标签更准确。
- OpenAPI 源文件和打包文件、世界地图开发文档已同步。
- 已验证：`python3 scripts/openapi_bundle.py`、`go test ./internal/app/game -run 'Test(WorldMap|MigrateWorldPositions|LegacyWorldCoordinateForPlayer|MapLegacyWorldCoordinateToCurrent|CalculateWorldMarchSeconds)'` 通过。

## 世界地图同账号侦查权限记录

- 继续对齐地图按钮和真实 PVP 接口：`ScoutPvpTarget` 会通过 `ensurePvpTargetAllowed` 禁止同账号存档互相侦查。
- 世界地图目标构建现在对同账号城池返回 `canScout=false`，并给出 `scoutReason=同账号存档不能侦查`；攻击和掠夺仍分别返回同账号禁用原因。
- 同账号增援不在本次额外禁用，继续按真实增援系统规则判断。
- 已验证：`go test ./internal/app/game -run 'Test(WorldMap|MigrateWorldPositions|LegacyWorldCoordinateForPlayer|MapLegacyWorldCoordinateToCurrent|CalculateWorldMarchSeconds)'` 通过。

## 世界地图每日攻击次数权限记录

- 继续对齐地图按钮和真实 PVP 出征接口：`StartPvpAttack` 会校验攻击方每日攻击次数，达到上限时攻击和掠夺都会失败。
- 世界地图目标构建现在读取查看者自己的 PVP 状态；当 `DailyAttackCount >= DailyAttackLimit` 时，非自己、非同账号目标返回 `canAttack=false`、`canPlunder=false`。
- 对应细分原因统一为 `今日攻击次数已用完`，侦查和增援不受该限制影响；目标状态标记为 `unavailable`。
- 已验证：`go test ./internal/app/game -run 'Test(WorldMap|MigrateWorldPositions|LegacyWorldCoordinateForPlayer|MapLegacyWorldCoordinateToCurrent|CalculateWorldMarchSeconds)'` 通过。

## 世界地图城池图标化记录

- 继续改善世界地图粗糙感：近景和中景下，玩家城池不再靠“城/我”文字辨认，改为使用 `Castle` 和 `House` 图标。
- 城池仍占满对应坐标格，远景仍隐藏图标和角标，保留国家颜色与关系描边，避免高密度视野过度拥挤。
- 该改动只调整前端视觉表达，不改变坐标、点击、筛选、距离或后端权限逻辑。
- 已验证：前端世界地图测试 65 条、前端类型检查通过。

## 世界地图目标面板动作级原因记录

- 继续改善目标面板可读性：禁用原因不再简单去重展示，改为显示“侦查：原因”“攻击：原因”“增援：原因”等动作级标签。
- 移除目标信息区单独展示的兼容 `reason` 文案，避免细分原因已经存在时仍显示一个笼统原因造成误解。
- 该改动只影响前端提示文本，不改变后端权限字段、按钮禁用状态或操作流程。
- 已验证：前端世界地图测试 65 条、前端类型检查通过。

## 世界地图角标图例补全记录

- 继续降低地图符号理解成本：图例不再只解释国家颜色和行军角标，也补充关系角标 `己/盟/他` 和状态角标 `保/免/禁`。
- `worldMapGridLogic.ts` 新增关系角标图例和状态角标图例常量，`WorldMapLegend` 统一渲染国家、关系、状态、行军四组说明。
- 该改动只影响前端说明展示，不改变筛选、格子、城池状态或后端字段。
- 已验证：前端世界地图测试 65 条、前端类型检查通过。

## 世界地图返回本城同步选中记录

- 修正返回本城体验：点击“我的城”或按地图 Home 键时，不再只移动视野，还会同步选中自己的城池。
- 如果玩家显式关闭了“自己”筛选，返回本城会清空当前选择，避免地图已回到自己坐标但右侧面板仍显示旧目标。
- 该改动只影响前端选中态，不改变世界坐标、筛选规则、目标缓存或后端接口。
- 已验证：前端世界地图测试 65 条、前端类型检查通过。

## 世界地图坐标输入同步记录

- 按用户强调的口径继续收敛：草地格和玩家城池格大小精准一致，每一格就是世界地图距离 1。
- 点击城池、点击草地格、坐标查找成功、最近城池跳转、全图概览跳转、返回本城时，坐标输入框都会同步到当前明确选中的格子坐标。
- 拖动、键盘平移、缩放不自动覆盖坐标输入框，避免玩家正在手动输入坐标时被打断。
- 该改动只影响前端坐标输入框和选中态，不改变世界坐标、距离计算、城池格子大小、目标缓存或后端接口。
- 已验证：前端世界地图测试 65 条通过。

## 世界地图行列对齐高亮记录

- 继续改善玩家判断坐标和方向的体验：地图悬停、选中城池、选中空地时，会在真实格子画布内高亮同一 X 列和同一 Y 行。
- 高亮只叠加在现有草地格按钮上，不新增资源点、装饰物或非坐标元素，继续保持每个草地格对应真实世界坐标 1。
- 该改动只影响前端定位反馈，不改变格子大小、拖动、点击、距离计算、目标筛选或后端接口。
- 已验证：前端世界地图测试 65 条通过。

## 世界地图目标关系文案记录

- 继续对齐开发文档“自己/同盟/其他玩家”的关系表达：目标面板不再只显示 `己/盟/他` 短角标，也展示完整关系名称。
- `worldMapGridLogic.ts` 新增 `worldMapRelationLabel`，统一返回 `自己/同盟/其他玩家`，供目标面板复用。
- 该改动只增强选中城池的信息可读性，不新增兵力、资源、武将、驻防或建筑详情暴露。
- 已验证：前端世界地图测试 65 条通过。

## PVP 目标兼容世界地图动作字段记录

- 继续降低旧 PVP targets 兼容接口和新世界地图接口的规则漂移：`PvpTargetSummary` 补齐 `relation/status/canScout/canPlunder` 以及动作级禁用原因字段。
- `ListPvpTargetsInArea` 仍保留旧字段和旧接口形态，但目标权限文案对齐世界地图：同账号禁侦查/攻击/掠夺、保护期禁攻击/掠夺、每日次数满只禁其他账号目标的攻击/掠夺，增援按钮读取真实增援来源槽位。
- 该改动不把旧 PVP targets 重新作为前端地图数据源，只让兼容接口继续服从权威世界坐标和世界地图动作规则。
- 已验证：`go test ./internal/app/game -run 'Test(PvpTargets|WorldMap|CalculateWorldMarchSeconds|MigrateWorldPositions|LegacyWorldCoordinateForPlayer|MapLegacyWorldCoordinateToCurrent)'` 通过。

## 世界地图初始坐标框同步记录

- 继续补齐“默认以自己城池为中心”的前端体验：首次进入世界地图或切换玩家存档后，X/Y 坐标输入框会同步显示自己的城池坐标。
- 普通手动刷新和行军自动刷新不会覆盖坐标输入框，避免玩家正在输入坐标或查看其它格子时被打断。
- 该改动只影响前端坐标框默认展示，不改变地图中心、格子大小、筛选、目标缓存、距离计算或后端接口。
- 已验证：前端世界地图测试 65 条通过。

## 世界地图坐标查找严格整数记录

- 继续强化“一格等于一个坐标”的输入规则：坐标查找不再用 `parseInt` 宽松截断输入，必须输入完整整数格子坐标。
- 空输入、小数坐标和混合文本（例如 `1.5`、`12abc`）都会按非法坐标处理，不会被误跳转到 `0` 或截断后的格子。
- 该改动只影响前端坐标查找校验，不改变地图格子、拖动、筛选、目标缓存、距离计算或后端接口。
- 已验证：前端世界地图测试 65 条通过。

## 世界地图 GM 坐标接口鉴权记录

- 继续补强世界地图 GM 能力的安全验收：新增 HTTP 测试锁定 `/api/v1/admin/world-map/occupancy` 和 `/api/v1/admin/world-map/positions/{playerId}` 必须使用 `X-Admin-Token`。
- 普通玩家 JWT 即使拥有自己的存档，也不能访问 GM 世界地图占用率、坐标查询或坐标调整接口。
- 该改动只补充后端鉴权测试，不改变已有中间件、GM 路由或业务返回结构。
- 已验证：`go test ./internal/transport/api -run 'TestWorldMap.*Require|TestOpenAPIWorldMapStaticConsistency'` 通过。

## 世界地图坐标输入数字化记录

- 继续承接严格整数坐标规则：坐标查找输入框从 `type=number` 改为文本数字输入，配合 `inputMode=numeric` 和 `pattern=[0-9]*`。
- 输入框变更时只接受空值或纯数字串，避免浏览器允许小数、科学计数法或其它非整数内容进入坐标状态。
- 该改动只影响前端坐标输入控件，不改变坐标查找校验、地图格子、拖动、筛选、距离计算或后端接口。
- 已验证：前端世界地图测试 65 条通过。

## 世界地图坐标候选覆盖记录

- 继续补强坐标分配算法验收：新增测试确认 `WorldMapCoordinateCandidates` 从中心、左上角、右下角出发都能覆盖第一版 100x100 全部 10000 个格子。
- 测试同时锁定候选坐标全部在合法范围内且没有重复，避免地图接近满容量时漏掉可用空格或重复扫描。
- 该改动只补充后端测试，不改变坐标分配顺序、迁移逻辑、地图接口或前端展示。
- 已验证：`go test ./internal/app/game -run 'Test(WorldMapCoordinateCandidates|WorldMapPositions|MigrateWorldPositions|WorldMapFull|CalculateWorldMarchSeconds)'` 通过。

## 世界地图迁移冲突最近格记录

- 继续补强老玩家坐标迁移验收：迁移冲突测试不再只确认“离开被占用格”，而是锁定必须使用候选顺序中的最近可用格。
- 冲突明细也会被测试校验为 `旧坐标 -> 最近新坐标`，方便线上回填后排查具体玩家位置变化。
- 该改动只补充后端测试，不改变坐标分配算法、迁移工具、地图接口或前端展示。
- 已验证：`go test ./internal/app/game -run 'Test(MigrateWorldPositions|WorldMapCoordinateCandidates|WorldMapPositions|WorldMapFull|CalculateWorldMarchSeconds)'` 通过。

## 世界地图单目标详情隐私记录

- 继续补强地图接口隐私验收：`GetWorldMapPlayerCityTarget` 单个玩家城池详情也加入 JSON 泄露字段测试。
- 详情接口和视野接口一样，不能返回 `totalArmy/resources/generals/garrison/troops` 等兵力、资源、武将或驻防相关字段。
- 该改动只补充后端测试，不改变目标详情接口结构、权限判断、前端缓存或操作流程。
- 已验证：`go test ./internal/app/game -run 'Test(WorldMapViewJSONDoesNotExposeHiddenTargetDetails|GetWorldMapPlayerCityTarget|WorldMap|MigrateWorldPositions|CalculateWorldMarchSeconds)'` 通过。

## 世界地图自己城池权限记录

- 继续补强操作按钮以后端为准的验收：世界地图视野测试现在直接断言自己的城池目标为 `relation=self/status=self`，距离为 0、方向为原地。
- 自己城池的侦查、攻击、掠夺、增援四个动作都必须由后端返回禁用，并分别给出 `自己的城池` 原因。
- 该改动只补充后端测试，不改变世界地图接口结构、权限逻辑或前端按钮流程。
- 已验证：`go test ./internal/app/game -run 'TestWorldMapViewEnsuresAuthoritativePositionsAndHidesArmy|TestWorldMap|TestGetWorldMapPlayerCityTarget'` 通过。

## 世界地图自己城池详情权限记录

- 继续补强点击城池后的单目标详情验收：`GetWorldMapPlayerCityTarget(viewerID, viewerID)` 也必须返回自己的城池关系、状态、距离和方向。
- 单目标详情接口里自己的城池同样禁用侦查、攻击、掠夺、增援，并分别返回 `自己的城池` 原因，避免视野列表正确但点击详情漂移。
- 该改动只补充后端测试，不改变目标详情接口结构、权限逻辑或前端刷新流程。
- 已验证：`go test ./internal/app/game -run 'TestGetWorldMapPlayerCityTarget|TestWorldMapViewEnsuresAuthoritativePositionsAndHidesArmy|TestWorldMap'` 通过。

## 世界地图切换存档状态清理记录

- 继续打磨世界地图切换存档体验：`activePlayerId` 变化时，除了视野和选中格，也会清空坐标输入框、侦查报告、出征/增援弹窗、选兵数量、携带武将和忙碌目标。
- 新存档加载成功后仍会把坐标框同步为自己的城池坐标；普通刷新和自动刷新不会覆盖玩家正在输入或查看的坐标。
- 该改动只影响前端状态清理，不改变世界坐标、地图缓存、筛选、行军距离或后端接口。
- 已验证：前端世界地图测试 65 条通过。

## 世界地图无存档缓存清理记录

- 继续完善世界地图存档切换边界：当 `activePlayerId` 为空时，不再只停止 loading，而是同步清空旧地图目标、视图、行军列表、同步时间、错误信息和刷新态。
- 同时重置 `hasLoadedMapRef` 与 `loadedPlayerRef`，避免玩家取消存档后再选回同一个存档时被误判为已有缓存刷新。
- 该改动只影响前端无存档状态清理，不改变世界坐标、地图格子、距离计算、筛选、目标权限或后端接口。
- 已验证：前端世界地图测试 65 条、前端类型检查、前端构建和 `git diff --check` 通过；构建仍保留既有大 chunk 提示。

## 世界地图组件语义收敛记录

- 继续落实“世界地图是基础能力，不是 PVP 目标列表”的架构方向：`WorldMapTab` 内部注释和错误文案不再称为 PVP 地图、PVP 出征或 PVP 行军。
- 前端测试新增约束，确保世界地图主组件继续使用世界地图语义；旧 PVP targets 兼容接口测试仍保留。
- 该改动只收敛前端语义和维护边界，不改变世界坐标、地图交互、行军计算、操作权限或后端接口。
- 已验证：前端世界地图测试 66 条、前端类型检查、前端构建和 `git diff --check` 通过；构建仍保留既有大 chunk 提示。

## 世界地图宽高响应口径记录

- 继续对齐世界地图开发文档中的 `width/height` 响应结构：前端世界地图缓存视图保留后端返回的 `worldWidth/worldHeight`，不再只把 `width` 压成单个 `worldSize` 用于全部展示。
- 第一版格子计算仍按 100x100 正方形 `worldSize` 处理；顶部世界尺寸和占用容量改为使用 `worldWidth * worldHeight`。
- 前端测试补充锁定宽高字段、缓存赋值和占用率计算口径，避免后续丢失接口中的高度信息。
- 已验证：前端世界地图测试 66 条、前端类型检查、前端构建和 `git diff --check` 通过；构建仍保留既有大 chunk 提示。

## 世界地图操作后刷新记录

- 继续落实“操作按钮可用性以后端为准”：玩家从世界地图成功发起攻击/掠夺或增援后，前端会静默刷新世界地图视图。
- 这样目标缓存中的保护状态、每日攻击次数限制、增援来源槽位、行军摘要和按钮禁用原因能尽快和后端同步，避免玩家看到旧按钮状态。
- 该改动只影响操作成功后的地图缓存刷新，不改变世界坐标、行军时间、战斗/增援结算或后端接口。
- 已验证：前端世界地图测试 67 条、前端类型检查、前端构建和 `git diff --check` 通过；构建仍保留既有大 chunk 提示。

## 世界地图增援行军角标记录

- 继续补齐地图上的行军状态：世界地图加载时不再只读取 PVP 行军，也会读取派出增援和收到增援。
- 前端把增援的 `marching/returning` 状态转换成地图角标口径，派出显示 `出/返`，收到的路上增援显示 `袭`，并纳入顶部行军摘要。
- 发起增援成功后会先把新增援记录合并进本地地图行军状态，再静默刷新世界地图，避免地图上短时间没有增援提示。
- 该改动只影响地图行军提示展示，不改变增援结算、行军时间、召回/遣返、加速或后端接口。
- 已验证：前端世界地图测试 68 条、前端类型检查、前端构建和 `git diff --check` 通过；构建仍保留既有大 chunk 提示。

## 世界地图增援到点刷新记录

- 继续补齐增援行军和地图自动刷新链路：增援记录转换为地图行军状态时，会把 `arriveAt` 映射为 `arrivesAt`，把 `expectedReturnedAt` 映射为 `returnsAt`。
- 这样地图现有到点刷新逻辑可以同时识别 PVP 行军和增援行军，避免增援到达或返程完成后地图角标长期停留。
- 该改动只影响前端地图自动刷新判断，不改变增援结算、行军时间、角标文案或后端接口。
- 已验证：前端世界地图测试 68 条、前端类型检查、前端构建和 `git diff --check` 通过；构建仍保留既有大 chunk 提示。

## 世界地图免战状态记录

- 继续对齐开发文档中的目标状态：世界地图后端在目标处于手动免战保护时返回 `status=truce`，其它保护类型继续返回 `protected`。
- 前端目标转换把 `truce` 也视作保护态，地图图例和目标面板继续显示“免/免战中”。
- 该改动只影响世界地图目标状态表达，不改变免战规则、攻击校验、保护原因或 PVP/增援结算。
- 已验证：`go test ./internal/app/game -run 'Test(WorldMap|GetWorldMapPlayerCityTarget|CalculateWorldMarchSeconds)'`、前端世界地图测试 68 条、前端类型检查、前端构建和 `git diff --check` 通过；构建仍保留既有大 chunk 提示。

## 世界地图行军公式复用记录

- 继续对齐开发文档“PVP 和增援都应复用世界地图行军时间函数”：增援按地图距离计算行军时间时，内部改为调用 `CalculateWorldMarchSeconds`。
- 增援原有距离小于 1 时按 1 格处理的行为保留；速度、行军加成和三小时上限统一由世界地图公共函数计算。
- 补充后端测试锁定增援距离行军函数必须复用世界地图公共公式，降低后续 PVP 和增援公式再次漂移的风险。
- 已验证：`go test ./internal/app/game ./internal/transport/api -run 'Test(Reinforcement|SendReinforcement|CalculateWorldMarchSeconds|PvpMarchDurationUsesSlowestUnitSpeed|WorldMap|GetWorldMapPlayerCityTarget|WorldMapHandlersRequireOwnedViewer)'` 和 `git diff --check` 通过。

## PVP 兼容目标免战状态记录

- 继续让旧 PVP targets 兼容接口服从世界地图状态口径：手动免战保护也返回 `status=truce`，不再和普通保护一起显示为 `protected`。
- 兼容接口仍保留旧字段和旧路径，但目标状态、按钮禁用和原因文本与世界地图接口保持一致。
- 补充后端测试覆盖 PVP targets 中手动免战目标的 `truce` 状态和攻击/掠夺禁用原因。
- 已验证：`go test ./internal/app/game ./internal/transport/api -run 'Test(PvpTargets|WorldMap|GetWorldMapPlayerCityTarget|WorldMapHandlersRequireOwnedViewer|OpenAPIWorldMapStaticConsistency)'` 和 `git diff --check` 通过。

## 世界地图禁用原因优先级记录

- 继续细化世界地图按钮原因：同账号目标如果同时处于免战保护，地图仍展示 `truce` 状态，但攻击/掠夺原因保留 `同账号存档不能攻击/掠夺`，不被免战原因覆盖。
- 世界地图接口和旧 PVP targets 兼容接口都按相同优先级处理：已有更基础禁用原因时，保护状态只补状态，不覆盖原因。
- 补充后端测试覆盖同账号 + 手动免战的状态和原因组合。
- 已验证：`go test ./internal/app/game ./internal/transport/api -run 'Test(PvpTargets|WorldMap|GetWorldMapPlayerCityTarget|WorldMapHandlersRequireOwnedViewer|OpenAPIWorldMapStaticConsistency)'` 和 `git diff --check` 通过。

## 世界地图城池格内收束记录

- 用户再次澄清：草地格必须和玩家城池格精准对应，一格等于距离 1，不能出现视觉上跨格的城池表达。
- `WorldMapGrid` 保持每个草地按钮对应一个真实坐标格，城池主体占满同一个 CSS grid 单元，同时把关系、状态、行军角标和选中标牌收进城池格内部。
- 前端测试补充约束，防止角标或选中标牌重新使用负偏移跨出坐标格。
- 已验证：前端世界地图测试 68 条、前端类型检查和 `git diff --check` 通过。

## 世界地图 OpenAPI 路径和契约记录

- 对照开发文档第 11 节发现旧规划仍写 `paths/world-map.yaml` 和 `schemas/world-map.yaml`，但当前项目地图接口统一维护在 `paths/map.yaml` 和 `schemas/map.yaml`。
- 已把世界地图开发文档中的 OpenAPI 文件路径改为当前真实结构，并注明世界地图接口与地图模块共用 map 文件。
- OpenAPI 静态测试补充锁定 `WorldMapTarget` 的 `truce` 状态枚举、四类操作可用字段和四类禁用原因字段，避免接口文档和前后端操作按钮契约漂移。
- 已验证：`go test ./internal/transport/api -run TestOpenAPIWorldMapStaticConsistency -count=1`、旧 world-map OpenAPI 路径搜索和 `git diff --check` 通过。

## 世界地图 README 维护记录

- 根 README 补充世界地图第一版规则：100x100 权威坐标网格、玩家城池一格一坐标、PVP/增援按曼哈顿距离和最低兵种速度计算，速度 1 为一格 5 分钟且三小时封顶。
- 根 README 补充 `hero3-dbtool backfill-world-positions --allow-non-test` 正式库老玩家世界坐标回填入口和输出口径。
- GM 后台 README 补充玩家世界坐标查看、地图占用率查看和世界坐标手动调整能力。
- 已验证：README/GM README 关键词检查和 `git diff --check` 通过。

## 世界地图前端注释规范记录

- 继续按项目开发规范检查新增世界地图文件：已确认新增文件顶部均有中文说明。
- 补充 `WorldMapCoordinateSearch`、`WorldMapFilters`、`WorldMapTargetPanel` 和 `viewSelf` 等组件/辅助函数的中文注释，降低后续维护成本。
- 该改动只补充注释，不改变世界地图交互、坐标、行军时间或接口行为。
- 已验证：前端世界地图测试 68 条、前端类型检查和 `git diff --check` 通过。

## 世界地图 HTTP 参数和隐私字段测试记录

- 继续补强真实运行入口测试：新增 handler 层测试，直接请求 `/api/v1/world-map/view?centerX=0&centerY=0&radius=100`，确保 0 坐标不会被当成未传参数。
- 同一测试验证世界地图全量半径视图能返回玩家城池，并验证单目标详情响应不包含 `totalArmy`、`resources`、`generals`、`garrison` 等隐藏字段。
- 该改动只补充 HTTP 层回归测试，不改变世界地图接口行为。
- 已验证：`go test ./internal/transport/api -run 'TestWorldMapHandlers' -count=1` 通过。

## 世界地图详情请求编码记录

- 前端 `getWorldMapPlayerCityTarget` 不再裸拼路径和查询参数：目标玩家 ID 进入路径前使用 `encodeURIComponent`，查看者 ID 由 `URLSearchParams` 生成查询串。
- 补充前端静态测试锁定世界地图目标详情接口的路径/查询编码方式，避免特殊 ID 或测试数据导致请求路径错位。
- 已验证：前端世界地图测试 69 条、前端类型检查和 `git diff --check` 通过。

## 世界地图 MySQL 仓储一致性记录

- 继续补强线上 MySQL 世界坐标仓储约束：静态测试锁定 `EnsureWorldPosition` 必须复用应用层 `WorldMapCoordinateCandidates` 候选顺序。
- 静态测试同时要求候选坐标耗尽后返回 `ErrWorldMapFull`，并要求 MySQL 优先坐标映射复用 `LegacyWorldCoordinateForPlayer`，避免线上迁移和内存测试坐标分配策略漂移。
- 补充 MySQL 世界地图仓储辅助函数中文注释，保持新增文件注释规范。
- 已验证：`go test ./internal/infrastructure/storage -run TestMySQLWorldMap -count=1` 通过。

## GM 世界坐标请求编码记录

- GM 后台 `getWorldPosition` 和 `updateWorldPosition` 不再裸拼玩家 ID 路径，改为使用 `encodeURIComponent(playerId)`。
- 前端世界地图测试补充静态约束，锁定 GM 世界坐标查询和调整接口必须编码玩家路径参数。
- 已验证：前端世界地图测试 70 条、玩家端类型检查、GM 后台类型检查和 `git diff --check` 通过。

## 世界地图行军文案收敛记录

- 继续打磨世界地图玩家体验：城池格 hover 不再写单一“增援约”，改为通用“行军约”，避免攻击/掠夺目标也显示增援语义。
- 目标面板和空地面板的基准时间文案从“速度1预计”改为“速度1 预计行军”，更明确这是速度 1 兵种的一格五分钟基准预估。
- 出征/增援选兵弹窗的预览字段从“预计”改为“预计行军”，更贴合开发文档要求的“预计行军时间”。
- 主组件内部行军合并变量从 `pvpMarches` 收敛为 `playerCityMarches`，减少继续把世界地图主视图当旧 PVP 目标列表维护的风险。
- 已验证：前端世界地图测试 70 条、前端类型检查、前端构建和 `git diff --check` 通过；构建仍保留既有大 chunk 提示。

## 世界地图全量加载边界记录

- 继续补强“玩家不多时全量加载并本地拖动”的前提：后端测试新增 `radius=100` 边界断言，中心在 `(0,0)`、`(50,50)` 或 `(99,99)` 时视野范围都必须覆盖完整 `0-99` 世界。
- 前端测试补充锁定世界地图首次加载使用 `WORLD_MAP_FULL_LOAD_RADIUS` 调用 `getWorldMapView`，避免拖动时重新退回按当前小视野请求。
- 已验证：后端世界地图全量半径相关测试、世界地图后端综合定向测试、前端世界地图测试 70 条和 `git diff --check` 通过。

## 世界地图旧区域注释收敛记录

- 继续清理旧 PVP 地图口径残留：`pvpRegionID` 注释不再写误导性的 `200x200`，改为说明这是旧 PVP 兼容字段按 20 格区块生成区域编号。
- 补充测试锁定旧 PVP 兼容坐标仍落在当前 `100x100` 世界内，`(0,0)` 区域为 1，`(99,99)` 和越界兼容坐标会归入东南第 25 区。
- 已验证：相关后端定向测试、世界地图后端综合定向测试、`200x200` 关键词搜索和 `git diff --check` 通过。

## 世界地图移动端布局记录

- 继续优化世界地图窄屏体验：移动端底部目标面板最高 `48vh`，页面底部留白从固定 `pb-44` 改为 `pb-[52vh]`，避免底部抽屉遮住最后一屏内容。
- 顶部筛选、图例和坐标查找区域在窄屏下改为整行换行展示，桌面端再恢复右侧对齐，减少横向挤压。
- 地图控制按钮允许换行，主画布移动端最小高度从 320px 调整为 260px，`sm` 以上仍保持 320px，提升小屏可用空间。
- 前端测试补充锁定移动端底部留白、控件换行和移动端画布高度约束。
- 已验证：前端世界地图测试 70 条、前端类型检查、前端构建和 `git diff --check` 通过；构建仍保留既有大 chunk 提示。

## 世界地图单入口测试记录

- 继续落实“地图页只有世界地图一个主视图”：前端测试补充检查 `web/src/pages/map/index.ts` 只导出 `MapPage`，`MapPage` 只渲染 `WorldMapTab`。
- 测试同时锁定旧 `PlayerTargetsTab`、`NpcCityTab`、`DungeonTab`、`MiniGamesTab` 和页签状态不会重新接回地图入口。
- 该改动只补充入口边界测试，不删除旧玩法组件文件，避免误伤后续仍可能迁移到新入口的 NPC、副本或小游戏代码。
- 已验证：前端世界地图测试 70 条、前端类型检查和 `git diff --check` 通过。

## 世界地图回填命令入口记录

- 发现 README 已要求正式库使用 `hero3-dbtool backfill-world-positions --allow-non-test`，但 `backfill-world-positions` 命令本身没有定义 `--allow-non-test`，传参会失败。
- 已补齐世界坐标回填命令的 `--dsn` 和 `--allow-non-test` 参数，复用写入型 dbtool 的非测试库保护逻辑，并把超时改为长命令超时。
- Makefile 新增 `make backfill-world-positions` 测试库入口，README 同步说明测试库走 Makefile、正式库直接调用 `hero3-dbtool ... --allow-non-test`。
- 新增 dbtool 静态测试锁定命令参数、长超时、输出数据库名和 Makefile 入口。
- 已验证：`go test ./cmd/dbtool -run TestBackfillWorldPositionsCommandOptions -count=1`、世界地图/dbtool 后端综合定向测试和 `git diff --check` 通过。

## 世界地图坐标格精度记录

- 用户明确补充：草地也必须对应玩家格子大小，草地格、城池格和距离格必须精确一致，一格等于距离 1。
- 前端世界地图继续使用同一套 CSS grid 渲染草地和城池：每个草地按钮就是一个真实坐标格，城池用同一坐标转换函数覆盖自身格子，不允许退回背景纹理式地图。
- 设计文档和 README 同步强调“一块草地就是距离 1，城池只能覆盖自己的格子”。
- `healthcheck-authority` 纳入 `player_world_positions` 覆盖检查，`maintenance-status` 汇总缺世界坐标数量，避免线上玩家无世界坐标时巡检仍通过。
- 已验证：前端世界地图测试 64 条、`go test ./cmd/dbtool -run 'Test(HealthcheckAuthority|BackfillWorldPositionsCommandOptions|MaintenanceStatus)' -count=1 -timeout=90s` 通过。

## 世界坐标生命周期清理记录

- 继续对照世界地图验收标准检查“所有玩家都有权威坐标、坐标唯一”：发现创建玩家时基础存档先写入、世界坐标后写入，若地图满格或坐标分配失败，会返回创建失败但可能留下无坐标玩家。
- `CreatePlayer` 在世界坐标分配、新手邮件等后半段失败时会硬清理刚创建的玩家，避免失败创建留下脏存档。
- `DeletePlayer` 的内存仓储和 MySQL 显式清理列表都加入 `player_world_positions`，即使历史库缺少外键级联，也会释放被删除玩家占用的世界地图格。
- 数据库文档和世界地图设计文档补充删除存档必须释放世界坐标，不能只依赖外键级联。
- 已验证：服务层世界地图/删档定向测试、MySQL 删除清理/世界地图仓储定向测试、dbtool 权威检查定向测试通过。

## GM 世界坐标占用检查记录

- 对照世界地图开发文档 Admin 能力，补齐“检查坐标冲突”：新增 GM 只读接口 `GET /api/v1/admin/world-map/positions/check?x=&y=`，返回指定格是否已占用以及占用玩家 ID。
- GM 玩家世界坐标面板新增“检查”按钮，保存前也会自动检查目标格；如果目标格已被其它玩家占用，前端直接拦截，后端保存接口仍保留最终唯一性校验。
- OpenAPI 新增 `WorldMapCoordinateCheck` schema 和 GM 占格检查路径，并重新打包 `openapi打包.yaml`。
- 前端世界地图测试补充锁定 GM 坐标检查 API、`WorldPositionPanel` 保存前检查、占用格错误提示和禁用保存按钮，防止后续 UI 调整丢掉冲突检查。
- OpenAPI 安全契约补充 `adminToken`，世界地图 GM 占用率、坐标检查、坐标查询和坐标调整接口都显式声明 `X-Admin-Token`。
- 服务层补充 `AdminCheckWorldCoordinate` 单测，覆盖空闲格、占用格和越界坐标，避免该规则只被 HTTP 层间接覆盖。
- 已验证：世界地图 GM HTTP 测试、OpenAPI 静态测试、GM 后台构建、前端世界地图测试 65 条、OpenAPI 打包产物安全声明检查、GM 坐标服务层定向测试和 `git diff --check` 通过。

## 世界地图概览格子精度记录

- 用户再次明确：草地也要和玩家城池使用同一个格子大小，任何可见草地格都必须精确对应距离 1。
- 主地图保持每个草地按钮等于一个真实坐标格，玩家城池继续用同一套 `gridColumn/gridRow` 坐标转换覆盖对应草地格。
- 右下角概览图不再使用固定 5x5 背景网格，改为按 `worldSize` 动态计算草地格背景尺寸；当前 100x100 世界中，概览每条格线也对应真实世界距离 1。
- 前端测试补充锁定概览草地格尺寸来自 `worldSize`，并禁止退回 `20%` 固定背景格。
- 已验证：前端世界地图测试 65 条和 `git diff --check` 通过。

## 世界地图方向感补强记录

- 对照世界地图开发文档继续优化玩家读图体验：地图主画布新增四向罗盘，明确上北、下南、左西、右东。
- 罗盘层使用 `pointer-events-none`，只提供方向感，不参与空地或城池点击，也不改变草地格和城池格的一格一坐标布局。
- 前端测试补充锁定罗盘存在、四向文字和不拦截点击的结构约束。
- 已验证：前端世界地图测试 65 条和 `git diff --check` 通过。

## 世界地图概览读图记录

- 继续补强世界地图的全图定位感：右下角概览图新增北、南、西、东四向标识，和主画布罗盘保持同一方向口径。
- 概览按钮的标题和可访问标签补充当前中心坐标与我的城池坐标，玩家通过概览跳转时能明确自己处在全图哪个位置。
- 概览中的玩家点、自己点和选中点补充坐标提示，并保持 `aria-hidden` 与不拦截点击，避免影响概览跳转。
- 前端测试补充锁定概览方向标识、当前中心/我的城池提示和点位标题。
- 已验证：前端世界地图测试 65 条和 `git diff --check` 通过。

## 世界地图中心距离记录

- 继续补强地图拖动后的距离感：地图顶部新增“中心距我 X 格 · 方位”，玩家移动视野后能直接判断当前看的区域离本城多远。
- 中心距离使用和行军一致的曼哈顿距离，方位复用世界地图方向函数，避免显示口径与攻击/增援距离理解不一致。
- 前端测试补充锁定中心格、中心方位、中心距离和顶部展示文案。
- 已验证：前端世界地图测试 65 条和 `git diff --check` 通过。

## 世界地图曼哈顿路径记录

- 继续补强点选目标后的距离理解：当自己城池和当前指向格都在当前视野内时，地图会显示从自己到目标的横纵折线路径。
- 路径按真实格子中心绘制，表达第一版曼哈顿距离“横向格数 + 纵向格数”，帮助玩家理解攻击、掠夺和增援的距离来源。
- 路径层使用 `pointer-events-none`，不影响草地格、城池格、概览和目标点击；目标超出当前视野时不显示半截路径，避免误导。
- 前端测试补充锁定路径组件、视野内校验、横纵段百分比计算和不拦截点击。
- 已验证：前端世界地图测试 65 条和 `git diff --check` 通过。

## 世界地图状态角标颜色记录

- 继续补强地图城池可读性：保护、免战、不可操作不再全部使用同一种黄色状态角标。
- 新增 `worldMapStatusBadgeClass`，保护使用黄色、免战使用紫色、不可操作使用深灰；地图城池角标和图例共用同一套状态样式。
- 前端测试补充锁定状态角标样式函数、地图格子调用和图例调用，避免后续状态颜色再次漂移。
- 已验证：前端世界地图测试 65 条和 `git diff --check` 通过。

## 世界地图自己城池高亮记录

- 对照文档“自己城池明显高亮”继续优化：自己的城池关系描边从普通 2px 增强为更醒目的 4px 高亮环和白色描边阴影。
- 远景模式隐藏城池细节时，自己的城池不再只是一个蓝色方块，而是在格子内显示“我”，方便玩家缩小视野后仍能快速定位本城。
- 前端测试补充锁定自己城池高亮环、远景“我”标识和普通目标远景仍按原规则隐藏拥挤角标。
- 已验证：前端世界地图测试 65 条和 `git diff --check` 通过。

## 世界地图自己筛选提示记录

- 继续补齐筛选状态反馈：当玩家显式关闭“自己”筛选后，顶部状态栏会提示“我的城池已被筛选隐藏”。
- 该提示避免玩家误以为世界坐标缺失或自己城池没有加载；“返回本城”和坐标显示仍保留，方便恢复定位。
- 前端测试补充锁定自己筛选关闭时必须有顶部提示。
- 已验证：前端世界地图测试 65 条和 `git diff --check` 通过。

## 世界地图返回本城筛选联动记录

- 继续补齐“返回本城”交互：点击“我的城”或坐标查找中的返回本城时，会自动恢复“自己”筛选为开启。
- 自己城池目标不再按当前筛选查找，而是直接从全量缓存按本城坐标查找，避免此前关闭“自己”筛选后返回本城仍然看不到自己。
- 前端测试补充锁定返回本城必须恢复自己筛选，并继续同步坐标、选中本城和移动视野中心。
- 已验证：前端世界地图测试 65 条和 `git diff --check` 通过。

## 世界地图最近城池行军提示记录

- 继续补强全图找人体验：最近城池快捷条新增速度 1 基准预计行军时间。
- 最近城池按钮的标题、可访问标签和可见内容都包含该行军时间，玩家不必先点开目标面板就能判断距离成本。
- 该时间复用目标缓存里的 `reinforceSeconds` 与统一 `formatDuration`，不新增独立公式。
- 前端测试补充锁定最近城池快捷条必须展示速度 1 预计行军时间。
- 已验证：前端世界地图测试 65 条和 `git diff --check` 通过。

## 世界地图空地选中标牌记录

- 继续补强点击空格后的地图反馈：选中空地格子时，地图格内会显示该空地坐标标牌。
- 该标牌固定在格子内部底部，使用 `pointer-events-none`，不影响草地点击，也不破坏一格一坐标布局。
- 远景隐藏普通坐标标签时，选中空地仍能直接看到坐标，避免只靠右侧面板确认。
- 前端测试补充锁定选中空地坐标标牌的格内定位和坐标内容。
- 已验证：前端世界地图测试 65 条和 `git diff --check` 通过。

## 世界地图筛选恢复记录

- 继续补强关系筛选体验：当筛选导致全图有玩家城池隐藏时，顶部“全图已隐藏”旁新增“显示全部”按钮。
- 点击后一次恢复自己、同盟、其他玩家三类关系筛选，避免玩家需要逐个勾选才能找回隐藏目标。
- 前端测试补充锁定一键恢复函数、按钮入口和图标。
- 已验证：前端世界地图测试 65 条和 `git diff --check` 通过。

## 世界地图隐藏城池命中记录

- 继续对齐“点击有城池的格子，选中玩家城池”的地图规则：当坐标搜索、概览跳转或点击草地格命中被筛选隐藏的玩家城池时，会自动恢复该城池对应关系筛选并选中目标。
- 隐藏目标不再只弹提示并保持空选中；提示文案改为“已自动显示并选中”，降低筛选状态和格子语义之间的冲突。
- 前端测试补充锁定隐藏目标命中时必须恢复对应关系、调用目标选中逻辑，并同步三类入口文案。
- 已验证：前端世界地图测试 65 条和 `git diff --check` 通过。

## 世界地图城池按钮语义记录

- 继续补强世界地图交互语义：玩家城池按钮新增可访问标签，包含目标名称、方位、坐标、距离和预计行军时间。
- 地图视觉不变，但按钮不再只依赖 `title` 承载目标信息，辅助技术和自动化检查能读到完整目标摘要。
- 前端测试补充锁定城池按钮 `aria-label` 必须包含核心地图目标信息。
- 已验证：前端世界地图测试 65 条和 `git diff --check` 通过。

## 世界地图自己兜底点击记录

- 继续补齐自己城池兜底显示：当后端目标列表未返回自己城池但自己坐标仍在视野内时，兜底展示层改为可点击按钮。
- 该按钮带有“我的城池，坐标 ...”可访问标签，点击会触发返回本城逻辑，不再作为静态遮罩挡住格子操作。
- 前端测试补充锁定兜底自己城池必须是按钮、阻止拖拽误触冒泡，并调用 `onFocusSelf`。
- 已验证：前端世界地图测试 65 条和 `git diff --check` 通过。

## 世界地图目标面板状态颜色记录

- 继续统一地图状态表达：目标详情面板的状态胶囊不再全部使用统一强调色，而是按自己、保护、免战、可攻击、不可操作和普通状态区分颜色。
- 新增 `worldMapStatusPillClass`，和地图状态角标同属世界地图样式口径，避免格子状态颜色和详情面板状态颜色漂移。
- 前端测试补充锁定状态胶囊样式函数和目标面板调用。
- 已验证：前端世界地图测试 65 条和 `git diff --check` 通过。

## 世界地图坐标查找语义记录

- 继续补强坐标查找入口：X/Y 输入框按世界坐标最大值动态限制输入长度，避免输入明显超长的无效坐标。
- 查找按钮补充当前坐标标题和可访问标签，例如未填时显示 `?, ?`，已填时读出目标坐标。
- 前端测试补充锁定动态坐标长度、坐标摘要和查找按钮语义。
- 已验证：前端世界地图测试 65 条和 `git diff --check` 通过。

## 世界地图概览自己筛选记录

- 修正概览图和主图筛选不一致：关闭“自己”筛选后，右下角概览图也不再单独显示我的城池点。
- `MapOverview` 接收 `showSelf`，主地图和概览图共用同一份自己城池显示状态，避免玩家看到主图隐藏但概览仍显示的割裂。
- 前端测试补充锁定概览图必须接收并使用 `showSelf`。
- 已验证：前端世界地图测试 65 条和 `git diff --check` 通过。

## 世界地图草地格距离口径记录

- 继续明确世界地图格子口径：草地不是单纯背景纹理，必须和玩家城池使用完全相同的正方形坐标格。
- 每个草地格都对应一个真实 `(x,y)` 世界坐标，点击草地格选择该坐标；城池也占满同一个坐标格，所以一格严格等于距离 1。
- 前端测试加固锁定主地图等分网格、草地格占满一格、城池按钮和城池视觉也占满同一格，防止后续退回成背景式草地或尺寸漂移。
- 已验证：前端世界地图测试 65 条和 `git diff --check` 通过。

## 世界地图老坐标迁移优先口径记录

- 继续加固老玩家世界坐标迁移：迁移服务在计算旧 PVP 伪随机坐标后，会把该坐标显式作为 `preferred` 起点传给权威坐标分配逻辑。
- 仓储层原本已有旧坐标兜底，本次把服务层意图也写实，避免后续仓储默认策略调整时破坏“老玩家优先保留旧位置”的文档要求。
- 后端测试补充锁定迁移服务必须计算 `LegacyWorldCoordinateForPlayer` 并传入 `ensureWorldPosition(..., &expected)`。
- 已验证：`go test ./internal/app/game -run 'Test.*WorldMap|TestMigrateWorldPositions|TestCalculateWorldMarchSeconds|TestLegacyWorldCoordinate|TestMapLegacyWorldCoordinate'`、前端世界地图测试 65 条和 `git diff --check` 通过。
## 城墙系数提交前记录

- 用户要求军事建筑防御分组的城墙接入战斗配置里的阵营城墙系数，并在 GM 配置预留城墙硬度规则。
- PVP 守城已传入防守方 `city_wall` 当前生效等级和防守方阵营，核心战斗按 `base^城墙等级` 提高防御；城墙建筑配置里的 `defenseBonus` 不再额外叠加，避免 20 级再多加基础 20%。
- 玩家端城墙卡片显示阵营系数结果，例如 20 级魏 `1.03^20` 显示约 `+80.6%`，吴约 `+63.9%`，蜀约 `+48.6%`。
- `combat.wallConfig` 新增硬度预留字段：魏偏脆 `20 -> 5`，蜀偏硬 `20 -> 16-17`，吴中等 `20 -> 9-12`；当前只保存配置和 PVP 战报快照，不执行攻城武器导致的城墙掉级。
- GM 战斗配置面板新增城墙硬度和 20 级破坏后等级区间编辑；bootstrap 下发只读 combat 配置供玩家端展示。
- 已验证：`go test ./internal/core/combat -count=1`、`go test ./internal/app/game -run 'TestPvpCityWallAppliesFactionDefense|TestBalanceConfigLoadsMilitaryBuildingModifiers' -count=1`、`web npm run build`、`admin npm run build`、`python3 scripts/openapi_bundle.py`、`git diff --check` 均通过。
