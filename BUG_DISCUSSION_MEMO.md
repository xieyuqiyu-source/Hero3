<!-- 本文件是本轮战报系统 Bug 讨论的临时备忘录，开发和验收完成后删除。 -->

# 战报系统 Bug 讨论备忘录

## 使用约定

- 讨论阶段只调查和记录，不修改业务代码。
- 每个 Bug 必须记录实际现象、预期行为、代码与规则依据、判断结论和拟处理方案。
- 判断结论统一使用：`正常`、`不正常`、`需进一步确认`。
- 用户确认讨论结束后，按本备忘录逐项开发和验证。
- 全部开发与验收完成后删除本文件。

## Bug 记录

### Bug 1：本城增援他城后，右侧栏的本城军队显示

- 涉及前端：`web-wlsg`
- 用户问题：本城派兵增援其他城池后，右侧菜单栏是否还有本城军队。
- 当前代码行为：
  - 右侧栏固定保留“本城直属军队”区块。
  - 发起增援时，后端会从本城 `army` 中扣除本次派出的兵力，并在响应 `patch.army` 中返回扣除后的权威兵力。
  - `web-wlsg` 会立即使用 `patch.army` 更新右侧栏，因此这里只显示仍留在本城的军队。
  - 已派出的兵力不再计入“本城直属军队”，而是在右侧“增援中”和军事页“本城去增援他城的军队”中单独显示。
  - 如果只派出部分兵力，右侧栏继续显示本城剩余兵力。
  - 如果把所有兵力全部派出，后端会移除数量为 0 的军队项，右侧栏应显示“当前没有直属军队”。
- 代码依据：
  - `go/internal/app/game/service_reinforcement.go`：`SendReinforcement` 调用 `validateAndConsumeArmy` 扣除派出兵力，并返回 `BuildGarrisonActionResult(fromState)`。
  - `go/internal/app/game/military_combat.go`：扣除后会清理数量为 0 的军队项。
  - `web-wlsg/src/game/stateService.ts`：增援成功后使用后端 `patch.army` 回写玩家状态。
  - `web-wlsg/src/components/SidePanel.vue`：右侧“本城直属军队”直接渲染最新 `model.army`。
- 判断结论：`正常`（前提是右侧只显示扣除增援兵力后的本城剩余军队）。
- 异常判定：如果派出的增援兵力仍重复保留在“本城直属军队”数量中，或全部派出后右侧仍显示这些兵力，则为 `不正常`。
- 待用户补充：现场看到的是“剩余兵力正常显示”，还是“已派出的兵力仍显示在本城军队中”。

### Bug 2：笑萌萌右侧有军队，但攻击 NPC 弹窗无兵可选

- 涉及前端：`web-wlsg`
- 涉及账号：登录账号 `xiaomimi`，玩家 `笑萌萌`，阵营 `shu`。
- 用户现象：右侧“本城直属军队”能够显示军队，但点击攻击 NPC 后，出征弹窗没有任何兵种可选。
- 线上只读核验：
  - `/api/v1/game/state` 与 `/api/v1/military/view` 均返回了笑萌萌当前实际军队，说明玩家军队数据存在且后端读取正常。
  - `/api/v1/game/bootstrap` 正常返回蜀国 11 个兵种配置，说明线上公共兵种配置没有缺失。
  - 用户原有页面的右侧栏能够显示军队，但军事页显示“暂无兵种配置”；同一页面状态下，NPC 出征弹窗使用相同的兵种配置来源，因此没有可选兵种。
  - 新开页面或完整刷新后重新初始化，军事页会恢复显示 11 个蜀国兵种及其正确数量。
- 根因：
  - 右侧栏直接根据玩家状态中的原始 `army` 渲染，不依赖公共兵种配置，所以仍能显示军队。
  - NPC 出征弹窗、军事页和征兵页均依赖 `state.bootstrap.units` 生成兵种列表。
  - `web-wlsg/src/session/service.ts` 的退出登录流程会把 `bootstrap` 清空为 `null`，但后续重新登录流程没有重新加载 `bootstrap`。
  - 因此，同一浏览器页面执行“退出后重新登录”时，玩家军队状态已经恢复，但公共兵种配置仍为空；最终形成“右侧有兵、出征弹窗无兵”的前端状态不一致。
- 判断结论：`不正常`。
- 临时规避：完整刷新页面，让应用重新执行初始化并加载 `/api/v1/game/bootstrap`，兵种列表即可恢复。
- 拟处理方案（讨论结束后开发）：
  - 在登录进入游戏前统一保证 `bootstrap` 已加载；建议在会话服务中增加可复用的 `ensureBootstrap` 逻辑，由初始化和登录流程共同调用。
  - 保留后端返回的兵种配置作为唯一权威来源，不在弹窗中硬编码兵种，也不使用右侧栏原始军队数据绕过配置。
  - 增加“初始化 → 退出登录 → 重新登录/选择玩家”的回归测试，验证 `bootstrap` 会被重新加载，军事页和 NPC 出征弹窗均能得到可选兵种。
  - 验证右侧栏、军事页、征兵页和 NPC 出征弹窗在重新登录后的兵种与数量保持一致。
- 开发状态：`已修复，等待用户验收`。
- 本次开发结果：
  - 会话服务增加统一的 `ensureBootstrap`，初始化和登录流程都会在进入玩家流程前保证公共配置存在。
  - 新增“初始化 → 退出 → 重新登录”回归测试，确认退出时清空的兵种配置会在登录时重新加载。
  - 浏览器实际执行退出、重新登录和重新选档后，军事页仍能得到完整兵种配置，未再出现“暂无兵种配置”。

### Bug 3：笑萌萌显示“暂无驻城将领”

- 涉及前端：`web-wlsg`
- 涉及账号：登录账号 `xiaomimi`，玩家 `笑萌萌`。
- 用户问题：“暂无驻城将领”是否因为将领已经派出去增援。
- 现场情况：笑萌萌当前“本城去增援他城的军队”中存在一支带有 `刘备（Lv 100）` 的增援军队。
- 当前代码规则：
  - 将领被派去增援时，后端会为其增加 `reinforcement` 任务占用。
  - 前端只把没有任何非主将任务占用的当前主将视为本城驻城将领。
  - 因此刘备处于增援行军、驻防、战斗或返回途中时，本城显示“暂无驻城将领”。
  - 增援撤回并完成返城后，后端释放该将领的增援任务占用，刘备才会重新显示为驻城将领。
- 代码与测试依据：
  - `go/internal/app/game/service_reinforcement.go`：`reserveReinforcementGenerals` 为增援将领建立任务占用，`releaseReinforcementGenerals` 在完成返城后释放占用。
  - `web-wlsg/src/game/militaryWarAdapter.ts`：`homeGeneral` 会排除存在非主将任务占用的将领。
  - `web-wlsg/src/game/viewModel.test.ts` 与 `web-wlsg/src/game/militaryWarAdapter.test.ts`：已有测试明确验证“武将增援或出征后不再显示为本城直属/驻城将领”。
- 判断结论：`正常`。
- 异常判定：如果增援已经完成返城，但将领仍长期显示为不在城内，或增援列表没有携带任何将领却显示主将离城，则为 `不正常`。
- 本项处理：当前不需要修改代码；后续开发完成时只需回归验证将领返城后能够恢复显示。

### Bug 4：右侧菜单兵种缩略图与兵种不对应

- 涉及前端：`web-wlsg`
- 用户现象：其他页面的兵种缩略图与兵种对应正确，只有右侧菜单的兵种缩略图不正确。
- 排查结论：右侧栏确实使用了错误且重复维护的图标编号映射，属于 `不正常`。
- 开发状态：`已修复，等待用户验收`（用户明确说“验收完毕”后再删除本条 Bug 4 记录）。
- 根因：
  - 其他页面统一通过 `web-wlsg/src/game/recruitmentAdapter.ts` 的 `officialCodeByUnitType`，按后端兵种类型取得官方编号。
  - 右侧栏则通过 `web-wlsg/src/game/viewModel.ts` 的 `officialUnitIconsByName`，按中文名称维护了另一套独立编号。
  - 两套映射已经发生偏移；现有右侧栏测试又按错误映射编写，因此没有拦住问题。
- 具体差异：
  - 魏国 10 个兵种：右侧映射与其他页面一致，编号 `101` 至 `110`，当前正确。
  - 蜀国 10 个兵种：右侧全部错误地使用 `111` 至 `120`；正确编号应为 `112` 至 `121`。
    - 贪狼营 `111 → 112`
    - 麒麟卫 `112 → 113`
    - 青龙军 `113 → 114`
    - 飞鸢 `114 → 115`
    - 西凉铁骑 `115 → 116`
    - 南蛮象 `116 → 117`
    - 临冲车 `117 → 118`
    - 轰天雷 `118 → 119`
    - 木牛流马 `119 → 120`
    - 汉室宗亲 `120 → 121`
  - 吴国 10 个兵种：右侧全部错误。正确编号从 `123` 开始，而不是从 `121` 开始。
    - 影卫 `121 → 123`
    - 修罗 `123 → 124`
    - 密探 `124 → 125`
    - 神风 `125 → 126`
    - 朱雀骑 `126 → 127`
    - 霸王骑 `127 → 128`
    - 对楼车 `128 → 129`
    - 炬石车 `129 → 130`
    - 风水师 `130 → 131`
    - 太平术士 `131 → 132`
- 代码依据：
  - `web-wlsg/src/game/recruitmentAdapter.ts`：征兵、军事、NPC 出征、玩家出征、地下城和战报等页面使用的正确官方编号来源。
  - `web-wlsg/src/game/viewModel.ts`：修复前由右侧菜单单独维护错误的 `officialUnitIconsByName` 映射。
  - `web-wlsg/src/game/viewModel.test.ts`：修复前断言错误地期待蜀国 `111-120`、吴国 `121/123-131`。
- 处理方案（已实施，待验收）：
  - 删除右侧栏独立的按名称图标映射，改为根据 `unitType` 调用全站统一的 `officialCodeForUnitType`。
  - 保留未知扩展兵种的通用占位图，不猜测或伪造官方编号。
  - 更新右侧栏测试，明确验证魏、蜀、吴三阵营与统一编号源完全一致。
  - 回归检查右侧栏、征兵页、军事页、出征弹窗、地下城和战报，同一兵种必须显示同一官方缩略图。
- 本次开发结果：
  - 已删除右侧栏独立的 `officialUnitIconsByName` 错误映射。
  - 右侧栏现已直接复用 `officialCodeForUnitType`，与其他兵种展示位置共用同一官方编号来源。
  - 已将右侧栏测试中的蜀国编号修正为 `112-121`，吴国编号修正为 `123-132`。
  - 红绿回归验证：修复前新断言按预期失败；修复后定向测试 7/7 通过。
  - 全量验证：`web-wlsg` 18 个测试文件、105 个测试全部通过，生产构建成功。

### Bug 5：“本城去增援他城的军队”布局与其他军队模块不一致

- 涉及前端：`web-wlsg`
- 用户现象：军事战争页中，“本城去增援他城的军队”与“本城所属的守城军队”“他城来增援本城的军队”布局明显不同。
- 当前表现：
  - 本城守军与他城来援使用完整军队卡片：将领行、兵种缩略图、各兵种数量和总耗粮。
  - 本城出援使用单独的四列表格：目标城池、将领、兵力文字摘要、状态。
  - 出援兵种被拼成一段文字，缺少与其他模块一致的兵种图标、分列数量和耗粮结构。
- 根因：
  - `web-wlsg/src/components/MilitaryWarStage.vue` 为来援军队使用 `war-guard-table`，但为出援军队另写了 `war-reinforcement-panel` 摘要表格。
  - `web-wlsg/src/style.css` 又为 `war-reinforcement-panel` 单独定义四列摘要样式，因此视觉结构必然不同。
  - 这不是接口或后端能力限制；`outgoingRows` 已经提供 `units`、`generalNames`、`foodPerHour`、`status` 和目标城池名称，可以直接渲染完整军队卡片。
- 判断结论：`不正常`（前端展示结构不一致）。
- 拟处理方案（讨论结束后开发）：
  - 每一批出援军队都使用与来援军队相同的独立 `war-army-panel + war-guard-table` 结构。
  - 标题显示“本城去增援【目标城池】的军队”，右侧保留行军、驻防、战斗或返回状态及倒计时。
  - 内容统一显示将领、兵种缩略图、各兵种剩余数量和总耗粮。
  - 没有出援记录时仍保留一个统一空状态模块。
  - 删除只服务于旧四列表格的 `war-reinforcement-panel` 样式，并增加页面结构回归测试。
- 开发状态：`已修复，等待用户验收`。
- 本次开发结果：
  - 每一批本城出援军队现已改用与守城、来援相同的 `war-army-panel + war-guard-table` 结构。
  - 卡片固定展示目标、状态、将领、兵种缩略图、剩余数量和总耗粮，空记录保留统一空状态。
  - 已删除旧四列表格专用样式；定向测试、全量测试和双尺寸浏览器检查均通过。

### Bug 6：web-wlsg 没有显示本城被进攻的来袭队列

- 涉及前端：`web-wlsg`。
- 用户问题：本城被其他玩家攻击、掠夺或侦查时，是否没有对应的被进攻队列。
- 排查结果：当前前端确实没有展示，属于 `不正常`（来袭队列功能缺失）。
- 后端现状：
  - `/api/v1/pvp/marches?playerId=...` 会返回当前玩家作为攻击方或防守方参与的全部相关 PVP 行军。
  - 仓储的 `ListPvpMarchesForPlayer` 明确同时匹配 `AttackerPlayerID` 和 `DefenderPlayerID`，因此后端已经能提供来袭队列。
- 前端根因：
  - `web-wlsg/src/game/marchAdapter.ts` 的 `mapPvpMarches` 只保留 `item.attackerPlayerId === playerId` 的行军。
  - 当前玩家作为 `defenderPlayerId` 的行军会被直接过滤，因此右侧栏看不到“被攻击”“被掠夺”“被侦查”的到达倒计时。
  - 现有 `marchAdapter.test.ts` 还把来袭行军当成应过滤的数据，测试需要随规则一起修改。
- 安全边界：
  - 后端当前 PVP 行军结构包含 `attackTroops`；来袭提示不能直接向防守方公开敌军精确兵种与数量，否则会提前泄露攻击情报。
  - 应由后端按查看者生成来袭投影，或提供专门的安全来袭 DTO；防守方默认只看到攻击来源、行动类型、状态和到达倒计时。
  - 敌军兵力、武将和其他受情报规则约束的字段必须隐藏，除非后续明确设计了侦查或预警能力。
- 拟处理方案：
  - 为 PVP 行军增加 `incoming`/防守方视角标记，并生成不含精确敌军兵力的安全响应。
  - 前端将来袭队列映射为“被攻击”“被掠夺”“被侦查”，目标名称显示攻击方城池或玩家名称。
  - 右侧栏显示独立来袭状态和到达倒计时，不提供加速、召回操作。
  - 增加攻击方出征、攻击方返程、防守方来袭三类映射测试，确保返程军队不会继续显示为本城来袭。
- 开发状态：`已修复，等待用户验收`。
- 本次开发结果：
  - 后端为 PVP 行军增加查看方角色；本人派出的队列标记为 `sent`，本城正在遭受的来袭标记为 `incoming`。
  - 防守方的来袭响应只保留攻击来源、行动类型、状态和到达时间，敌军兵力、武将、速度、原始时长、出发/创建/更新时间及战报关联等字段均被脱敏，不能通过时间差反推敌方行军速度；进攻方和管理端仍能读取完整出征数据。
  - 前端只接受后端明确标记为 `incoming` 且仍处于 `marching` 的来袭，分别显示“被攻击”“被掠夺”“被侦查”、来源玩家和到达倒计时。
  - 来袭详情固定显示“敌军兵力：未知”，不显示加速或召回按钮，状态服务也会拦截对来袭队列的操作请求。
  - 右侧栏在线期间每 15 秒低频刷新一次行军；玩家进入页面后才产生的新来袭也能自动出现，离开页面时会清理轮询。
  - 已增加后端脱敏与进攻方完整视图测试，以及前端三类来袭、返程过滤、合并排序和不可操作测试。
  - Go 全仓测试通过；web-wlsg 18 个测试文件、112 个测试全部通过，生产构建成功。
  - 1280×800 与 1440×900 浏览器检查均通过，无横向溢出；在线后新增来袭在约 15.4 秒自动出现，干净验收页面控制台 0 错误、0 警告。
  - 独立复核确认脱敏、在线轮询、可信标记过滤和操作拦截均无阻塞或高风险问题，可以进入用户验收。

### Bug 7：战报将领特性没有输出详细数值

- 涉及前端：`web-wlsg` 战报详情。
- 用户现象：战报“将领特性”一栏只能看到特性名称，没有输出该特性的具体比例、概率、固定值或实际作用数量。
- 初步判断：`不正常`。
- 排查结果：
  - 后端参战武将快照中的 `traits` 已携带每项特性的 `scope`、`targetUnitType` 和 `params` 配置数值。
  - 后端已触发特性还会在标准战报 `detail.traits[].detail` 与兼容字段 `traitOutcomes[].detail` 中保存实际结算结果。
  - 前端 `BattleReportGeneralState` 类型遗漏了 `params` 等字段，`reportAdapter.ts` 在没有触发结果时只映射特性名称，并把 `detailText` 固定为空。
  - 当某一项特性触发后，前端还会用“已触发特性列表”整体替换将领的完整特性列表，导致同一将领未触发或常驻生效的其他特性及配置数值不再显示。
- 拟处理方案：
  - 补齐战报武将特性快照类型，保留后端已有的作用范围、目标兵种和参数数值。
  - 将将领完整特性列表与本场实际触发结果按 `traitId` 合并：配置参数作为基础，实际结算详情覆盖或追加。
  - 百分比按百分数输出，数量使用千分位，秒数标明单位；所有稳定参数键转换为中文标签。
  - 即使特性本场未触发，也显示其当前配置数值；触发时额外显示阶段和实际影响兵力/资源。
- 开发状态：`已修复，等待用户验收`。
- 本次开发结果：
  - 前端战报武将特性类型已补齐 `traitType`、`scope`、`targetUnitType` 和 `params`，不再丢弃后端快照中的配置数值。
  - 将领完整特性与本场触发结果按 `traitId` 合并；未触发特性仍显示配置比例、概率、固定值、上限和秒数，已触发特性额外显示阶段及本场实际兵力/资源变化。
  - 所有当前武将配置参数均补齐中文标签；比例按百分数、数量按千分位、时长按秒、兵种按中文名称输出。
  - 敌我同将领同特性及多支同将领援军按玩家、绝对角色、标准位置和将领联合归属，不会把一方实际结果串到另一方。
  - 敌军情报隐藏时，实际特性结果采用默认拒绝和己方安全白名单；未知 `scope`、敌方作用域及未来未知字段均不会旁路泄露敌军兵种、数量或可推导比例，武将快照中的公开配置参数仍正常显示。
  - 无标准 `detail` 的历史进攻、防守、增援战报已按 `viewType/type` 正确推断攻防位置和特性归属。
  - `web-wlsg` 目标测试 20/20、全量测试 123/123 通过，TypeScript 类型检查和生产构建成功，`git diff --check` 通过。
  - 1280 宽度浏览器复验显示“每分钟产兵 500、每日上限 3,000、虎卫 +10、攻防各 10%”，无横向溢出，控制台 0 错误、0 警告；独立复审未发现剩余 P1/P2 问题。
- 已知核心边界：后端 `traitOutcomes` 当前以 `traitId` 为唯一键；敌我双方同一特性同时触发时，核心只能保留其中一份实际结算结果。前端已确保现存结果不会串方，但若要完整保留双方实际值，需要用户授权调整 Go 战报核心契约。

### Bug 8：进攻方没有显示获得经验

- 涉及账号：登录账号 `xiaomimi`，玩家 `笑萌萌`。
- 用户现象：战报进攻方身份栏中的“获得经验”显示为 `-`。
- 现场核对：最新相关战报为 `br_6cd75219b74699a1`，标题“笑萌萌 进攻 麦城（NPC）”；该战报的进攻方将领快照为空，`generalExpGained` 与标准详情 `rewards.generalExp` 均为空。
- 将领状态：笑萌萌当前只有刘备，刘备仍存在 `reinforcement / marching` 的增援占用记录，因此没有参加该场 NPC 进攻。
- 规则判断：当前“获得经验”是将领战斗经验，不是玩家或兵种经验；只有实际随军参战的将领才会结算和显示经验。没有携带将领时固定显示 `-`。
- 结论：`正常`，不是经验结算或战报显示 Bug，无需修改。

### Bug 9：防守方完整战报看不到进攻将领获得的经验

- 涉及战斗：`机器战测·蜀 进攻 神武`，事件 `pvp_battle_8be2bdbe8e41c975190d138c`。
- 用户视角：用户是防守方神武，在自己的防守战报中可以看到进攻方刘备，但“获得经验”显示为 `-`。
- 现场核对：
  - 进攻方战报 `br_pvp_atk_14da81137337bca7` 已结算刘备获得经验 `380`，等级从 Lv1 升至 Lv3。
  - 防守方战报 `br_pvp_def_fd9ea802fb0867d1` 只保存防守方曹操获得经验 `800`；进攻方刘备快照已复制，但没有复制刘备的经验结果。
- 根因：标准战报 `rewards.generalExp` 目前只能保存“当前战报拥有者”的一份将领经验，`BattleReportGeneral / BattleReportSide` 没有各参战方独立的经验快照。前端只能把该唯一数值归给战报拥有者，其他参战方显示 `-`。
- 规则判断：防守方和增援方已约定看到完整战报；既然进攻方将领及兵力完整可见，其本场已结算经验也应完整显示。
- 结论：`不正常`，属于标准战报核心契约不完整，不是单纯前端显示问题。
- 处理结果：每个参战将领已增加独立的 `generalExpGained / generalLevelBefore / generalLevelAfter` 快照；PVP 新战斗会把攻防双方结果同步到进攻、防守及增援视角，前端按将领卡片直接展示。
- 历史兼容：读取详情时按 `eventId` 汇合同事件各视角已经保存的拥有者经验，因此上述旧战斗的防守报告可从进攻报告补回刘备 `+380`、Lv1 → Lv3，无需批量改写历史数据。
- 安全规则：先补齐同事件客观结果，再执行玩家情报投影；敌方将领被隐藏时，其经验与等级变化也一起隐藏。
- 开发状态：`已修复，等待用户验收`。

## 战报系统讨论基线

### 当前是否为统一战报框架

- 总体结论：当前是“统一骨架 + 兼容分支”，不能视为已经完全统一。
- 已统一部分：
  - 后端定义了标准 `BattleReportDetail`，统一包含来源、战斗类型、玩家视角、客观胜方、玩家结果、双方快照、奖励、特性、情报可见性和扩展信息。
  - 后端通过 `NormalizeBattleReport` 为旧战报补齐标准字段；新旧战报读取后原则上都能得到 `detail`。
  - `web-wlsg` 的战报详情统一进入 `reportAdapter.ts`，最终由同一个 `OfficialBattleReport.vue` 组件渲染。
  - NPC、PVP、防守和增援方目前共用同一种参战方卡片和兵种矩阵骨架。
- 尚未完全统一部分：
  - 前端仍保留 `legacyDetail`，当接口没有 `detail` 时会继续从旧顶层字段临时拼装详情。
  - PVP 增援方仍从顶层 `pvpReinforcements / pvpReinforcementLosses` 追加，不是完全只消费 `detail` 标准结构。
  - 详情页胜负印主要根据 `winnerSide + side.role` 推导，没有以当前玩家的 `ownerOutcome` 作为完整展示主语义。
  - 扫荡使用 `lightweight` 摘要模式，但当前详情组件没有独立的扫荡汇总模板，仍会落入通用双方战斗框架。
  - 结算信息区目前固定挂在进攻方，只完整展示掠夺资源、战损反馈、掉落、城金和城墙提示；防守、协防、侦查、黄巾、副本等专属上下文尚未形成统一的可扩展区块协议。
  - 标准设计要求新玩法只构造标准详情即可接入，但当前前端仍会读取若干 PVP/历史兼容顶层字段，因此还没有达到该目标。
- 当前架构判断：
  - 数据契约层：`基本统一`。
  - 前端入口与外壳：`已经统一`。
  - 各玩法详情语义与内容模板：`尚未完全统一`。
- 后续讨论原则：不能只因为所有战报都进入同一个 Vue 组件，就判断战报系统已经统一；需要逐类核对 NPC、PVP、协防、侦查、扫荡、黄巾和副本的身份、胜负、兵损、奖励及专属上下文。

## 战报重新设定规则

### 总原则：固定模板，缺失数据保留占位

- 战报标准化的核心不是按数据动态增删布局，而是所有同类战报使用固定结构、固定区块、固定行列和固定顺序。
- 某项数据不存在时，对应位置继续保留，并使用 `-`、`0` 或“无”等统一占位值。
- 某项数据存在时，在原固定位置完整显示真实内容，不改变其他区块的位置和尺寸。
- 不允许因为没有将领、没有特性、某兵种未参战、没有奖励或情报不可见，就直接删除该行或折叠整体结构。
- 多个实际参战实体（例如多个增援方）可以按同一固定参战方模板重复展示，但每个参战方内部结构必须一致。
- 该总原则优先于此前“无数据时隐藏模块”的草案判断。

### 规则 1：普通进攻方未携带将领时保留固定将领占位

- 适用战报：进攻方 VS 防守方；目标可以是玩家或 NPC；本条先限定为没有驻防援军参与的普通双方战斗。
- 规则：进攻方没有携带将领时，仍保留完整的将领身份栏。
- 空占位内容：将领名称 `-`、官职 `-`、军衔 `-`、获得经验 `-`。
- 固定保留内容：左侧阵营标识、右侧胜负印、将领栏尺寸以及后续兵种矩阵的位置均不改变。
- 有将领时：在相同位置显示将领名称、等级、官职、军衔和经验等真实内容，不新增另一套布局。
- 截图说明：用户提供的空将领栏样式正是固定结构缺少将领数据时的目标占位形式；该规则同时适用于玩家与 NPC 进攻方。
- 与前一版草案的冲突：此前记录为“进攻方无将领时隐藏整块身份栏”，现根据用户明确的固定模板总原则修正；旧草案不再作为开发依据。
- 当前代码行为：`不正常`。`OfficialBattleReport.vue` 当前会在进攻方无将领时删除整块身份栏，需要在后续战报重构中改为固定占位。
- 开发状态：`已修复，等待用户验收`。
- 本次开发结果：参战方身份栏与将领特性行固定渲染；无将领时名称、官职、军衔、经验和特性均使用 `-`，兵种矩阵位置不再上移。

### 规则 2：增援方与被进攻方查看同一份完整战报

- 适用战报：玩家城池或其他可驻防目标遭到攻击，且援军实际参加了本次战斗。
- 核心规则：增援方收到的不能只是“我的援军损失通知”，而应是与被进攻方相同的完整战斗报告。
- 完整战报内容：
  - 展示本次战斗的进攻方完整参战区块。
  - 展示被进攻方完整参战区块。
  - 展示所有实际参战的增援方区块，包括当前查看者自己的援军。
  - 展示同一场战斗的客观胜负、发生时间、兵种出动、阵亡、将领、特性和战斗结算上下文。
  - 各参战方仍遵循“固定模板、缺失数据保留占位”的总原则。
- 与被进攻方保持一致的含义：
  - 增援方使用同一个 `eventId` 和同一份战斗快照骨架，不再单独生成只有自己队伍的残缺详情。
  - 增援方看到的攻击方、防守方及其他援军信息，应采用与被进攻方相同的情报可见性投影；不能比被进攻方额外隐藏整支参战方。
  - 当前玩家专属字段可以不同，例如 `ownerSide=reinforcement`、`ownerOutcome`、自己的援军贡献、自己的武将经验、已读和分享状态。
  - 可以在完整战报中标识“我的援军”，但该标识不能替代或裁掉整场战斗内容。
- 当前代码行为：`不正常`。
  - `BuildBattleReportDetail` 在 `viewType=reinforcement` 时把 `primarySide` 改成援军，并直接将 `secondarySide` 设为 `nil`。
  - `buildPvpReinforcementReports` 只保存当前一批援军的兵力和损失，没有把完整攻击方、被进攻方及所有参战援军写入标准详情。
  - `report_standard_test.go` 当前还断言增援战报不应暴露 `secondarySide`，该测试与新规则冲突，开发时必须改写。
  - 前端因此无法稳定按“进攻方 → 防守方 → 所有增援方”的固定结构展示增援方视角。
- 与旧设计的冲突：旧战报文档只要求增援方看到攻击来源摘要，并允许隐藏敌方完整兵力；现根据用户明确规则修正为“与被进攻方相同的完整战报及同等级情报投影”，旧规则不再作为开发依据。
- 后续开发方向：同一战斗事件先构造一份标准完整快照，再为攻击方、被进攻方和增援方生成不同 `ownerSide / ownerOutcome / ownerContribution` 的玩家视角，不再为增援方另造残缺结构。
- 开发状态：`已修复，等待用户验收`。
- 本次开发结果：
  - PVP 与黄巾结算均以守城方完整标准战报为母版，为每个实际参战援军生成同一事件、同一攻防快照和全部援军列表的增援视角战报。
  - 增援战报只覆盖当前拥有者、`ownerSide / ownerOutcome`、自己的损失、贡献和武将经验；攻防双方及其他援军不再被裁掉。
  - 前端按进攻方、防守方、全部增援方固定顺序展示，并只把当前援军经验显示在对应援军卡片。
  - 旧增援简报若历史上没有保存完整快照，继续走单侧兼容展示，不伪造缺失的历史战斗数据。
  - 独立审查发现并修复“多支黄巾援军因完整快照被重复发放经验”的问题；经验补发现在只定位当前战报拥有的援军批次，并增加双援军回归测试。
