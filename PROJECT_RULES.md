# Hero3 Project Rules

最后更新：2026-06-28

## 1. 规则定位

本文档是 Hero3 的长期项目决策规则，和 `AGENTS.md` 相辅相成。

- `AGENTS.md`：给 AI/协作者的执行规则。
- `PROJECT_RULES.md`：给 AI/协作者的项目判断规则。
- `docs/`：主要供用户参考、思考、规划和沉淀，不作为优先级最高的开发判断依据。

后续架构、模块、目录和实现判断，优先参考本文档。

## 2. 规则生效方式

- 用户明确说“记住这个规则”“以后按这个判断”“加入规则”时，应优先补充到本文档。
- 用户明确说“记下聊天记录”“记录记忆”时，应把本次对话关键内容压缩记录到根目录 `memory/YYYY-MM-DD.md`。
- 每次提交前必须更新当天的记忆文件，避免项目上下文只存在于聊天记录里。
- 新会话开始或用户要求“读一下记忆”时，应优先阅读 `AGENTS.md`、`PROJECT_RULES.md` 和最近的 `memory/` 文件。
- 如果新需求和本文档已有规则冲突，应先指出冲突点，再建议是否调整规则。
- 规则可以随着项目理解变深而修订，但修订必须明确记录原因。
- `docs/` 中的产品和设计文档可以辅助判断，但不能覆盖本文档的长期规则。
- 文档状态制度已取消；后续不再使用 `active`、`planning`、`historical`、`deprecated` 判断文档优先级。
- 文档如果过期或阶段性完成，应直接清理、合并或在正文中说明适用范围，不再靠“文档状态”规避污染。

## 3. 当前稳定规则

### 3.1 核心与模块边界

Hero3 采用“稳定核心 + 可扩展玩法模块”的方向。

核心铁律：

- 核心在建设期可以和用户一起讨论、补全和修正基础能力。
- 核心一旦搭建完毕，就应视为极难修改的稳定地基。
- 后续新增玩法模块必须能够通过标准接口接入核心能力。
- 新玩法默认不直接修改核心，不绕过核心处理玩家长期资产。
- 如果新玩法无法接入核心，应先判断核心是否不完整，并由用户决定是否补核心，而不是让玩法模块私自绕开核心。
- 这条铁律是最高优先级的架构开发规范；如果普通文件、模块内说明或历史实现习惯与它冲突，以这条铁律为准。

核心负责玩家长期拥有的基础资产和基础能力：

- 玩家存档
- 资源
- 建筑
- 兵力
- 武将
- 道具
- 货币
- Buff/加成
- 时间结算
- 奖励发放
- 通知/信函/战报接口
- 存档读写
- 事件管线

玩法模块通过标准接口接入核心，不直接绕过核心修改玩家状态。

### 3.2 技术核心与游戏核心分开判断

Hero3 的核心分两类：

- 技术核心：路由、统一响应、错误码、鉴权、配置、日志、数据库、事务、OpenAPI。
- 游戏核心：玩家存档、资源、建筑、兵力、武将、道具、货币、Buff、时间结算、奖励、事件管线。

讨论“核心”时必须先判断是在说技术核心还是游戏核心。

### 3.3 玩法模块不进入核心

具体玩法默认不直接并入核心。判断一个内容是否属于玩法模块时，优先看它是不是“围绕核心资产运行的一套具体规则或活动”。

典型特征：

- 有自己的开启条件、参与规则或结算规则。
- 会读取或消耗玩家资产。
- 会产出奖励、战报、信函、排行或模块状态。
- 可以被单独开发、开关、替换或暂缓上线。

玩法模块可以使用核心能力，也可以有自己的模块状态，但不应该让核心理解具体玩法细节。

尚未确定一定开发的玩法名称，不应写入长期决策规则。具体玩法只在产品规划或系统设计文档确认后，才作为后续判断依据。

### 3.4 可扩展内容优先配置化或注册化

当内容未来可能由多个模块新增、产出、修改或监听时，应优先考虑配置化或注册化。

优先级较高的类型：

- 资源类型
- 兵种
- 道具
- 武将
- Buff/加成 key
- 奖励类型
- 事件类型

新增一种资源、兵种、道具或武将时，原则上不应该修改核心流程代码，而应通过配置、注册表和标准奖励接口接入。

### 3.5 模块接入核心的基本问题

每个新玩法模块设计前，先回答：

- 它读取什么玩家状态？
- 它修改什么玩家状态？
- 它使用或锁定什么玩家资产？
- 它发放什么奖励？
- 它触发什么事件？
- 它是否需要自己的模块状态？

只有这些问题回答清楚，才进入接口和代码设计。

### 3.6 核心不兼容必须提前指出

如果新需求出现“现有核心无法接入、接入后会破坏核心边界、需要模块绕过核心直接改状态”的情况，必须第一时间指出。

这种情况不能强行继续开发，应先让用户判断：

- 是当前核心设计不完整。
- 是新需求应该调整为模块化接入。
- 是需要先完善核心接口，再继续开发玩法。

用户也可能因为理解不完整而提出会破坏核心铁律的需求。遇到这种情况，协作者必须主动指出冲突、解释风险，并给出更符合核心地基的实现建议；不能为了顺从需求而让核心边界失控。

### 3.7 文档清理规则

文档状态制度已取消。后续维护文档时，优先保持少量、当前有效、能指导开发的文档。

清理原则：

- 阶段性改造方案完成后，如果内容已被数据库设计、架构设计或 README 吸收，应删除阶段文档。
- 历史进度记录不再堆在 `docs/`，重要上下文写入 `memory/`。
- 过期结论必须直接删除或改成当前事实，不用状态标签掩盖。
- 未来规划可以保留，但必须明确它是规划，不应写成当前实现事实。

当文档内容和代码、`PROJECT_RULES.md`、`memory/` 冲突时，优先级为：

1. `PROJECT_RULES.md`
2. `AGENTS.md`
3. 最近的 `memory/YYYY-MM-DD.md`
4. 当前代码和配置
5. `docs/` 中仍保留的当前文档

### 3.8 迁移协作规则

数据库迁移、线上库结构调整、线上数据回填、线上发布回滚属于高风险操作，必须由用户严格配合确认。

默认规则：

- AI 可以准备迁移方案、校验脚本、回填脚本、演练步骤和风险清单。
- AI 不能在没有用户明确确认的情况下推进线上迁移、线上回填、线上清库、线上结构变更或线上发布。
- 涉及线上迁移时，必须让用户参与确认当前库、目标库、备份、执行时机、回滚方式和验证结果。
- 本地 `test_` 库可以继续用于演练，但不能把本地演练等同于线上迁移完成。

### 3.9 左侧菜单军队排序规则

左侧菜单中的主军队展示必须使用固定军事分类顺序，不能跟随接口返回顺序跳动。

固定顺序：

1. 步兵 `infantry`
2. 骑兵 `cavalry`
3. 攻城 `siege`
4. 特殊 `special`

同一分类内可按解锁等级、配置顺序或名称做稳定兜底排序；任何兵力更新、征兵、战斗、兑换或刷新都不应改变这个主顺序。

### 3.10 主分支与生产数据库规则

后续 Hero3 主开发分支和生产发布分支统一使用 `main-core`。

- `main-core` 是后续主仓库开发、合并和线上发布来源。
- `main` 保留为旧发布线或历史稳定线，不再作为日常主开发分支。
- GitHub Actions 生产发布应监听 `main-core`，并由服务器拉取指定提交构建部署。

服务器生产库拆分为三套：

- `hero3`：原生产库，旧服务仍可作为回滚参照。
- `old_hero3`：核心旧数据备份库。
- `new_hero3`：新核心结构迁移库，完成迁移和校验后作为新核心上线目标库。

新核心首次上线时，应让生产服务使用 `new_hero3`。后续发布不应自动执行清库、复制数据或回填命令；只允许后端启动时执行轻量表结构初始化和兼容迁移。

### 3.11 物品与背包系统后续规则

物品系统后续开发按完整背包系统推进，不只停留在武将页临时物品列表。

稳定规则：

- 当前背包入口在武将页侧边区域，后续该区域应改为格子形背包展示。
- 背包容量限制为 9999。
- 物品堆叠上限必须生效；超过单格堆叠上限时应拆分为多个格子或多条堆叠记录。
- 物品品质分为 5 个等级，参考钓鱼品质体系，从普通到史诗；史诗为金色表现。
- 只有史诗品质物品使用时需要二次确认，普通品质物品不弹二次确认。
- 玩家交易暂缓，不进入当前物品系统改造范围。
- 玩法掉落应先在物品配置中定义，再由副本、据点、活动、PVP、NPC 等玩法通过奖励或掉落配置引用，不应由玩法硬编码发具体物品。
- Admin 后台需要支持物品相关管理能力，包括发放、查看和后续配置维护。
- 物品流水需要补齐，记录获得、消耗、来源、数量、玩家和时间。
- 物品配置后续还要继续讨论确认；未确认前不应随意扩展大量正式物品。

### 3.12 并发与战报根治顺序

后续处理线上数据库死锁、战报膨胀和资产事务排队问题时，按以下顺序推进，不能跳过前置治理直接扩大玩法写入量。

1. 先做战报物理清理与保留策略，包含过期战报、软删战报、事件、状态、参与方和分享链接的分批清理。
2. 再限制战报产生量，NPC 扫荡等高频玩法优先合并战报，只对关键结果保留完整战报。
3. 最后重构道具、NPC、奖励等资产事务，收窄 `FOR UPDATE` 范围，把全量背包同步改为增量写入，减少玩家资产表互锁。
