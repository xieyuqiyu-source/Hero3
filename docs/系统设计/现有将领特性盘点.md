# 现有将领特性盘点

最后更新：2026-07-20

## 1. 文档目的

本文档用于盘点项目中当前已有的将领特性，作为后续“将领特性短期治理”的基础资料。

当前盘点来源：

- `go/config/generals.json`
- `go/internal/core/general/traits/`
- `go/internal/app/game/general_effects.go`
- `go/internal/app/game/general_growth.go`

本文档只描述“现在项目里已有的特性长什么样”，不代表所有特性都已经完成验收。

## 2. 当前整体结构

当前每个启用将领基本采用“双特性结构”：

- `specialTrait`：特殊特性，偏主动触发、战前削弱、战后伤害、减损、行军、掠夺等效果。
- `bonusTrait`：加成特性，偏攻防、兵种、速度、减损、产量等效果。

将领属性点中，“武力”对应系统 key：

```text
force
```

当前换算规则：

```text
1 点 force = 2% 攻击加成
GeneralStatPercentPerPoint = 0.02
最终进入 StatAttackBonus
```

这类属性加成属于被动属性装配，不应当作为战斗中触发的特性写入战报。

## 3. 已注册特性类型

当前特性大致分为以下几类。

| 类型 | 触发阶段 | 典型效果 | 示例 |
| --- | --- | --- | --- |
| 战前削弱 | `before_battle` | 战斗开始前减少敌方参战兵力 | 疑兵偷袭、水淹七军、威震震慑、奇门遁甲 |
| 战前攻防修正 | `before_battle` | 修改本次战斗单位攻击、防御 | 武圣破军、江东固守、魏武统御 |
| 战斗结算后追加伤害 | `after_combat_resolve` | 修改 `CombatResult`，追加敌方损失 | 火攻、西凉突击、老当益壮、火烧联营 |
| 战斗结算后减损 | `after_combat_resolve` | 降低己方本场损失 | 龙胆救援 |
| 战斗结束后返还/复活 | `after_battle` | 战后把部分损失加回 | 仁德、仁主守护、护主死战 |
| 行军速度 | `march_create` | 缩短出征或增援行军时间 | 疾行奔袭、七进七出、白衣渡江、白衣急行、快如闪电 |
| 征兵消耗 | `recruit_cost` | 降低征兵资源消耗 | 神鬼之才、王佐之才 |
| 掠夺收益 | `plunder_resolve` | 提高或降低掠夺收益 | 锦帆劫掠、江东号令 |
| 特性压制 | `after_combat_resolve` | 压制敌方若干特性触发 | 卧龙谋制、苦肉计 |
| 被动属性 | 装配阶段 | 进入将领最终四维或 Modifier，不进战报触发 | 天神下凡、内政精营 |

## 4. 魏国将领特性

| 将领 | 特殊特性 | 当前效果 | 加成特性 | 当前效果 |
| --- | --- | --- | --- | --- |
| 曹操 | 魏武号令 `weiwu_haoling` | 留城时资源结算每分钟补充 500 虎卫，单次最多 3000；离城失效且不写战斗触发 | 魏武统御 `weiwu_tongyu` | 所带虎卫攻击、步防、骑防各提升 10%；主动进攻、守城或作为援军均可生效 |
| 司马懿 | 疑兵偷袭 `yibing_touxi` | 战前有 35% 概率使敌方 35% 兵力形成真实伤亡，进入最终扣兵和战报 | 谋定后发 `mouding_houfa` | 仅防守时使进攻方全军攻击降低 10% |
| 甄宓 | 美人计 `meiren` | 仅主动进攻战斗前必定俘虏敌方各兵种 20%，单兵种最多 10000；同阵营兵种归队，跨阵营兵种进入驻防 | 魅惑扰阵 `meihuo_raozhen` | 仅主动进攻时使敌方全军两类防御降低 10% |
| 许褚 | 虎痴冲阵 `huchi_chongzhen` | 仅主动进攻时有 35% 概率使敌方全军两类防御降低 20% | 破敌防御 `pojun_pofang` | 仅主动进攻时使敌方全军两类防御降低 35% |
| 典韦 | 护主死战 `huzhu_sizhan` | 进攻、守城或作为援军战败后有 35% 概率返还本场真实阵亡的 15%，单场最多 10000 | 死战到底 `sizhandaodi` | 仅主动进攻时提升所带步兵攻击 35% |
| 夏侯渊 | 疾行奔袭 `jixing_benxi` | 出征或增援创建时固定提升 20% 行军速度，最低 60 秒 | 盾阵防御 `dunzhen_fangyu` | 仅防守或增援时使所带全军两类防御提升 35% |
| 张辽 | 威震震慑 `weizhen_zhenhe` | 战前有 35% 概率使敌方 20% 兵力仅本场不参战，战后保留 | 威震逍遥 `weizhen_xiaoyao` | 仅主动进攻时提升所带骑兵攻击 35% |
| 郭嘉 | 神鬼之才 `shengui_zhicai` | 留城时降低 50% 征兵资源消耗，离城失效 | 鬼才遗策 `guicai_yice` | 进攻、守城或作为援军战败后返还本场真实阵亡的 10%，单场最多 10000 |
| 荀彧 | 王佐之才 `wangzuo_zhicai` | 留城时降低 5% 征兵资源消耗，离城失效 | 内政精营 `neizheng_jingying` | 作为主将留城时提升 5% 资源产量，离城失效且不写战斗触发 |

## 5. 蜀国将领特性

| 将领 | 特殊特性 | 当前效果 | 加成特性 | 当前效果 |
| --- | --- | --- | --- | --- |
| 刘备 | 仁德 `rende` | 进攻、守城或作为援军战斗结束后必定按本场真实阵亡复活 50%，单场最多 10000 | 仁主守护 `renzhu_shouhu` | 战后返还本场真实阵亡的 10%，单场最多 10000，累计归队不超过本场阵亡 |
| 关羽 | 水淹七军 `shuiyan_qijun` | 战前有 35% 概率使敌方 35% 兵力形成真实伤亡，进入最终扣兵和战报 | 武圣破军 `wusheng_pojun` | 仅主动进攻时提升所带全军攻击 20% |
| 张飞 | 震慑全军 `zhenhe_quanjun` | 战前有 50% 概率使敌方 50% 兵力仅本场不参战，战后保留 | 万人怒吼 `wanren_nuhou` | 仅主动进攻时提升所带步兵攻击 20% |
| 诸葛亮 | 奇门遁甲 `qimen_dunjia` | 战前必定使敌方 25% 兵力仅本场不参战，战后保留 | 卧龙谋制 `wolong_mouzhi` | 战斗结算后必定拦截敌方 1 项后续特性；无可拦截特性时实际压制数为 0 |
| 赵云 | 龙胆救援 `longdan_jiuyuan` | 仅防守或作为援军时有 35% 概率降低 20% 本场最终实际损失 | 七进七出 `qijin_qichu` | 主动出征或增援创建时固定提升 100% 行军速度，最低 60 秒 |
| 马超 | 西凉突击 `xiliang_tuji` | 进攻或防守时有 35% 概率按敌方骑兵战前人数尝试追加 12% 损失 | 天神下凡 `tianshen_xiafan` | 被动增加 20 点 `force`，转换为 40% 攻击加成，不写触发战报 |
| 黄忠 | 百步穿杨 `baibu_chuanyang` | 仅主动进攻时有 35% 概率使敌方全军两类防御降低 20% | 老当益壮 `laodang_yizhuang` | 进攻或防守主将战斗结算后按敌方各兵种战前人数尝试追加 10% 损失 |
| 魏延 | 奇兵绕后 `qibing_raohou` | 仅主动进攻时有 35% 概率使敌方全军两类防御降低 20% | 固守汉中 `gushou_hanzhong` | 仅防守或增援时使所带全军两类防御各增加 20 点 |

## 6. 吴国将领特性

| 将领 | 特殊特性 | 当前效果 | 加成特性 | 当前效果 |
| --- | --- | --- | --- | --- |
| 孙权 | 江东号令 `jiangdong_haoling` | 仅掠夺战防守失败时使敌方最终掠夺收益降低 20%，普通进攻或防守获胜时无效 | 江东固守 `jiangdong_gushou` | 防守或增援时有 50% 概率使所带全军两类防御提升 50% |
| 孙策 | 小霸王追击 `xiaobawang_zhuiji` | 仅掠夺战胜利后有 35% 概率按敌方各兵种战前人数尝试追加 10% 损失 | 小霸王 `xiaobawang_tieqi` | 仅主动进攻时使所带霸王骑单位攻击固定增加 50 点 |
| 周瑜 | 火攻 `huogong` | 仅主动进攻方在战斗结算后必定按敌方各兵种战前人数尝试追加 25% 损失，防守方不会触发 | 美周郎军略 `meizhoulang_junlue` | 仅主动进攻时提升所带全军攻击 5% |
| 陆逊 | 火烧联营 `huoshao_lianying` | 进攻或防守主将有 35% 概率按敌方步兵战前人数追加 100% 损失，使目标步兵最终全灭 | 连营增伤 `lianying_zengshang` | 进攻或防守主将按敌方步兵战前人数尝试追加 10% 损失 |
| 吕蒙 | 白衣渡江 `baiyi_dujiang` | 出征或增援时 35% 概率提升 20% 行军速度，最低 60 秒 | 白衣急行 `baiyi_jixing` | 固定提升 20% 行军速度，最低 60 秒，可与白衣渡江逐次叠加 |
| 太史慈 | 快如闪电 `kuairu_shandian` | 出征或增援时 35% 概率提升 400% 行军速度，最低 30 秒 | 信义勇烈 `xinyi_yonglie` | 只提升所带援军自身两类防御 10%，主动进攻和主城守军无效 |
| 甘宁 | 锦帆劫掠 `jinfan_jielue` | 仅掠夺战主动进攻获胜时使最终掠夺收益提升 20%，普通进攻或掠夺战败时无效 | 锦帆奇袭 `jinfan_qixi` | 仅掠夺战主动进攻时提升所带全军攻击 10%，普通进攻无效 |
| 黄盖 | 苦肉计 `kurouji` | 战斗结算后有 35% 概率拦截敌方 1 项后续特性；无可拦截特性时实际压制数为 0 | 苦肉反击 `kurou_fanji` | 进攻或防守主将战斗结算后按敌方各兵种战前人数尝试追加 10% 损失 |

## 7. 特殊硬编码特性

除统一模板 `catalog.go` 外，当前还有几个独立文件实现的特性。

| 特性 | 文件 | 当前说明 |
| --- | --- | --- |
| 美人计 `meiren` | `meiren.go` | 战斗开始前俘虏敌方部分兵力；核心记录候选俘虏，应用层按真实兵种阵营把同阵营兵归队、跨阵营兵送入驻防 |
| 火攻 `huogong` | `huogong.go` | 战斗结算后给敌方追加损失，已限制为主动进攻方触发 |
| 仁德 `rende` | `rende.go` | 战斗结束后按损失比例复活士兵 |

旧的无下划线 ID `weizhenxiaoyao` 已删除。它没有被正式将领配置使用，却会和正式的 `weizhen_xiaoyao` 同名出现在 GM 注册表中；其震慑职责由 `weizhen_zhenhe` 承担，骑兵加攻职责由 `weizhen_xiaoyao` 承担。

## 8. 首轮治理结果

截至 2026-07-19，已完成以下基础修复和自动化验证：

### 8.1 完整性矩阵

以下统计必须区分“代码存在”“主流程验收”和“全部场景完成”，不能互相替代：

| 验收层级 | 当前进度 | 说明 |
| --- | --- | --- |
| 正式 ID、后端注册和双前端中文名称 | 50/50 | 只能证明特性可识别，不代表实际战斗有效 |
| 正式配置参数锁定 | 50/50 | 每项正式特性的归属、场景、参数和核心可消费效果均已锁定 |
| 按将领完成代表性真实主流程 | 25/25 | 每名将领至少有资源、征兵、行军、PVP、NPC、黄巾、扫荡或增援中的权威状态与战报对账；这不代表每项特性的全部场景都已完成 |
| 本轮定义的严格治理矩阵 | 50/50 | 50 项正式特性均已具备注册、正式配置、真实效果、战报结果、后端业务测试和双前端中文展示证据；随机项另有合法未命中与错误场景证据 |
| 开放式全组合治理 | 持续维护 | 新增玩法、修改配置或出现新组合时重新开启对应矩阵；它不是可以一次穷举完的固定验收项 |

代表性主流程已经验收的 25 名将领：曹操、司马懿、甄宓、许褚、典韦、夏侯渊、张辽、郭嘉、荀彧、刘备、关羽、张飞、诸葛亮、赵云、马超、黄忠、魏延、孙权、孙策、周瑜、陆逊、吕蒙、太史慈、甘宁、黄盖。

因此截至 2026-07-20，本轮短期治理可以记为“代表性将领主流程 100%，正式特性配置与真实收益 100%，本轮定义的严格治理矩阵 100%”。这表示当前 25 名将领、50 项正式特性和已列玩法入口的既定验收项已经收口；后续新增玩法、调整配置或发现新的跨模块组合时，仍按持续治理提示词重新打开对应项目，不能把本轮结论当作永久豁免。

- 增加 `allowedSides`、`allowedScenes`、`requiredOutcome`，由核心事件总线统一约束触发方、场景和胜负条件。
- 周瑜火攻已验证进攻触发、防守不触发；火攻战报追加伤害已与真实守军扣兵同值校验。
- 孙权江东固守已验证进攻无效、防守增加 50% 防御，并在 PVP 战报中归属防守将领。
- 赵云龙胆救援限制为防守或增援，主动进攻不会误触发；真实 PVP 对照验证守城主将和赵云所在援军批次均按最终损失降低 20%，实际剩余兵力、攻方战报、守方战报和援军独立战报使用同一逐兵种减损值。两个前端同时展示后端返回的设计减损比例和真实逐兵种减损，不自行按比例重算。
- 太史慈信义勇烈已从当前守方核心不消费的援军攻击改为核心可结算的援军两类防御 `+10%`，只修改太史慈所在援军批次；真实 PVP 开关对照验证防御战力由 `1010` 提升到 `1110` 且实际兵损下降，进攻方、防守方和援军方三份战报均记录步防、骑防实际 `+1`。主动进攻与主城守军继续由 `allowedSides=[reinforcement]` 和反向测试锁定为无效。魏延奇兵绕后限制为进攻方；甘宁两项能力限制在掠夺场景。
- 司马懿谋定后发补齐遗漏的敌方攻击削弱分支；曹操魏武统御现在会同时应用攻击和防御参数。
- 马超天神下凡改为被动 `force +20`，进入 `effectiveStats` 和攻击 Modifier，不进入 `TraitTriggered`。
- 仁德已验证 PVP 中 `返回兵力 = 出动 - 阵亡 + 复活`，特性结果同时记录复活总数和逐兵种 `revivedUnits`，与回城后的真实兵力一致。
- 仁德和仁主守护已验证黄巾防守及真实 PVP 进攻、守城、援军：正式配置分别按原始真实损失复活 50% 和返还 10%，实际返程、守军或援军兵力与三方战报一致。修复敌方视角标准详情仍显示战前返兵值及援军独立战报漏记归队兵力的问题；同场多个复活/返还效果累计不能超过本场真实阵亡，战报只记录实际返还值。
- 战报新增 `survivedUnits` 作为战后实际存活/归队快照；扫荡中即使复活兵重复参战导致累计阵亡超过初始出动，也不再用错误减法推算存活。
- 郭嘉鬼才遗策、典韦护主死战已完成 PVP 进攻、守城和援军的胜负正反验证：所属部队战败才返还本场真实阵亡的 10%/15%，获胜不触发。援军独立战报使用“原始阵亡、战后归队、最终存活”口径，主战双方援军损失汇总使用最终净损失；两种口径都与援军记录的实际剩余兵力对账。
- 郭嘉双特性已完成同一真实存档的跨流程验收：留城征兵 2 名、基础消耗 200 木材时，神鬼之才实际只扣除 100 木材并建立征兵队列，不生成战斗战报；随后郭嘉带 100 名魏步兵出征战败，原始阵亡 100，鬼才遗策返还 10，返程和标准战报最终存活均为 10。参战快照保留神鬼之才与鬼才遗策两项拥有特性，但新旧触发时间线只出现鬼才遗策；`web` 与 `web-wlsg` 均锁定原始阵亡、实际返还、最终存活和非战斗特性不被补造。
- 荀彧王佐之才与内政精营已完成同一真实存档的留城、出征、战斗和归城闭环：留城一小时木材产量为 `53`，征兵 2 名基础消耗 200 时实际扣除 `190`；出征期间一小时只生产 `50` 木材并按原价扣除 `200`；归城后再次恢复 `53` 产量和 `190` 征兵成本。真实 PVP 中 100 名魏步兵对 50 名守军，荀彧方阵亡 `37`、返程 `63`，守军阵亡 `50`；双方参战快照保留两项拥有特性，但新旧战报和标准战报触发时间线均为空。`web` 与 `web-wlsg` 同步锁定兵损、经验和非战斗特性不被补造。
- 曹操魏武号令与魏武统御已完成同一真实存档的资产到战斗闭环：出征事务先按留城状态结算魏武号令并产出 `3000` 虎卫，本次立即选取其中 `100` 人出征，城内权威库存剩余 `2900`；离城 24 小时不再产兵。魏武统御把本场虎卫攻击、步防和骑防从 `10/10/8` 提升为 `11/11/9`，攻防战力为 `1100/1000`，虎卫阵亡 `87`、返程 `13`，100 名守军全灭。归城库存为 `2913`，再结算一个留城时段后准确增加到 `5913`。双方参战快照保留两项拥有特性，触发时间线只出现魏武统御及实际 `+1/+1/+1`；`web` 与 `web-wlsg` 同步锁定战力、兵损、经验和魏武号令不被补造成战斗触发。
- NPC 单城攻击和扫荡已补齐武将校验失败的事务原子性验收：测试让曹操存档具备可立即触发魏武号令产兵的结算时间与小数进度，再故意同时选择两名将领。两条请求均返回武将校验错误，玩家兵力、资源结算时间、特性进度、武将状态、NPC 城池、奖励和战报记录逐字段保持不变。`web` 锁定两个 NPC 入口只在成功响应后回写状态和打开战报；`web-wlsg` 真实模拟接口失败，确认玩家状态不变、结果标记为失败且只展示后端错误。
- PVP 与 NPC 侦查均已补齐留城特性异常回滚验收：曹操存档在魏武号令已达到可结算时间且小数进度为 `0.5` 时，仅保留 100 虎卫而不提供本阵营侦察兵。两类侦查返回兵力不足后，虎卫数量、资源结算时间、魏武号令进度和 NPC 城池逐字段保持不变；PVP 目标玩家也不变化，双方均不生成行军或战报。内存仓储克隆事务、NPC 战斗事务与 MySQL 双玩家事务保证待结算产兵不会半提交；`web` 的两个 NPC 侦查入口和玩家侦查入口均只在成功响应后回写，`web-wlsg` 真实模拟 NPC 与玩家侦查失败，确认资源、兵力、进度、结算时间、NPC 状态和本地行军保持不变。
- 孙策小霸王追击已限制为掠夺场景，并完成掠夺获胜、普通进攻、掠夺战败三种 PVP 验证；只有掠夺获胜会追加正数损失，防守方真实扣兵和双方战报一致。追加伤害为零时不再生成误导性触发记录。
- 战前 `pre_damage` 与 `suppress` 已拆分结算口径：疑兵偷袭、水淹七军形成真实伤亡并进入 `CombatResult`、经验、扣兵和战报；威震震慑、震慑全军、奇门遁甲只让部分兵本场不参战，战后仍保留。
- 司马懿疑兵偷袭、关羽水淹七军已完成正式 ID 的真实 PVP 攻守双向验收：两项特性由进攻主将或防守主将携带时，都会对敌方 1000 人造成准确的 350 人战前真实伤亡；该数值进入最终战损，双方旧战报与标准战报一致，进攻返程兵力或守军最终兵力均按完整出动数减最终实际损失对账。两个前端均显示正式名称、阶段“战斗前”和逐兵种“战前真实伤亡 +350”。
- 张辽威震震慑、张飞震慑全军、诸葛亮奇门遁甲已完成正式将领真实 PVP 攻守双向验收：三项特性分别按 20%、50%、25% 降低敌方本场参战战力，压制兵不进入阵亡。战斗记录、双方新旧战报、守军最终兵力或进攻返程兵力均以完整出战基数减真实阵亡对账；两个前端统一显示“本场压制兵力”。
- 已完成疑兵偷袭、水淹七军、威震震慑、震慑全军、奇门遁甲五个正式 ID 的 NPC 单城逐项验收：前两项均对 100 人守军产生 35 人战前真实伤亡并进入最终扣兵；后三项分别压制 20、50、25 人，仅减少本场参战人数且战后保留。五项的设计比例、逐兵种实际数量、玩家返兵、NPC 剩余兵力、旧战报、标准战报及将领归属完全一致，`web` 与 `web-wlsg` 同步展示“战前真实伤亡”或“本场压制兵力”。PVP 防守方战前伤亡、黄巾守城战前伤亡、主城与援军混编分摊也已完成端到端验证；同时修复战斗军团与原始出征快照共用切片导致战前伤亡重复扣除的问题。
- NPC 压制战报会恢复完整战前兵力基数，标准战报的阵亡和剩余与 NPC 真实守军一致；25% 情报阈值也按包含压制兵的完整基数计算。`web` 与 `web-wlsg` 分别显示“战前真实伤亡”和“本场压制兵力”。
- `web` 不再把所有 `revivedUnits` 固定显示成“仁德复活”，统一显示为“战后归队兵力”；具体是复活还是最终减损返还，继续由特性结果明细说明。
- 曹操魏武号令明确为资源结算过程能力，并补充真实增兵测试；荀彧内政精营明确为留城被动 Modifier。真实一小时资源结算验证当前测试平衡下留城木材产量由基础 `50/h` 提升为 `53/h`，离城后恢复 `50/h`；PVP 或增援归城会先按离城状态结算，再释放武将并刷新当前产量，避免追溯补发离城期间的内政收益。该被动不进入战斗触发战报，两个前端只使用后端返回的当前产量。
- 曹操魏武统御的目标从不存在的“特殊兵分类”收敛为真实兵种 `huWei`；孙策小霸王的目标从无效 ID `overlord_cavalry` 修正为真实兵种 `overlordRider`。正式配置测试验证曹操只强化虎卫、孙策只强化霸王骑。
- 曹操魏武统御已完成真实 PVP 攻守双向验收：100 虎卫进攻时攻击战力从 1000 提升为 1100，防守时防御战力从 1000 提升为 1100；双方新旧战报分别记录虎卫实际攻击、步防、骑防 `+1/+1/+1`，战斗记录、守军状态和进攻返程兵力一致。两个前端同步展示三类实际修正。
- 将领配置加载新增 `targetUnitType` 校验：只允许已注册兵种 ID 或已存在兵种分类；写错目标时直接拒绝配置，避免特性静默失效。新战报把原先含义模糊的 `modifiedUnits` 拆为逐兵种 `attackModifiedUnits`、`infantryDefenseModifiedUnits`、`cavalryDefenseModifiedUnits`，两个前端分别显示实际攻击、步防和骑防修正，并继续兼容旧字段。
- 模板特性不再给未配置 `effectRate` 的特性伪造“效果比例 0%”；固定值、专用比例和玩法过程特性只展示自身真实结果，避免战报同时出现有效修正和零效果比例。
- 修复分类目标无法命中真实兵种 ID：马超西凉突击的 `cavalry`、陆逊火烧联营和连营增伤的 `infantry` 现在会根据战斗军团中的兵种分类匹配实际损失项。西凉突击已完成真实 PVP 攻守双向验收：马超进攻时追加守方骑兵损失，守城时追加攻方骑兵损失，同行步兵不受影响；双方新旧战报、战斗记录、守军或返程真实兵力使用同一逐兵种增量。两个前端同步显示后端实际追加损失，不按配置比例重算。
- 战前百分比攻防特性按配置顺序逐次作用，每次对当前整数属性四舍五入；许褚双破防已验证 `101 -> 81 -> 53`，两项特性分别在战报记录 `-20`、`-28` 的真实步防和骑防变化。魏延固守汉中文案同步收敛为真实效果“全军两类防御固定提升”。
- 攻守交叉战前属性已完成真实 PVP 验证：攻方破敌防御先把吴步兵两类防御 `10/8 -> 7/5`，守方江东固守再提升为 `11/8`，最终防御战力与双方战报分别记录的 `-3/-3`、`+4/+3` 一致。
- 攻守双方各自强化的战前属性也已完成正式特性交叉验收：曹操进攻方魏武统御把 1000 虎卫单兵攻击 `10 -> 11`，孙权防守方江东固守把 1000 吴步兵步防/骑防 `10/8 -> 15/12`，最终攻防战力为 `11000/15000`。掠夺战进攻方损失 `608`、返程 `392`，防守方损失 `391`、剩余 `609`，曹操与孙权经验分别增加 `391/608`。双方新旧战报按“魏武统御、江东固守”记录攻方实际攻击 `+1` 和守方实际防御 `+5/+4`，标准兵力行、权威状态和两个玩家前端完全对账，两个特性不会串方。
- 孙权江东固守已继续补齐“防守方向正确但随机未命中”的真实 PVP 边界：`triggerChance=0` 时等势双方保持基础 `10000/10000` 战力，兵损、存活和经验均为 `500/500`，返程和守军权威兵力一致。孙权拥有快照仍保留正式特性，但双方旧结果与标准时间线均为空；两个玩家前端不会因为当前方向允许或快照存在而补造防御加成。该验收与既有防守命中、主动进攻无效共同区分概率、方向和实际触发三种口径。
- 马超被动与随机触发的独立性也已完成真实 PVP 验收：西凉突击固定未命中时，天神下凡仍以 `force +20` 提供 40% 攻击 Modifier，形成 `14000/10000` 战力和核心兵损 `382/617`；守方不会再承受成功场景中的骑兵追加损失 `120`。权威存活 `618/383`、经验 `617/382` 与双方标准兵力行一致。马超快照保留两项拥有特性及被动 Modifier，触发时间线为空；两个前端只展示后端被动战力，不把任一能力补造成随机触发结果。
- 攻守双方连续修改同一攻击属性也已完成顺序敏感验收：孙策小霸王先把 200 名霸王骑单兵攻击 `28 -> 78`，司马懿谋定后发随后按当前攻击降低 10% 得到 `70`，最终攻击战力为 `14000`。普通进攻准确结算霸王骑损失/返程 `90/110`、魏步兵损失/剩余 `1000/0`，武将经验按敌军真实兵种权重分别为 `1000/360`。双方新旧战报和标准时间线按真实分发顺序分别保存小霸王实际 `+50`、谋定后发实际 `-8`；`web` 与 `web-wlsg` 均按攻守侧展示后端整数，不把两项合并或从比例重算。
- 攻守双方连续修改同一防御属性也已完成顺序敏感验收：甄宓主动进攻先由魅惑扰阵把吴步兵步防/骑防 `10/8 -> 9/7`，孙权防守随后由江东固守基于当前整数加成 50%，得到 `14/11`，实际变化分别为 `-1/-1` 与 `+5/+4`，不能让两项各自读取原始属性。1000 虎卫对 1000 吴步兵形成 `10000/14000` 战力，真实兵损/存活为 `617/383` 与 `382/618`，甄宓、孙权经验分别为 `382/617`。双方旧战报和标准时间线固定为“魅惑扰阵、江东固守”，玩家状态、返程和两个前端的攻守归属、中文兵种与实际整数完全对账。
- PVP 双方战报统一合并攻方战前、守方战前、战斗结算后、攻方战后、守方战后的完整结果，修复防御战报漏攻方战前/战后特性、攻击战报漏守方战后特性的问题。
- 双方同优先级压制特性现在同时结算，不再由攻方列表顺序取得天然先手；双方压制都进入双方战报，只跳过更低优先级的后续特性。主战场未消费的压制次数会继续作用于同为战斗结算后阶段的援军特性，但不会追溯取消战前援军加成，也不会跨到战后返还阶段。
- 诸葛亮卧龙谋制、黄盖苦肉计已完成正式 ID 的真实 PVP 攻守双向对照验收：无压制时敌方老当益壮真实追加 500 损失，压制后该特性不触发且实际战损恰好减少 500；双方新旧战报只记录压制方“压制特性数 +1”，不再出现被压制特性，最终留城或返程兵力与战斗记录一致。两个前端均覆盖两个正式名称和真实压制数量。
- 甄宓美人计已完成真实 PVP 正反验收：跨阵营俘虏会在 PVP 主事务中原子合并到进攻方“获得驻防”，同阵营俘虏进入进攻方返程军队，防守方携带时不触发。双方战报、敌方真实扣兵、返程军队或驻防实际兵力使用同一逐兵种数值；已结算行军重复读取不会重复发放俘虏兵。
- 美人计战报不再只记录 `totalCaptured`，同时按去向记录 `capturedUnits` 或 `capturedToGarrison`。`web` 与 `web-wlsg` 均统一正式名称“美人计”，并分别显示“俘虏归队”或“俘虏驻防”的具体兵种和数量。
- 美人计单兵种俘虏上限已完成真实 PVP 封顶验收：两类 20000 人守军按 20% 原应各俘虏 4000，正式测试上限 1000 后实际各俘虏 1000，总数 2000。剩余 19000 步兵和 19000 骑兵形成 `342000` 防御战力，目标权威库存各剩 19000，获得驻防各增加 1000；战斗记录总减员包含俘虏，标准战报 `Lost` 只记阵亡并保持 `20000/0/19000`，经验也只按真实阵亡结算。双方新旧战报、逐兵种结果、总数和重复结算幂等一致，两个玩家前端同步展示“设计单兵种俘虏上限 1000”而不误解为全场总上限。
- 修复已结算 PVP 重复读取仍再次执行后置副作用的问题：PVP 积分、保护状态和援军武将经验现在只在首次事务提交产生新战报时发放；真实增援战斗验证重复读取后双方积分、援军将领经验和战报数量均不变化。
- `web` 按同一武将的后端援军快照精确汇总经验；`web-wlsg` 不再丢弃 `pvpReinforcements.generalExpGained`，增援参战方会显示本场真实武将经验。
- 主战武将经验与等级变化已完成真实 PVP 对账：测试把曹操设置为距离升级只差 1 点经验，战斗后数据库累计经验、最终等级、旧战报和标准战报中的本场经验及 `1 -> 2` 等级变化完全一致。`web` 与 `web-wlsg` 只在 `generalLevelAfter > generalLevelBefore` 时展示等级变化，不升级时只显示后端经验值。
- 黄巾防守武将经验与升级也已完成真实战斗对账：刘备从临界经验进入防守后，数据库累计经验、最终等级、旧战报和标准战报完全一致。修复 `web-wlsg` 固定把全局经验挂到 `primarySide` 的错误，现在根据标准战报 `ownerSide` 把经验归给防守方 `secondarySide`；`web` 原有的同类归属逻辑已补回归测试，避免经验数值正确却显示在敌方名下。
- 谋定后发、盾阵防御、固守汉中、江东固守已补齐黄巾守城真实战斗验收：谋定后发按正式 10% 降低黄巾攻击并记录单兵攻击 `-1`；后三项分别按 35%、固定 20 点、50% 提高守军防御，并记录单兵步防/骑防 `+4/+3`、`+20/+20`、`+5/+4`。对照战力、最终守军、旧战报和标准战报完全一致，标准特性归属为防守方。司马懿正式双特性继续验证同阶段组合顺序：1000 名黄巾先由疑兵偷袭产生 `350` 名真实伤亡，剩余 650 人再由谋定后发把单兵攻击从 `10` 降为 `9`，攻击战力准确由无特性对照的 `10000` 变为 `5850`；魏国 3% 城墙公共防御在两场均为 `10300`。最终黄巾损失包含战前伤亡和核心战损，司马懿经验、守军库存、旧战报与标准战报对账，时间线固定为“疑兵偷袭、谋定后发”。孙权也使用正式双特性完成同存档正反对照：100 名吴步兵叠加相同 2.5% 城墙时，关闭江东固守的防御战力为 `1025`，开启后为 `1537`，黄巾攻击战力保持 `1000`；江东号令因仅限掠夺不会触发，木材保持 `1000`，孙权经验、最终守军和两套战报完全对账。将领快照保留江东号令与江东固守，真实时间线只出现江东固守；`web` 与 `web-wlsg` 均锁定两项正式组合的阶段、归属和实际整数变化。
- 甘宁两项特性已完成普通进攻与掠夺战的真实 PVP 正反验收：普通进攻时锦帆奇袭与锦帆劫掠均不触发且不转移资源；掠夺胜利时锦帆奇袭按 +10% 把 100 名吴步兵攻击战力从 1000 提升到 1100，锦帆劫掠再按 +20% 修正最终资源。双方战报使用同一触发结果和实际整数变化。
- `web` 与 `web-wlsg` 均按后端顺序把锦帆奇袭标记为“掠夺战战斗前”、锦帆劫掠标记为“掠夺结算”，避免把普通进攻或配置比例误显示为真实收益。
- 魏延奇兵绕后已完成真实 PVP 攻守正反验收：魏延主动进攻时把敌方步防、骑防从 `10/8` 降为 `8/6`，防御战力从 1000 降为 800；魏延作为防守主将时不触发，防御战力保持 1000。双方战报记录同一逐兵种 `-2/-2` 实际变化，防守方真实兵力与战斗损失一致。
- `web` 与 `web-wlsg` 均把奇兵绕后明确标记为“主动进攻战斗前”，并分别展示实际步防、骑防变化。
- 纯防御特性不再在进攻侧生成无实际收益的触发战报：夏侯渊盾阵防御、魏延固守汉中、孙权江东固守统一限制为防守或增援。三项正式特性作为 100 人援军时，分别把包含 1 名主城守军的总防御战力从 `1010` 提升到 `1410`、`3010`、`1510`，并真实减少援军损失；三方战报、标准特性时间线和援军剩余兵力一致。主将场景也已验证盾阵防御形成 `10/8 -> 14/11`、固守汉中形成 `10/8 -> 30/28`，且进攻侧均不误触发。
- `web` 与 `web-wlsg` 均把三项纯防御特性明确标记为“防守/增援战斗前”，展示正式作用范围及后端返回的逐兵种实际步防、骑防变化。
- 敌方属性削弱特性已按核心真实战力方向收紧：谋定后发只允许防守方降低进攻攻击；魅惑扰阵、虎痴冲阵、破敌防御、百步穿杨只允许主动进攻方降低守军防御。五项真实 PVP 正反测试验证错误方向不触发，正确方向分别改变 `attackerPower` 或 `defensePower`；双方新旧战报记录同一逐兵种实际变化，守军最终兵力与战损一致。
- 魅惑扰阵、虎痴冲阵、破敌防御、百步穿杨、奇兵绕后已补齐 NPC 单城真实业务验收：固定 `10/8` 防御分别变为 `9/7`、`8/6`、`7/5`、`8/6`、`8/6`，NPC 防御战力对应变为 `900/800/700/800/800`。逐兵种步防、骑防实际变化，玩家返兵、NPC 剩余兵力、旧战报和标准战报完全一致；两个前端均验证 NPC 战报按主动进攻方展示设计破防比例和实际整数变化。
- `web` 与 `web-wlsg` 同步区分“防守战斗前”和“主动进攻战斗前”，避免把无实际战力作用的方向展示成有效触发。
- 已锁定谋定后发、魅惑扰阵、虎痴冲阵、破敌防御、百步穿杨、奇兵绕后、盾阵防御、固守汉中、江东固守共九项正式配置。真实战报同时返回语义明确的设计减攻、破防、百分比加防或固定加防值，以及逐兵种实际整数变化；魏武统御同步补齐攻击和防御两种设计值，两个前端均直接展示后端结果。
- 纯攻击加成已按核心真实战力方向收紧：死战到底、威震逍遥、武圣破军、万人怒吼、小霸王、美周郎军略只允许主动进攻方触发。六项真实 PVP 正反测试验证正确方向改变最终攻击战力并记录逐兵种实际变化，错误方向不触发；双方新旧战报与防守方真实兵力完成对账。
- `web` 与 `web-wlsg` 均把上述六项特性标记为“主动进攻战斗前”，并展示后端返回的实际攻击修正。
- 已锁定八项战前攻击加成的正式配置：魏武统御虎卫攻防 `+10%`、死战到底步兵攻击 `+35%`、威震逍遥骑兵攻击 `+35%`、武圣破军全军攻击 `+20%`、万人怒吼步兵攻击 `+20%`、小霸王霸王骑单位攻击 `+50`、美周郎军略全军攻击 `+5%`、锦帆奇袭掠夺战全军攻击 `+10%`。战报同时保存设计加成和整数取整后的逐兵种实际变化，两个前端均直接展示后端结果。
- 死战到底、威震逍遥、武圣破军、万人怒吼、小霸王、美周郎军略、锦帆奇袭已补齐 NPC 单城真实业务验收：固定 100 人出征分别产生单兵攻击 `+4/+5/+2/+2/+50/+1/+1`，最终攻击战力、玩家返兵、NPC 剩余兵力、旧战报和标准战报一致。锦帆奇袭只在 NPC 掠夺中生效，普通 NPC 进攻保持基础战力且不生成虚假特性结果；`web` 与 `web-wlsg` 同步验证主动进攻和掠夺阶段文案及后端实际修正。
- 黄忠老当益壮、陆逊火烧联营与连营增伤、黄盖苦肉反击已完成真实 PVP 攻守双向验收：四项特性由进攻主将或防守主将携带时都能追加敌方损失，双方新旧战报、战斗记录、守军实际扣兵或进攻返程兵力一致。战报记录本次实际增加量，例如 10% 特性对 1000 人记录 `+100`，火烧联营会扣除基础伤亡后只记录真正追加的数量。
- `web` 与 `web-wlsg` 均验证上述四项特性显示“战斗结算后”和逐兵种实际追加损失，不根据配置比例自行重算。
- 修复非战斗事件没有注入 `OwnerSide` 导致正式 `allowedSides` 约束永远不通过的问题；甘宁锦帆劫掠和孙权江东号令现在能分别以进攻方、防守方身份进入掠夺结算。
- PVP 掠夺改为先计算基础收益、再执行甘宁/孙权等掠夺特性、最后按修正后的最终数量转移资源；真实 PVP 已验证甘宁正增益、孙权负增益、攻方增加、守方减少、战斗记录与双方特性战报一致，仓库容量和守方保护资源也在最终转移时再次约束。修复普通进攻获胜也错误转移资源的问题，现在只有掠夺模式获胜才会进入资源结算；普通进攻和掠夺战败均不触发两项特性，也不改变双方资源。
- 甘宁锦帆劫掠与孙权江东号令已补齐特性修正后资源转移的重复结算幂等验收：首次结算保存甘宁正向、孙权负向的逐资源实际变化并完成最终木材转移；再次解析同一行军只返回原战斗和原战报，攻守双方木材不再变化，每名玩家仍只有一份战报，两项 `plunderDelta` 继续保持首次实际值。同步修复两个前端的主队/援军归属边界：过去 `web-wlsg` 会把 `secondary` 防守特性复制到任意非主方援军区，`web` 在防守主将与援军使用相同将领 ID 时也可能串方；现在明确归属方优先于将领 ID，防守结果只属于防守主队，只有 `ownerSide=reinforcement` 或 `ownerRole=reinforcement` 才能进入援军区。两端均锁定同一战报中甘宁、孙权资源修正各展示一次。
- 甘宁锦帆劫掠与孙权江东号令已继续完成同一行军双路并发解析验收：两个调用在 Go `-race` 下返回同一个战斗 ID、同一对战报 ID 和相同最终掠夺值；攻守资源只转移一次，双方兵损、武将经验、PVP 积分只结算一次，持久层只有一场战斗且每名玩家只有一份战报。两份战报都只保存一次甘宁正向与孙权负向 `plunderDelta`，进攻视角的奖励等于最终掠夺，防守视角只展示敌方所得而不会给自己生成奖励。内存事务锁与 MySQL 行军行 `FOR UPDATE` 保证第二路读取已落库结果；`web` 新增按战报 ID 保留首次权威记录的列表去重，`web-wlsg` 原有跨页去重补齐明确回归测试，异常重复响应也不会显示两份掠夺战报。
- 夏侯渊疾行奔袭、吕蒙白衣渡江/白衣急行、太史慈快如闪电已逐个完成真实 PVP 出征与增援创建验收：每个正式 ID 都修改最终秒数、速度倍率和到达时间，增援往返时长一致，且行军创建阶段不生成战斗战报。吕蒙双特性按 `ceil(ceil(基础秒数/1.2)/1.2)` 逐次叠加；概率为 0 时不触发，全部特性遵守各自最低秒数。两个前端均直接使用后端最终到达时间，不自行重算。
- 吕蒙双行军能力已继续完成同一支部队的跨流程验收：真实公共 Modifier 处理后的无特性基线为 `2970` 秒，白衣渡江与白衣急行逐次叠加后为 `2063` 秒，战后返程沿用相同真实时长。两份战报的吕蒙快照保留两项拥有特性，但触发结果和标准时间线为空，战斗经验与兵损照常结算；两个前端均验证不会从快照补造战斗触发。
- 吕蒙双行军能力已补齐同玩家并发出征验收：先以相同玩家状态关闭双特性的真实事务得到 `2970` 秒基线，开启后唯一成功行军按 `2970 -> 2063` 秒记录。玩家仅有 100 名吴步兵时，两路同时携吕蒙派出全部兵力，在 Go `-race` 下只能一笔成功，另一笔返回兵力不足；最终城内兵力为 0、吕蒙只有一条 PVP 占用、持久层只有一条行军、每日攻击次数只增加 1，目标到达前保持不变且不生成战报。`web` 对侦查、攻击/掠夺和增援三个入口增加同步请求锁；`web-wlsg` 保持全局出征互斥并验证只提交一次，两端都采用后端 `2063` 秒到达时间和权威兵力、占用状态。
- 吕蒙双行军能力也已补齐同玩家并发增援验收：仅有 100 名吴步兵时，两路同时携吕蒙向同一目标派出全部兵力，在 Go `-race` 下只能一笔成功并按真实对照记录 `2970 -> 2063` 秒往返时间，另一笔返回兵力不足。最终兵力只扣一次、吕蒙只有一条增援占用、派出方和接收方都只持久化同一条援军；到达前目标兵力与主将占用不变，双方均不生成战报。内存事务锁和 MySQL 双玩家稳定顺序行锁保证并发读取权威扣兵状态；`web` 的同步请求锁与 `web-wlsg` 的全局出征互斥均覆盖增援，两端只提交一次并采用后端到达时间、兵力和武将占用。
- 吕蒙作为真实援军的对称链路也已完成：双加速援军抵达后参与 `1000/1010` 攻防战力的守城战，最终魏军、蜀守军、吕蒙援军损失分别为 `100/1/98`，援军剩余 `2`，吕蒙经验 `+100` 并从 `Lv.1 -> Lv.2`。三方战报、驻防权威记录、玩家存档和两个前端完全对账，白衣渡江与白衣急行始终不进入战斗触发时间线。
- 太史慈已完成主动进攻的跨阶段反向边界验收：相同玩家状态下无特性的真实行军基线为 `2970` 秒，快如闪电必定触发时按 `ceil(2970/5)` 缩短为 `594` 秒，战后返程继续使用 `594` 秒；1000 名吴步兵对 1 名魏步兵时，信义勇烈因仅允许 `reinforcement` 而不触发，攻击战力保持 `10000` 而不是 `11000`，双方真实兵损为 `0/1`，太史慈经验 `+1`。双方战报保留两项拥有特性快照，但旧触发、结果明细和标准时间线全部为空。`web` 明确显示“援军战斗前，主动进攻或主城守军无效”，`web-wlsg` 验证主动进攻快照不会补造触发。
- 太史慈已补齐第十六项随机特性的合法未命中跨流程验收：同一真实增援场景中，快如闪电 `triggerChance=0` 时行军保持公共 Modifier 后的 `2970` 秒基线，不得误用强制命中的 `594` 秒；抵达后信义勇烈仍独立形成 `1110` 防御战力和步防、骑防 `+1/+1`。110 名魏步兵全部阵亡，主城 1 名守军阵亡，太史慈 100 名援军损失 `98`、存活 `2` 并获得 `110` 经验。进攻、防守、援军三份战报只记录信义勇烈，快照保留两项拥有特性；`web` 与 `web-wlsg` 同步锁定行军时间、战力、兵损、经验和中文兵种结果。至此 16 项低于 100% 的随机特性均已有合法未命中业务边界，其中选取的 16 场代表流程均不会从快照补造未触发结果。
- 太史慈信义勇烈已完成真实援军正向验收：关闭特性时守方总防御战力为 `1010`，启用后太史慈援军的步防、骑防各提升 10%，总防御战力变为 `1110` 并产生更低的真实兵损；三方战报均记录设计防御加成 `10%` 和逐兵种实际修正 `+1/+1`。`web` 与 `web-wlsg` 同步显示中文兵种名和后端实际整数，不在前端重算。
- 郭嘉神鬼之才与荀彧王佐之才已逐项通过真实征兵正反验证：基础消耗 200 木材时，留城分别实际扣除 100 和 190，离城后都恢复扣除 200。玩家资源状态使用最终扣费，征兵过程能力不生成战斗战报；`web` 明确显示正式比例和留城条件，`web-wlsg` 验证将领快照中的非战斗内政特性不会被伪造成战报触发。
- 两项征兵减耗特性已进一步完成同玩家并发验收：郭嘉仅有 100 木材、荀彧仅有 190 木材时，两路同时征募 2 名基础单价 100 木材的魏步兵，在 Go `-race` 下都只能一笔成功、另一笔明确返回资源不足；最终木材为 `0`，只存在一条数量为 `2` 的征兵队列且不生成战报。内存仓储以事务互斥串行更新，MySQL 事务以玩家行 `FOR UPDATE` 读取前一笔扣费后的状态。同步修复 `web` 征兵成功到弹窗关闭动画结束之间可再次点击的窗口：同步请求锁在请求期间也禁止关闭，成功后立即采用后端资源和队列；`web-wlsg` 保持全局征兵互斥，并以真实并发调用测试确认只请求一次、最终只接收后端的一条折后队列和 `0` 木材。
- 两项征兵减耗特性已补齐失败请求的完整原子性验收：郭嘉、荀彧留城且资源存在一小时待结算时，即使先按 `50%/5%` 计算折后成本，100000 人征兵仍因资源不足失败；失败前后的完整玩家存档逐字段一致，资源、当前产量、资源结算时间、队列和战报均不变化。同步修复征兵成功响应只返回资源数量、遗漏 `resourceProduction`、`resourceSettledAt` 和 `generalTraitProgress` 的契约缺口；否则两个前端会用新资源配合旧结算时间再次预测已结算产量。后端军事动作响应、OpenAPI、`web` 和 `web-wlsg` 现已统一返回并采用四项权威状态，空特性进度也以空对象明确覆盖旧值；失败时两个前端保持原状态，只显示错误。
- 魏武号令已补齐出征、侦查和增援成功响应的权威状态闭环：三类事务都会先结算留城产兵，再扣兵或占用武将；过去响应只带新兵力，两个前端仍保留旧资源结算时间和旧小数进度，可能把已经结算过的时段再次用于本地资源预测。现在 PVP 攻击、掠夺、玩家侦查均直接返回资源、当前产量、资源结算时间和非战斗特性进度；增援只在派出补丁中返回这组字段，召回、到达等未加载资源的操作不会误覆盖。三条真实曹操存档测试逐项核对响应与持久化状态完全相等，`web` 和 `web-wlsg` 同步采用权威结果；OpenAPI 新增 PVP 侦查与出征两条缺失路径，打包接口由 124 条增至 126 条。
- 同一权威响应规则已扩展到 NPC 进攻、扫荡和侦查：三类动作也会在战斗事务中先结算魏武号令，过去 NPC 成功响应缺少当前产量、资源结算时间和非战斗特性进度，侦查还缺少资源本身，可能让战报弹出后的玩家状态继续使用旧时间基线。现在三类响应都返回完整四项状态，`web` 的出征面板、NPC 快捷卡片和扫荡页以及 `web-wlsg` 的 NPC 状态服务均同步采用；三条真实曹操流程逐项核对响应与持久化状态完全相等，OpenAPI 的 NPC 响应契约和 126 条打包接口文档已同步。
- 曹操魏武号令已完成军事视图真实持久化和重复读取验收：24 小时离线结算受单次上限约束，权威军队增加 3000 虎卫；立即重复读取不会再次发放，且不会生成战斗战报。后端持久化不足 1 个虎卫的小数进度，一次结算 3 秒和连续三次各结算 1 秒都会准确获得 25 虎卫，不再因高频结算逐次取整而少发兵。PVP 出征取消与增援召回均验证离城前正常结算、离城 24 小时不产兵、归城后新时段恢复产兵，归城时不会追补离城时段。正式参数由误导性的 `maxGuardPerDay` 改为 `maxGuardPerSettle`，旧配置加载时自动兼容迁移。`web` 与 `web-wlsg` 只使用后端权威军队，不根据累计进度自行发兵，也不会把魏武号令伪造成战斗触发。
- 曹操魏武号令已继续完成同一结算时点的并发验收：32 路同时结算同一份存档，在 Go `-race` 下只把 3 秒产量推进一次，权威军队准确增加 `25` 虎卫，资源结算时间推进到同一时点，小数进度归零且不生成战报。内存仓储用事务互斥串行更新，MySQL 更新路径使用玩家行 `FOR UPDATE`，后续请求读取前一事务已经推进的时间戳。同步发现并修复 `web` 的前端并发响应覆盖风险：旧军事视图响应现在不能覆盖较新的兵力或切换存档后的状态；`web-wlsg` 原有刷新互斥已补实际并发测试，两端最终都只采用后端返回的 `25` 虎卫，不在本地累加产量。
- 曹操魏武号令已继续完成 PVP 出征与增援的跨模块并发验收：同一存档以 100 虎卫和待结算产兵同时提交两类请求，且两边都尝试占用曹操。连续 20 次均只有一条成功，失败事务不提交已计算的产兵、资源时点、小数进度、扣兵或将领占用；最终剩余兵力、曹操唯一非主将模块占用、PVP/增援记录和每日进攻次数与唯一成功响应完全一致，到达战斗前双方均无战报。`web` 的四类地图军事动作共享同步请求锁，`web-wlsg` 的攻击与增援共享 `dispatchingMarch`，不同入口也不能交叉重复提交，失败路径不会覆盖成功后的权威状态。
- 主动战斗的将领归属已完成隔离验收：NPC 和 PVP 在未携带将领时只使用基础部队及非将领公共 Modifier，不借用城内主将的固定 Buff 或正式触发特性，不给城内主将发经验，也不写参战将领快照和特性时间线；NPC 携将对照可正常触发必定火攻。PVP 双方战报、真实守军和基础攻击战力已完成对账，两个前端均不会为空将领战报补造特性结果。
- 无将援军也已完成同样的将领归属隔离验收：派出方城内曹操保留已启用的魏武统御，但 100 名虎卫未携将增援时只贡献基础 `1000` 防御，与主城 1 名守军合计 `1010`，不会借用曹操的属性、Buff 或特性。三方战报和驻防记录的援军将领快照、经验、等级变化与特性时间线均为空，曹操权威经验保持为 0；真实援军兵损、剩余兵力和战报继续一致。`web` 与 `web-wlsg` 均验证无将援军只展示后端兵力和损失，不补造城内主将或特性结果。
- PVP 行军中的配置变更已完成攻守双向验收：特性以到达结算时的最新将领配置为准。同一行军中曹操火攻由开转关、孙权江东固守由关转开，结算后攻击战力保留曹操固定 Buff 的 `2000`，火攻在效果、快照和时间线中全部缺席；孙权最新防御特性形成 `15000` 防御战力，并一致进入双方参战快照、特性结果和标准时间线。两个前端最终只展示真实生效的江东固守，不依据出发时配置补回火攻；`web-wlsg` 标准战报将领快照现完整保留特性参数、方向和场景契约。
- 增援采用与主攻行军不同的部署时点：派出时冻结武将属性、Buff 和特性快照，驻防期间不因全局配置后来开关而追溯改变。魏武统御派出后关闭时，本批援军、驻防记录和三方战报仍一致保留该特性并形成 `1110` 防御战力；派出时关闭、战前开启时，本批援军仍保持 `1010` 基础防御且三方时间线为空。两个前端只按后端实际时间线展示，当前配置和拥有快照都不能单独制造或删除本场结果。
- 增援派出配置的冻结规则已继续覆盖黄巾协防：同一组魏武统御开关正反事务通过真实派出和到达后参与黄巾守城，分别保持 `1110/1010` 防御战力。主城防守战报、援军独立战报、标准时间线、驻防武将快照和最终记录全部与派出时配置一致；两个玩家前端对 `yellow_turban` 来源同样不读取当前全局配置推断历史触发。
- 将领特性快照现已从外层切片复制收敛为嵌套字段完整深拷贝。过去 `Params` 会在玩家将领、PVP 参战快照、增援记录和标准战报之间共享，部分转换还共享方向与场景切片，存在后续配置或响应对象修改污染历史战报的风险。统一复制函数现在隔离 `Params`、`AllowedSides` 和 `AllowedScenes`；五类构造路径均有直接变异测试，两个玩家前端继续把快照参数视为派出时拥有信息，不据此补造实际触发。
- PVP 守城将领按敌军到达时的真实占用状态参与：来袭行军创建后，孙权真实带兵增援第三方时，主城防守只保留城墙基线，不再获得孙权固定防御或江东固守，不给孙权发放守城经验，也不写防守将领快照。双方战报、守军库存和离城增援记录已完成对账；两个前端对无在城主将的防守方不补造将领或特性。
- 上述守城时序已补齐归城正向对照：孙权在敌军到达前完成增援到达、召回和归城后，固定防御、江东固守、守城经验与双方将领快照同步恢复；101 名守军的最终防御战力为 `3090.6`，逐兵种防御变化、数据库经验、守军库存和战报一致。两个前端根据后端真实时间线恢复孙权和江东固守展示。
- 马超天神下凡已完成真实 PVP 被动属性验收：最终武力固定增加 20 点，按每点 2% 转化为 40% 部队攻击加成，同兵力进攻战力从 1000 提升到 1400。双方战报的攻击方战力、损失、返程兵力和守军状态一致；特性保留在参战将领快照中，但不会进入旧特性结果或标准战报触发时间线。两个前端均按被动属性展示，不伪造成战斗中触发。
- 曹操魏武统御已补齐真实援军场景：正式配置显式允许 `reinforcement`，曹操所带 100 虎卫把包含 1 名主城守军的总防御战力从 1010 提升到 1110，并真实减少援军损失。进攻方、防守方和援军方三份战报均记录虎卫实际 `攻击 +1 / 步防 +1 / 骑防 +1`，援军剩余兵力与三份战报一致；两个前端同步支持援军归属和三类实际修正。
- 行军模板参数化测试逐项覆盖疾行奔袭、七进七出、白衣渡江、白衣急行、快如闪电；征兵模板逐项覆盖神鬼之才、王佐之才，确保每个正式特性 ID 都正确订阅事件并修改最终值。
- 赵云七进七出已完成真实 PVP 出征与玩家增援创建验收：同距离、同兵种对照下，正式 `speedBonusRate=1` 按 `ceil(基础秒数/2)` 缩短最终时长并遵守 60 秒下限；开始/到达时间差、增援往返时长和速度倍率与最终秒数一致。该能力不生成战斗触发战报，两个前端直接展示后端最终到达时间，不自行重算。
- 正式配置现有 25 名将领、50 个唯一特性 ID。全量审计确认每个 ID 都有正式测试引用；补充矩阵测试逐项覆盖剩余战前攻防、压制和战后追加伤害特性，防止“模板本身有效，但某个正式 ID 未注册或未订阅”的静默失效。
- 注册中心已收敛为与正式配置完全一致的 50 个 ID；删除未配置、同名且效果冲突的旧 `weizhenxiaoyao`，正式配置测试会拒绝以后再次出现未被任何正式将领使用的孤立注册特性。
- NPC 扫荡聚合战报会累计同一特性在各场产生的真实伤亡、压制、复活、返还和资源变化，并记录 `triggerCount`；单场攻防属性修正不会按场次错误叠高。真实双城扫荡已验证火攻累计值、两座 NPC 剩余兵力、总战损和标准战报一致。
- NPC 扫荡总结果不再固定写成进攻胜利：全胜、全败和混合战果分别归为胜利、失败和平局，摘要同步列出胜负平场数。典韦护主死战、郭嘉鬼才遗策已通过真实扫荡战败返兵验收。
- 典韦和郭嘉的最终减损已补齐黄巾防守正反场景：守城失败返兵，守城获胜不触发；真实主城兵力、旧战报、标准战报使用同一归队值。黄盖苦肉反击作为防守方时，追加黄巾损失也会进入战报与武将经验所使用的战损结果。
- `web` 与 `web-wlsg` 均增加 50 个正式特性 ID 的中文名称映射测试；两端战报继续按同一组后端结果字段展示攻击、步防、骑防、真实伤亡、临时压制、战后追加损失、减损、复活和掠夺数值，并统一把 `disabledTraitCount` 展示为“压制特性数”。
- 已使用本机 Chrome 检查两个前端未登录首屏：均能正常加载，控制台无错误，没有横向溢出或可见控件文字裁切。由于本轮未输入账号，登录后的真实战报页面仍由双前端自动化测试与后端真实战斗测试交叉验收。
- 删除尚未开发的虚假参数：`fireDamageBonusRate`、`warningDelayRate`、`selfCostRate`、`disableTraitRate`。
- 战报支持双方同一特性同时触发，不再因 `traitID` 相同互相覆盖；`web` 与 `web-wlsg` 均兼容真实特性 ID。
- 特性概率判定已统一为严格边界：`triggerChance <= 0` 必不触发，`>= 1` 必定触发，中间值才进行随机判定。火攻、仁德、美人计不再保留各自略有差异的随机比较；零概率测试验证三项能力均不修改兵力、损失或俘虏状态，也不生成虚假特性战报。正式配置中的火攻、仁德、美人计均为 100% 触发，`web` 明确展示正式比例和“必定触发”，`web-wlsg` 使用精确阶段并只展示后端实际结果。
- 正式配置中共有 19 项特性显式设置 `triggerChance`，已增加将领 ID、特性 ID、概率值逐项清单测试，防止配置误删后静默退回模板默认值。其中随机战斗特性统一为 35% 或 50%，火攻、仁德、美人计为 100%；`web` 的每项说明均显示准确概率，`web-wlsg` 验证战报可准确展示 35%/50%/100%，且不自行决定是否触发。
- 正式配置中 16 项概率低于 100% 的随机特性已补齐零概率未命中边界：14 项配置化随机战斗特性按正式 ID 逐项验证战前兵力与属性、战后损失、返兵和压制预算均不变化，白衣渡江与快如闪电继续验证行军时长不变化，全部场景都不生成 outcome。真实业务流程已进一步锁定十五项代表：疑兵偷袭未命中保持 `10000/10000` 战力和双方各 `500` 损失；护主死战未命中时 100 人弱势掠夺只返程 `4`；江东固守合法防守未命中保持基础 `10000/10000`；西凉突击未命中时天神下凡仍形成 `14000` 攻击战力但不追加骑兵损失；小霸王追击在掠夺获胜时未命中，铁骑加攻仍形成 `15600/8000`，守军损失保持核心 `721` 而不是成功对照的 `821`；虎痴冲阵在主动进攻时未命中，破敌防御仍独立把原始两类防御 `10/10` 各降低 3，形成 `10000/7000` 战力和 `602/1000` 兵损；百步穿杨未命中时等势核心各损失 500，后续老当益壮仍追加 100，使守方最终损失 600；水淹七军未命中时不产生 350 名战前伤亡，武圣破军仍形成 `12000/10000` 战力，最终兵损为 `435/564`；威震震慑未命中时不产生 200 名临时压制，威震逍遥仍形成 `19000/8160` 战力与 `300/1000` 兵损；震慑全军未命中时不产生 500 名临时压制，万人怒吼仍形成 `12000/10300` 战力与 `804/1000` 兵损；火烧联营未命中时等势核心各损失 500，连营增伤仍追加 100，使守方最终损失 600；苦肉计未命中时黄盖反击与敌方老当益壮均各追加 100，双方最终损失均为 600；奇兵绕后合法进攻未命中时保持 `10000/10000` 战力和双方各 `500` 损失，固守汉中也不会跨方向补位触发；龙胆救援合法增援未命中时七进七出仍把行军缩短为 `1500` 秒，但赵云援军承担完整 `100` 人损失而不是减损后的 `80`；白衣渡江合法出征未命中时白衣急行仍独立把 `2970` 秒缩短到 `2475` 秒，而不是双命中的 `2063` 秒。十五场状态、经验和各方新旧战报均只反映真实发生的能力，将领快照仍可保留拥有特性；`web` 与 `web-wlsg` 不会由快照补造未命中的随机结果。
- 进度口径更新：上条“十五项代表/十五场”是太史慈补测前的历史阶段记录；当前已由太史慈“快如闪电未命中、信义勇烈独立生效”的真实增援流程补齐为 **16/16**，以本文第十六项验收记录和最新全量回归结果为准。
- 周瑜火攻战报在兼容总额 `extraDamage` 的同时新增逐兵种 `targetExtraLosses`；核心双兵种测试及真实 PVP 验证逐兵种明细之和等于总额，且每项追加值都进入对应兵种最终真实损失。两个前端统一显示“目标兵种追加损失”，不再只给无法逐项对账的总伤害。
- 修复独立实现特性与配置化模板战报字段不一致的问题：火攻、仁德、美人计的真实结果现在都会返回正式 `triggerChance`，仁德额外返回 `effectRate`，美人计额外返回 `captureRate`。核心和真实 PVP 均验证这些字段经过标准化与仓储读取后仍存在；两个前端可同时展示计算比例、本场实际数量和触发概率，不再依赖手写测试数据假设字段存在。
- 纠正陆逊火烧联营的效果解释：正式 `effectRate=1` 是按目标步兵原始人数尝试追加 100% 损失，再受剩余人数封顶，因此触发后目标步兵最终全灭，并不是“追加等同基础阵亡”。真实 PVP 已验证基础损失与追加损失相加等于原始人数；两个前端展示封顶后的实际追加值和最终全灭状态。
- 已锁定西凉突击、老当益壮、小霸王追击、火攻、火烧联营、连营增伤、苦肉反击共七项正式配置，包括概率、比例、目标兵种、攻守方向、掠夺场景和胜负条件。真实 PVP 战报同时保存“设计效果比例/设计伤害比例”和封顶后的逐兵种实际追加损失，双方战报、战斗记录、守军或返程兵力使用同一实际数值；`web` 与 `web-wlsg` 已同步展示，避免把配置比例冒充实际扣兵。
- 老当益壮、火烧联营、连营增伤、苦肉反击已补齐 NPC 单城正式 ID 业务验收：固定 1000 人守军下，三项 10% 能力各追加 100 人真实损失；火烧联营按剩余兵力封顶并使目标步兵最终全灭。四项均完成玩家返兵、NPC 权威剩余兵力、旧战报、标准战报、实际逐兵种增量和将领归属对账；`web` 与 `web-wlsg` 同步按 NPC 来源展示正式名称与真实整数值。
- 西凉突击、小霸王追击、火攻也已补齐正式将领 NPC 单城验收：马超按 500 名骑兵原始基数追加 60 人损失且不影响步兵；周瑜按 1000 人原始基数追加 250 人火攻损失，逐兵种明细之和等于总额；孙策仅在 NPC 掠夺获胜后追加 10 人损失，普通进攻和掠夺战败均不触发。三项的玩家返兵、NPC 权威剩余兵力、旧战报、标准战报和归属方完全一致，两个玩家前端同步使用 NPC 来源与实际整数值验收。
- 已锁定护主死战、鬼才遗策、仁德、仁主守护、龙胆救援共五项正式配置，包括触发概率、胜负条件、适用方向、设计比例和单场上限。真实 PVP 的双方主战报与援军独立战报同时保存设计减损/复活比例、设计上限和逐兵种实际减损/返还值；真实守军、进攻返程或援军剩余兵力继续按实际整数值对账，`web` 与 `web-wlsg` 已同步展示。
- 五项战后复活/返兵特性已补齐 NPC 单城正式将领验收：典韦、郭嘉在 100 人全灭的战败场景分别返还 15、10 人，获胜时均不触发；刘备仁德与仁主守护分别返还 50、10 人，最终真实库存为 60；赵云龙胆救援在主动 NPC 进攻中不触发且不生成虚假战报。原始阵亡、返还明细、最终存活、玩家权威兵力、旧战报和标准战报保持独立口径并完全对账，两个前端同步验证正确归属，`web` 明确标注龙胆“主动进攻无效”。
- 黄忠老当益壮、陆逊火烧联营/连营增伤、黄盖苦肉反击已补齐黄巾守城正式 ID 验收：四项均由防守将领在战斗结算后追加黄巾真实损失，封顶后的逐兵种实际值与黄巾总损失、玩家守军库存、旧战报和标准战报一致。周瑜火攻仅允许主动进攻、孙策追击仅允许掠夺获胜，两项在黄巾守城中均不触发且不生成虚假战报；两个前端同步验证防守侧归属和“魏步兵”实际损失文案。
- 已锁定疾行奔袭、七进七出、白衣渡江、白衣急行、快如闪电共五项正式行军配置：加速分别为 `20%/100%/20%/20%/400%`，白衣渡江和快如闪电触发概率均为 35%，最低时长分别为 `60/60/60/60/30` 秒。真实 PVP 出征与增援继续验证最终秒数、速度倍率、到达时间和增援往返时间一致，行军创建不会生成战斗战报；两个前端只使用后端最终时间，不自行重算。
- 已锁定锦帆劫掠和江东号令的正式配置：两项只允许掠夺场景，分别归属进攻方和防守方，正式修正为 `+20%/-20%`。成功掠夺的双方新旧战报同时保存设计掠夺修正与逐资源实际变化，最终战斗记录、进攻方资源和防守方资源使用同一结算值；`web` 与 `web-wlsg` 均显示木材、石料、铁矿、粮食中文名称。
- 已锁定疑兵偷袭、水淹七军、威震震慑、震慑全军、奇门遁甲五项正式配置。前两项造成真实伤亡并进入最终扣兵，后三项只减少本场参战兵力且战后保留；真实 PVP 已覆盖特性属于攻方和守方的双方战报、标准战报、返程兵力与守军状态。战报同时保存设计效果比例、设计最大影响比例、逐兵种实际数量和触发概率，`web` 与 `web-wlsg` 已同步展示。
- 已锁定卧龙谋制和苦肉计的正式配置：两项设计压制数均为 1，卧龙谋制必定触发，苦肉计有 35% 概率触发。修复过去“特性触发即写压制 1 项”的假战报，新战报区分设计压制数与总线实际跳过数；没有敌方后续特性时实际为 0，确实拦截主将或援军后续特性时才累计为 1。真实 PVP 已覆盖攻守双方、被压制伤害不再扣兵、双方新旧战报与标准战报一致；`web` 与 `web-wlsg` 已同步展示并兼容旧嵌套字段。
- 卧龙谋制和苦肉计已补齐“没有可拦截目标”的真实 PVP 攻守双向验收：两项特性分别由进攻方或防守方携带，对手不具备后续战斗特性时，等势掠夺仍保持 `1000/1000` 战力和双方各 `50` 的核心兵损，返程、守军库存及所属将领经验 `+50` 均不受额外影响。双方新旧战报只记录本次设计压制数 `1`、实际压制数 `0`，且完整时间线没有被补造的敌方特性；`web` 与 `web-wlsg` 均显式保留“实际压制特性数 0”，不能隐藏零值或误写为成功拦截 1 项。
- 已锁定魏武号令、美人计、神鬼之才、王佐之才、内政精营、天神下凡六项正式配置。美人计战报新增设计单兵种俘虏上限，与俘虏比例、实际逐兵种数量、总数和触发概率同时展示；真实 PVP 继续验证同阵营归队、跨阵营驻防、防守方无效和重复结算不重复发兵。五项留城或被动能力均已有真实资源、征兵、产兵、武力和离城失效验收，并明确不生成战斗触发战报。信义勇烈调整为援军防御后，全部 50 项正式特性均已进入分组配置锁定测试。
- 周瑜美周郎军略与火烧赤壁已完成同将领跨阶段真实 PVP 叠加验收：100 名影卫的攻击战力先从 `1000` 提升到 `1100`，随后火攻按 1000 名原始守军追加 `250` 人损失。基础战损、追加损失、最终守军库存和攻守双方战报完全对账，标准时间线固定保持战前加攻在先、战斗结算后火攻在后。两个玩家前端按后端顺序展示两个结果，`web` 同时补齐正式兵种中文兜底，旧战报或兵种配置未就绪时不再直接显示内部 ID。
- 司马懿疑兵偷袭与谋定后发已完成守城同阶段真实 PVP 叠加验收：1000 名进攻军先产生 `350` 名真实战前伤亡，剩余 650 人的单兵攻击再从 `10` 降至 `9`，最终攻击战力为 `5850`。战前伤亡、核心战损、进攻返程、司马懿经验、守军库存和攻守双方战报完全一致；两个玩家前端均把两项结果归属防守侧，并分别展示实际伤亡和逐兵种减攻。
- 诸葛亮奇门遁甲与卧龙谋制已完成双压制真实 PVP 对照：100 名守军中 `25` 名只在本场暂不参战，防御战力降为 `750`，战后仍保留；卧龙谋制另外实际拦截敌方 1 项后续特性，精确移除老当益壮造成的 `100` 名追加损失，且被拦截特性不进入战报。双方真实兵力、进攻返程和标准时间线均一致，两个玩家前端明确区分临时压兵数量与实际压制特性数。
- 陆逊火烧联营与连营增伤已完成同目标双追加伤害真实 PVP 对照：基础损失为 `564` 时，火攻未命中由连营增伤追加 `100`，总损失 `664`；火攻命中则按剩余人数只追加 `436` 并封顶全灭，连营增伤因没有实际增量而不进入触发战报。进攻返程、守军库存、攻守双方战报和将领快照一致，两个玩家前端只采用后端真实时间线，不把“拥有两项特性”误显示为“两项都触发”。
- 甄宓美人计与魅惑扰阵已完成 NPC 同阶段组合验收：1000 名跨阵营守军先俘虏 `200` 人进入获得驻防，剩余 800 人按降低后的步防 9 形成 `7200` 防御战力，核心阵亡 `800` 人。修复 NPC 状态未扣被俘兵造成的重复发兵，并让特性详情按应用层最终路由写入 `capturedToGarrison`；NPC 库存、获得驻防、旧战报和标准战报完全对账，标准守军行固定为 `1000/800/0`，被俘 `200` 单独展示。两个玩家前端同步验证美人计与魅惑扰阵的实际数值。
- 甄宓双特性已继续完成真实 PVP 同阶段与重复结算验收：1000 名跨阵营守军先由美人计俘虏 `200` 人，剩余 800 人再由魅惑扰阵把步防从 `10` 降至 `9`，最终形成 `10000/7200` 攻防战力。进攻方真实阵亡 `626`、返程 `374`；防守方真实阵亡 `800`、被俘 `200`、最终库存为 0，甄宓只按真实阵亡获得 `800` 经验。持久化战斗总减员记 `1000` 并包含俘虏，双方标准战报继续按 `1000/800/0` 展示阵亡与存活、另列获得驻防 `200`，两种口径不互相冒充。重复解析只返回原战斗，获得驻防、经验和战报数量均不再增加；`web` 与 `web-wlsg` 已把双特性展示验收从 NPC 扩展到 PVP，并保持“美人计、魅惑扰阵”的后端时间线。
- 关羽水淹七军与武圣破军已完成主动进攻同阶段组合验收：1000 名守军先产生 `350` 名真实战前伤亡，剩余 650 人叠加 3% 城墙后形成 `6695` 防御战力；关羽所带 1000 人单兵攻击 `10 -> 12`，形成 `12000` 攻击战力。最终守军全灭，进攻方损失 `436`、返程 `564`，关羽经验 `+1000`；双方新旧战报、守军库存、返程行军及标准兵力行完全一致。两个玩家前端按“水淹七军、武圣破军”的后端时间线展示实际整数，不把城墙公共 Modifier 写成特性效果。
- 张飞震慑全军与万人怒吼已完成主动进攻同阶段组合验收：1000 名守军中 `500` 名临时不参战，剩余 500 人叠加城墙形成 `5150` 防御战力；张飞步兵攻击 `10 -> 12`，形成 `12000` 攻击战力。实际参战守军阵亡 500，压制兵战后保留 500，进攻方损失 `300`、返程 `700`，张飞经验 `+500`。本轮同时修复同阶段战报按 ID 字母排序造成的顺序错误，统一改为触发方批次及 `special -> bonus`；两个玩家前端已同步修正张飞和司马懿组合时间线，并锁定真实兵力数值。
- 张辽威震震慑与威震逍遥已完成主动进攻同阶段组合验收：1000 名守军中 `200` 名临时不参战，剩余 800 人使用对骑兵防御 `8`，再叠加蜀国一级城墙 `2%` 形成 `6528` 防御战力；张辽骑兵攻击按 35% 从 `14` 取整提升到 `19`，形成 `19000` 攻击战力。实际参战守军阵亡 `800`，压制兵保留 `200`，进攻方损失 `218`、返程 `782`，张辽经验 `+800`。双方新旧战报、标准兵力行、守军库存和返程行军完全对账，两个玩家前端按“威震震慑、威震逍遥”时间线同步展示实际压制与骑兵 `+5` 攻击修正。
- 许褚虎痴冲阵与破敌防御已完成真实 PVP 同阶段组合验收：守军步防逐次由 `10 -> 8 -> 5`，骑防由 `8 -> 6 -> 4`，两项实际修正分别为 `-2/-2` 与 `-3/-2`，证明第二项 35% 破防基于第一项处理后的当前整数值而非原始属性。最终攻击/防御战力为 `10000/5000`，进攻方损失 `373`、返程 `627`，守军阵亡与许褚经验均为 `1000`；双方新旧战报、权威兵力与标准时间线完全一致，两个玩家前端按“虎痴冲阵、破敌防御”顺序展示两组设计比例和实际整数变化。
- 典韦死战到底与护主死战已完成真实 PVP 跨阶段组合验收：100 名步兵先把单兵攻击从 `10` 提升到 `14`，攻击战力 `1400`，战败后原始阵亡 `100` 再返还 `15`，最终返程与存活均为 `15`；加攻后的实际守军阵亡与典韦经验均为 `61`。双方新旧战报、权威兵力与标准兵力行完全对账，标准时间线按实际阶段固定为“死战到底、护主死战”，不会因护主死战属于特殊特性而错误提前；两个玩家前端同步区分设计加成、实际攻击 `+4`、原始阵亡、返还和最终存活。
- 黄忠百步穿杨与老当益壮已完成真实 PVP 掠夺跨阶段组合验收：守军两类防御先从 `10/8` 降至 `8/6`，形成 `10000/8000` 攻防战力；核心基础损失为进攻方 `421`、防守方 `578`，老当益壮再追加 `100`，最终防守损失 `678`、剩余 `322`，进攻返程 `579`，黄忠经验 `+678`。双方新旧战报、权威兵力、标准兵力行和最终木材转移完全一致，战斗记录与两套战报奖励也使用同一掠夺值；时间线固定为“百步穿杨、老当益壮”。两个玩家前端明确区分核心基础战损、特性实际增量 `100` 与最终总损失，不从配置比例或总损失反推追加量。
- 孙策小霸王与小霸王追击已完成真实 PVP 掠夺跨阶段组合验收：200 名霸王骑的单兵攻击只对正式兵种 `overlordRider` 从 `28` 固定提升到 `78`，并按骑兵方向读取守军骑防 `8`，形成 `15600/8000` 攻防战力；核心基础损失为霸王骑 `55`、守军 `721`，追击胜利后再追加 `100`，最终守军损失 `821`、剩余 `179`，霸王骑返程 `145`，孙策经验 `+821`。双方新旧战报、权威兵力、标准兵力行和木材转移完全一致，时间线固定为“小霸王、小霸王追击”；两个玩家前端同步展示霸王骑实际攻击 `+50` 与追击实际扣兵 `100`。
- 甘宁锦帆奇袭与锦帆劫掠已完成真实 PVP 掠夺跨阶段组合验收：100 名吴步兵的单兵攻击 `10 -> 11`，形成 `1100/1050` 攻防战力；攻方损失 `48`、返程 `52`，守方损失 `54`、剩余 `51`，甘宁经验 `+54`。存活部队基础负重为 `260`，锦帆劫掠实际增加木材 `52`，最终掠夺 `312`，攻守双方资源结算为 `312/9688`。双方新旧战报和标准战报按“锦帆奇袭、锦帆劫掠”记录设计比例、实际攻击 `+1` 与实际资源 `+52`；两个玩家前端同步验证战力、兵损、经验和最终木材。
- 孙权江东固守与江东号令已完成真实 PVP 掠夺防守跨阶段组合验收：江东固守先把 100 名吴步兵的两类防御 `10/8 -> 15/12`，形成 `2000/1500` 攻防战力；攻方损失 `79`、返程 `121`，守方损失 `60`、剩余 `40`，攻守将领经验为 `60/79`。攻方基础负重 `605` 再由江东号令实际削减木材 `121`，最终掠夺 `484`，双方资源为 `484/9516`。攻守双方新旧战报和标准战报按“江东固守、江东号令”把实际防御 `+5/+4` 与资源 `-121` 归属孙权；两个玩家前端同步验证兵力、战力、经验、最终木材和防守侧特性时间线。
- 掠夺类特性的错误归属方向已完成真实 PVP 反向验收：孙权作为进攻将领时江东号令无效，甘宁作为防守将领时锦帆奇袭与锦帆劫掠均无效。成功掠夺仍只使用基础 `10000/100` 攻防战力、`1/9` 战损和 999 名存活兵的 `4995` 基础负重，不产生任何攻击或掠夺比例修正；双方资源、返程、守军和经验与两套战报一致。参战快照仍列出三项拥有特性，但新旧触发时间线均为空；`web` 与 `web-wlsg` 明确只展示后端真实 outcome，不根据快照推断触发。
- 黄盖苦肉计与苦肉反击已完成真实 PVP 同阶段组合验收：等势掠夺平局的核心基础损失为双方各 `500`；苦肉计实际压制防守方老当益壮 1 次，被压制能力不再扣进攻兵且不进入双方战报，黄盖自身苦肉反击仍对 1000 名守军追加 `100`。最终攻方损失/返程为 `500/500`，守方损失/剩余为 `600/400`，双方经验为 `600/500`。新旧战报与标准战报时间线固定为“苦肉计、苦肉反击”，两个玩家前端同步确认敌方被压制特性缺席、实际压制数 `1`、实际追加损失 `100` 和最终兵力，防止压制预算错误作用于黄盖自身后续特性。
- 双方同时携带苦肉计的真实 PVP 已补齐同名、同优先级压制验收：攻方苦肉计与守方苦肉计同时生效，各自实际拦截敌方 1 项低优先级追加伤害，苦肉反击和老当益壮均不进入触发结果。吴步兵 100 人以 `1000` 攻击战力对蜀步兵 100 人，蜀国公共城墙使防御战力为 `1020`，最终攻守真实损失为 `50/49`、存活为 `50/51`，两名将领分别只按敌方真实阵亡获得 `49/50` 经验。双方旧战报使用唯一存储键保留两条苦肉计结果，标准时间线按“进攻方苦肉计、防守方苦肉计”保留两个正式 ID 且实际压制数均为 `1`；`web` 与 `web-wlsg` 均锁定同名特性不合并、不串方，公共城墙数值也不归因于特性。
- 马超天神下凡与西凉突击已完成真实 PVP 被动/触发组合验收：天神下凡的 `force +20` 先形成 40% 攻击 Modifier，使 1000 名蜀步兵形成 `14000` 攻击战力，对 `10000` 防御战力的 1000 名魏骑兵产生 `382/617` 核心损失；西凉突击只对骑兵追加正式 12% 即 `120`，最终攻方返程 `618`，守方损失/剩余 `737/263`，双方经验 `737/382`。战报将领快照保留两项拥有特性与本场实际 `Lv.1、40%` Modifier，触发时间线只出现西凉突击；本场经验使马超升级到 `Lv.2`，等级变化由奖励字段单独表示。修复过去经验结算后才生成快照导致战报错误显示升级后 `42.02%` 加成的问题，两个玩家前端同步验证被动不伪装成触发、骑兵实际追加 `120` 与最终兵力。
- 魏延奇兵绕后与固守汉中已完成真实 PVP 方向互斥组合验收。主动进攻时仅奇兵绕后生效：目标步防/骑防 `10/8 -> 8/6`，攻防战力 `10000/8000`，攻守损失 `421/578`、存活 `579/422`，魏延经验 `+578`；守城时仅固守汉中生效：自身步防/骑防 `10/8 -> 30/28`，攻防战力 `10000/30000`，攻守损失 `826/173`、存活 `174/827`，魏延经验 `+826`。两场魏延都以 `Lv.1` 参战并在战后升至 `Lv.2`，参战快照保留两项拥有特性，触发时间线只保留当前方向有效的一项。双方新旧战报、标准兵力行、权威兵力与经验完全一致，`web` 和 `web-wlsg` 同步验证错误方向特性不会因存在于快照而被补造。
- 夏侯渊疾行奔袭与盾阵防御已完成同一支真实增援的跨流程组合验收：100 虎卫在 10 格距离下由疾行奔袭把 `3000` 秒缩短为 `2500` 秒，过程阶段不生成战斗战报；到达后盾阵防御把虎卫步防/骑防 `10/8 -> 14/11`，总防御战力 `1010 -> 1410`。无特性对照的 100 援军全灭，组合启用后实际损失/剩余为 `70/30`，夏侯渊经验 `+110`。攻守援三份新旧战报、援军权威记录和标准兵力行均使用 `100/70/30`，触发时间线只出现盾阵防御；参战快照虽保留两项拥有特性，但疾行奔袭不会被前端补造成战斗触发。`web`、`web-wlsg` 均使用正式虎卫名称并展示盾阵实际防御 `+4/+3`。`web-wlsg` 与 OpenAPI 的增援将领快照契约已补齐真实属性、Buff、特性和占用字段，打包接口文档同步更新。
- 赵云七进七出与龙胆救援已完成同一支真实增援的跨流程组合验收：100 名蜀步兵在 10 格距离下由七进七出把 `3000` 秒缩短为 `1500` 秒，过程阶段不生成战斗战报；到达后与 1 名守城蜀步兵共同形成 `1010` 防御战力，对抗 `1100` 攻击战力。核心原始结果为进攻方损失 `97`、援军损失 `100`，龙胆随后实际减少援军损失 `20`，最终进攻方存活 `13`、守城主队损失 `1`、援军损失/剩余为 `80/20`，赵云经验 `+97`。攻守援三份战报、援军权威记录和标准兵力行均使用 `100/80/20`，特性详情单独展示减少损失 `20`；参战快照保留七进七出与龙胆救援，触发时间线只出现龙胆救援。`web` 与 `web-wlsg` 同步验证三方兵力、经验、特性归属及过程特性不会伪装成战斗触发。
- 赵云双特性已继续完成黄巾守城正反对照：200 名黄巾以 `2000` 攻击战力进攻 100 名贪狼营，守军叠加蜀国 2% 城墙后防御战力为 `1020`；关闭龙胆时守军损失/存活为 `100/0`，固定命中后实际减损 `20`，最终权威损失/存活为 `80/20`。两场黄巾损失均为 `76`，赵云经验准确为 `+76`，证明龙胆只修改自身最终损失，不改变敌军战损或经验来源。将领快照保留龙胆救援和七进七出，旧战报与标准时间线只出现龙胆救援；`web` 与 `web-wlsg` 同步展示设计减损 20%、实际减损 20 和 `100/80/20` 兵力口径，不把行军过程特性补造成战斗触发。
- 黄巾来袭中的主城守将已锁定为到达结算时读取最新配置，与派出时冻结快照的协防援军明确分离。孙权江东固守启用后关闭的真实事务得到 `1025` 防御战力，且 outcome、将领快照和标准时间线均无该特性；关闭后启用得到 `1537` 防御战力，三处均出现该特性。守军权威库存、旧战报存活和标准兵力行使用同一最终数值，两个前端只按后端真实时间线展示。
- 黄巾来袭途中主将离城与归城已使用真实增援往返流程完成双向验收。孙权带 1 名士兵离城后，留城 100 名守军只保留蜀国 2% 城墙加成，防御战力为 `1020`，不获得经验，旧快照、标准将领卡和特性时间线均不得出现孙权。到达、召回并完成返程后，101 名守军恢复孙权自带防御 Buff、江东固守和守城经验，防御战力为 `3090`。两场的权威库存、旧战报存活和标准兵力行完全对账；`web` 与 `web-wlsg` 均只展示结算时真实将领快照、经验和特性结果。
- 黄巾来袭途中正式换将已完成真实事务验收。从司马懿切换到夏侯渊后，黄巾结算只使用夏侯渊的盾阵防御，形成 `1000/1442` 攻防战力；司马懿的疑兵偷袭、谋定后发和经验全部保持不变，夏侯渊的行军特性也不写战斗时间线。本轮同时修复黄巾防守主将在经验发放后才生成快照的 BUG：夏侯渊本场实际以 `Lv.1` 参战，战后获得 `100` 经验升到 `Lv.2`，旧快照和标准将领卡现在保持战前 `Lv.1` 及当时 Buff，奖励字段和玩家存档单独记录 `Lv.1 -> Lv.2`。两个前端已同步验证旧主将缺席、新主将战前等级、盾阵实际防御 `+4/+3` 和战后升级。
- NPC 单城战斗已修复同类战后等级污染：周瑜以 `Lv.1` 参战时，美周郎军略实际形成 `1100` 攻击战力，战后获得 `100` 经验升至 `Lv.2`；旧快照和标准将领卡保持战前 `Lv.1`，奖励字段单独记录升级，真实时间线只保留美周郎军略，不从拥有快照补造火攻。后端及两个玩家前端均已增加契约测试。
- NPC 连续扫荡已修复聚合时点错配：聚合原本使用第一场战力和初始兵力，却不断覆盖成最后一场将领快照，跨场升级时会把不同场次的数据拼到同一参战卡。真实双城事务现验证每城经验 `1`，将领按 `Lv.1 -> Lv.2 -> Lv.3` 承接，最终累计经验 `2`；聚合主侧统一使用第一场战前 `Lv.1` 快照、战力和初始兵力，奖励字段单独表示 `Lv.1 -> Lv.3`。聚合复制与战报响应复制均深拷贝 Buff 和特性嵌套字段，两个玩家前端不会用最终等级覆盖初始参战卡。
- 战斗配置容器已修复可变映射浅拷贝问题：过去 `GetCombatConfig` 返回的 `activeCombatRules`、`rules`、`wallConfig` 与全局状态共享，临时场景规则切换会污染用于恢复的旧配置，导致后续特性事务可能使用错误损失公式。读取和保存现在都先深拷贝三个映射；核心测试验证调用方修改读取副本或保存后的原对象均不能反向污染全局，应用层连续执行规则切换与双城扫荡 10 次也保持正确规则。
- 战报查询、脱敏与分享的响应隔离已补齐：顶层兵力/奖励/特性结果、援军将领/Buff/特性、标准双方属性/特性/奖励和动态结果明细全部深拷贝，轮回战斗记录的特性结果也不再共享嵌套引用。第二轮审计发现无需脱敏时 `detail.extra` 仍保留浅引用，现已在统一克隆入口按原类型复制 PVP 援军、援军损失、城墙指针、扫荡守军兵力/资源以及普通动态映射和切片。直接克隆、本人查询和公开分享测试都会修改响应副本并证明库存原始战报不变；两个玩家前端同时验证敌方将领不会从旧 PVP 快照补回，且适配器不原地改写后端扩展、援军或特性快照。
- 双方同一正式特性 ID 已完成真实 PVP 验收：两个玩家同时携带黄忠参加对等掠夺战，双方 1000 名蜀步兵的核心损失各为 `500`，两条老当益壮分别对敌方实际追加 `100`，最终双方损失/存活均为 `600/400`，两位黄忠经验均为 `+600`。双方旧战报使用唯一存储键保存两个独立 outcome，标准战报仍输出两条正式 `laodang_yizhuang` 并分别归属进攻方和防守方，设计比例、实际追加值和最终兵力完全对账。`web-wlsg` 已修复双方将领 ID 相同时优先按 ID 匹配导致两条结果都串到进攻侧的问题，现以标准 `ownerRole` 为最高归属依据；`web` 同步验证旧唯一键能够通过 outcome 的真实特性 ID 正确展示。
- 双方同一正式战前特性 ID 已完成真实 PVP 验收：两个玩家同时携带曹操和 100 虎卫参加对等掠夺战，两条魏武统御分别把己方虎卫攻击、步防和骑防从 `10/10/8` 提升到 `11/11/9`，攻防战力均为 `1100`，双方损失/存活均为 `50/50`，两位曹操经验均为 `+50`。旧战报分别保存攻防两个 outcome，标准战报输出两条正式 `weiwu_tongyu` 并按显式 `ownerRole` 分边；`web` 与 `web-wlsg` 同步验证真实战力、兵力和三类 `+1` 整数变化，不因双方将领 ID 相同而合并或串方。
- 双方同将领、同双特性的战后返兵已完成真实 PVP 验收：两个玩家同时携带刘备参加对等掠夺战，双方 1000 名蜀步兵核心各阵亡 `500`，随后各自由仁德复活 `250`、仁主守护返还 `50`，最终进攻返程与防守库存都为 `800`，两位刘备经验各为 `+500`。双方新旧战报保留四条独立结果，标准兵力行统一为 `1000/500/800`，原始阵亡、两类返兵和最终存活没有互相替代。`web` 与 `web-wlsg` 均按显式 `ownerRole` 把两项能力分别归属攻防两侧，不因将领 ID 和特性 ID 相同而合并或串方。
- 刘备双返兵的多兵种单场总上限已完成真实 PVP 验收并修复随机分配：此前仁德和通用返兵执行器直接遍历损失 `map`，达到总上限时不同兵种的实际返兵可能随运行变化；现在统一按兵种 ID 升序稳定分配。30000 蜀骑兵和 30000 蜀步兵原始全灭后，仁德固定复活骑兵 10000，仁主守护再返还骑兵 3000、步兵 3000，最终返程和标准存活固定为 `13000/3000`。双方战报保持原始阵亡各 30000，分别列示设计上限、实际复活和实际返还；战力、权威返程、加权经验与状态一致。核心 50 次重复分发、真实 PVP、`web` 和 `web-wlsg` 均锁定同一结果。
- 双前端已完成 38 个正式运行时特性结果字段的中文化契约验收，覆盖触发次数与概率、设计比例与上限、战前伤亡、临时压兵、俘虏去向、攻防修正、追加伤害、减损、复活、返兵、特性压制和掠夺资源修正。每个字段都必须显示玩家可读中文，不能退回 `targetExtraLosses`、`disabledTraitCount` 等后端键名。两端汇总口径统一为“俘虏总数、复活总数、复活兵力、掠夺资源修正”，避免同一战报在两个项目中使用不同含义的简称。

- 已修复多名玩家援军的战报特性串卡问题，并补齐真实双援军 PVP 验收：常山甲、常山乙分别派出 100、200 名蜀步兵且都携带赵云，同场形成 `4000/3010` 攻防战力；两次龙胆救援分别减少 20、40 人损失，最终两批援军净损失为 80、160。驻防权威记录、攻防主战报和两份援军独立战报全部对账，旧结果和标准时间线均保留两条同 ID、同将领但不同 `ownerPlayerId` 的结果。两个前端先按援军玩家归属，再按将领 ID 兼容旧战报，各自援军卡片只展示自己的特性结果，不能仅凭 `ownerRole=reinforcement` 把同一结果复制给全部援军。
- 已补齐旧战报特性结果的正式接口契约：OpenAPI 的 `BattleReport` 现在声明 `traitTriggered`、`traitOutcomes` 和完整 `TraitOutcomeReport`，包括特性类型、触发方、玩家、将领、范围和结果详情，不再出现后端实际返回而接口文档缺失的字段。JSON 往返、公开分享和 `web-wlsg` 无标准 `detail` 的历史双援军战报均验证 `ownerPlayerId` 不丢失；`web` 与 `web-wlsg` 类型同步，打包文档保持 126 条路径。
- 已修复重复特性 ID 的前端渲染键冲突：`web` 标准战报面板过去只用 `traitId + generalId`，两名玩家都使用赵云时两条龙胆救援会产生同一个 React key；现在纳入角色、玩家、将领和时间线序号。`web-wlsg` 在存在 `ownerPlayerId` 时也把玩家加入特性 key，同时保持普通单方战报的稳定键；标准和历史双援军均验证两条结果数量、数值和 key 独立。
- 已修复 `web` 标准军情详情只渲染特性名称和摘要、遗漏全部后端 `detail` 数值的问题。进攻方、防守方和每支援军现在都按自身阵营配置逐项展示中文设计参数和实际结算值，包括真实扣兵、减损、复活、返兵及攻防修正；有结算语义的零值不会被真假值过滤。`web-wlsg` 同步锁定同场两支赵云援军各自的设计减损比例和实际逐兵种减损，避免两端展示口径退化。
- 已修复 `web` 历史战报兼容详情丢失全部特性与援军的问题：没有标准 `detail` 时，现按旧 `traitTriggered` 的唯一存储键和顺序读取 `traitOutcomes`，恢复正式特性 ID、攻守/援军角色、`ownerPlayerId`、将领名称及真实明细；旧 `pvpReinforcements`、逐批损失和 `survivedUnits` 同步进入详情，缺少 `unitName` 的旧兵力行使用启动配置恢复中文兵种名。两名玩家都使用赵云的历史战报在 `web`、`web-wlsg` 中均保持两张独立援军卡片、两条龙胆救援和实际减损 `20/40`，战后返兵形成的权威存活不会再被前端减法覆盖。
- 已补齐部分迁移战报的新旧时间线优先级：已有标准 `detail` 但 `detail.traits` 为空时，`web` 与 `web-wlsg` 会恢复旧 `traitTriggered` / `traitOutcomes`；标准时间线存在真实条目时则完全以标准结果为准，不与旧结果叠加。`web` 还会在标准扩展字段缺失时恢复旧 PVP 援军快照和损失，双端测试分别锁定旧结果可见、标准结果优先和不重复展示。
- 已补齐部分标准 PVP 扩展的嵌套兼容：`detail.extra.pvp` 只包含城墙等新字段时，`web` 不再用该对象整块覆盖旧援军快照和损失，而是保留标准字段并逐字段补回旧数据。`web-wlsg` 继续从顶层旧快照恢复援军卡片，同时把缺少 `unitName` 的正式兵种 ID 转换为中文；双端均验证部分迁移援军的名称、出动 100、损失 80、存活 20 和龙胆实际减损 20。
- 已修复双前端历史视角还原与后端标准转换不一致的问题：旧防守战报的 `player*` 是守城所有者、`defender*` 是来袭目标，现统一交换为来袭方主侧和守城方次侧，攻守将领快照、`200/50/150` 与 `100/20/80` 兵力、守方经验和仁主守护结果都归属正确。旧援军独立战报即使误带敌情可见标记也只生成一张自身卡片，不会把顶层援军再复制一次；`web-wlsg` 的历史兵力卡和特性明细均使用“贪狼营”等正式中文名。
- 历史角色兼容所需的旧字段已补进正式 OpenAPI：战报所有者和目标的阵营、名称、战力、兵力、损失与资源，以及 PVP 攻守将领的 Buff 和拥有特性快照均有明确契约；标准时间线仍是本场真实触发的唯一权威来源。
- 历史防守战报的城墙解释已补齐：`web` 过去在无标准 `detail` 时丢弃顶层 `pvpWall`，现与 `web-wlsg` 一样消费后端权威快照并显示“城墙 Lv.10 / 防守加成 20%”；等级、阵营系数、总加成、硬度和损坏等级范围已进入双前端类型与 OpenAPI。标准详情的 `winnerSide`、`ownerSide`、`ownerOutcome` 也已补入接口契约，避免兵力换位正确但胜败或经验归属仍缺少正式字段依据。
- 部分迁移战报的奖励数值已补齐双端逐字段兼容：标准 `rewards` 缺失字段时恢复旧资源、掉落、城金、武将经验、等级和溢出；标准明确返回木材 7、城金 0、经验 0、空掉落与空溢出时保持权威，不回退旧木材 100、城金 5、经验 30 和征战令 2。正式 OpenAPI 已声明旧 `rewards`、`overflowCityGold`、`overflow` 与 `defenderRevealed`，战报特性结果和最终奖励可继续独立对账。
- 曹操魏武号令已补齐 PVP 出征与 NPC 即时战斗的跨入口并发验收：100 虎卫和待结算产兵只能支持一个携带曹操的军事动作，真实强守军 NPC 与 PVP 请求连续并发 20 次，失败方只允许返回兵力不足或将领已占用，不得留下产兵进度、兵力、占用、经验、战报、每日次数或行军记录。两个前端只在成功响应后采用权威状态；NPC 旧失败响应不能覆盖 PVP 刚提交的兵力和曹操占用。
- 曹操魏武号令已补齐玩家增援与 NPC 即时战斗的跨入口并发验收：同一份待结算产兵、100 虎卫和曹操只允许一个动作成功；NPC 成功时曹操累计经验必须等于战前经验加战报本场经验，增援成功时经验保持不变且派出/接收双方只存在同一条记录。失败动作不得改动 NPC、目标玩家、兵力、占用、进度或战报。MySQL 跨事务锁顺序审计后，PVP 攻击、玩家侦查和增援创建均补齐可重试存储冲突的三次有限重试；故障注入证明第一次 `1213 deadlock` 不会重复扣兵或生成第二条记录。`web` 与 `web-wlsg` 均锁定晚到的 NPC 失败响应不会覆盖增援权威状态。

当前仍需继续体检。以下项目表示“需要增加真实业务流程或更多交叉场景”，不表示对应特性尚未注册：

- 战前真实伤亡、临时压制、具体兵种/分类目标、许褚双破防、攻方破防与守方加防、攻方自加攻与守方自加防、双方同 ID 战前加成都已完成；仍需继续验证其余攻防数值组合。
- 典韦、郭嘉、甄宓美人计以及甘宁/孙权掠夺方向已经完成对应 PVP、黄巾防守或扫荡正反验收；其他带场景、阵营或胜负条件的特性继续补端到端正反测试。
- 行军、征兵、掠夺类特性已完成正式 ID 的服务层主流程验收，后续继续补异常、并发和跨玩法组合场景。
- 援军武将经验、累计经验基线和等级快照已经使用后端权威字段，并完成连续参战、跨多级升级、失败恢复和重复结算幂等验收。两个前端只展示后端返回的本场经验与等级变化，并按援军 ID 去重重试返回的相同快照，不自行推算经验曲线。

已确认的核心边界与待决策问题：

- 太史慈信义勇烈的核心不兼容已解决：不扩展稳定战斗核心读取守方攻击，而是把特性重设计为仅援军两类防御 `+10%`。真实援军 PVP 已证明战力、兵损、驻防记录和三方战报同步变化；主动进攻和主城守军仍然无效。后续遇到类似“配置写入但当前玩法公式不消费”的特性，应先收敛为核心可消费且符合角色边界的效果；若玩法身份不能调整，再提交核心扩展决策，禁止只凭字段或假战报标记完成。
- 增援 `enemy_army` / `enemy_traits` 接入已完成，不修改稳定战斗核心：PVP 模块把援军对敌特性映射为防守联盟成员参与标准事件排序和共享压制预算，结算后再恢复 `ownerSide=reinforcement` 及真实玩家、将领、援军记录归属。正式配置中的 20 项对敌特性已锁定角色矩阵，其中 13 项无显式攻守限制，可由援军按原场景和胜负条件触发；谋定后发以及魅惑扰阵、虎痴冲阵、破敌防御、百步穿杨、奇兵绕后、火攻等 7 项因明确限定防守主将或主动进攻而在援军场景排除。真实 PVP 已验证张辽压制来袭战力 `1100 -> 880`、关羽战前真实扣兵 `38`、黄忠战后追加损失 `11`、诸葛亮实际压制后续特性 `1`，三方战报和双前端均展示后端真实数值；进攻方压制援军后续特性的反向顺序也已覆盖。
- 轮回绝境已通过玩法模块适配层接入统一 `BeforeBattle -> combat.Resolve -> AfterCombatResolve -> AfterBattle` 特性管线，不修改稳定战斗核心。进攻波按 `attacker`、防守波按 `defender` 执行正式方向和场景约束；被动 Modifier 继续进入战斗输入，行军、掠夺、征兵、留城和援军专属能力因副本不存在对应过程而不生成战斗触发结果。真实攻防波已验证关羽战前扣兵 `350` 与攻击战力 `1200`、孙权防御战力 `1500`、黄忠对来袭方追加损失 `100`、刘备攻防两侧复活/返兵，以及周瑜火攻在防守波不触发。
- 轮回绝境的特性状态和战报已完成端到端对账：战前真实伤亡、临时压制、战后追加损失、复活/返兵分别进入波次敌军、玩家权威兵力、标准战报和持久化副本战斗记录；参战将领快照在经验结算前冻结。甄宓美人计可在进攻波把跨阵营敌军 `200` 人原子合并到获得驻防，俘虏不冒充阵亡，重复 `clientActionId` 只返回原战报且不重复加兵。副本战斗记录新增复活、最终存活、敌军俘虏、敌军剩余和特性结果字段，OpenAPI 与两个玩家前端类型同步；`web`、`web-wlsg` 均按后端时间线展示实际扣兵、俘虏和复活整数。
- 轮回绝境已新增全部 50 项正式特性的 100 个真实攻防波事务矩阵：30 项可在进攻波触发，22 项可在防守波触发；15 项留城、征兵、行军、被动、掠夺或援军专属能力在副本战斗中保持不触发，13 项仅主动进攻和 5 项仅防守的能力在错误方向也不触发。每个应触发项除卧龙谋制、苦肉计允许如实记录“实际压制 0”外，都必须在旧结果、标准时间线和持久化副本战斗记录中保留非零实际整数变化；虎卫、骑兵、霸王骑等指定目标使用真实兵种命中。马超天神下凡只改变进攻战力而不生成触发结果。
- 允许无将领出战的 PVP、NPC、增援和轮回四类入口均已完成“不借用城内主将”验收。轮回新增攻防两侧真实事务：城内曹操即使拥有 `+100%` 攻击 Buff、火攻和魏武统御，空将领列表仍保持基础攻防战力 `1000`，不增加曹操经验，不生成攻守将领快照、特性结果或标准时间线；玩家兵力与副本战斗记录继续和标准战报对账。两个玩家前端同步验证无将副本不显示将领卡、经验或特性。
- 本轮最终验收已通过 `go test ./...`、轮回及行军/黄巾/NPC 配置、主将占用、换将、扫荡聚合、战前快照时点、战报响应深拷贝、跨模块军事争用、攻守同属性顺序敏感及合法方向随机未命中定向竞态测试、`web` 199 项测试与生产构建、`web-wlsg` 231 项测试与生产构建，以及 OpenAPI 126 条路径打包。马超合法场景随机未命中锁定 `14000/10000` 战力、`382/617` 兵损、`618/383` 存活和 `617/382` 经验，并验证 PVP 与标准战报快照完整保留基础 `stats`、被动后的 `effectiveStats`、`attributes` 和 `buffs`；孙策合法掠夺胜利未命中锁定铁骑加攻后的 `15600/8000` 战力、核心 `55/721` 兵损与 `721/220` 经验，追击不产生额外 100 人损失；许褚合法主动进攻未命中锁定后续破敌防御直接读取原始防御并形成 `10000/7000` 战力、`602/1000` 兵损及实际 `-3/-3`；黄忠合法主动进攻未命中锁定 `10000/10000` 核心战力与 `500/500` 核心损失，后续老当益壮独立追加 100 并形成最终 `500/600`；关羽合法主动进攻未命中不产生 350 名战前伤亡，武圣破军仍独立形成 `12000/10000` 战力、`435/564` 兵损、`565/436` 存活和 `564/435` 经验；张辽合法主动进攻未命中不产生 200 名临时压制，威震逍遥仍独立形成 `19000/8160` 战力、`300/1000` 兵损、`700/0` 存活和 `1000/600` 加权经验；张飞合法主动进攻未命中不产生 500 名临时压制，万人怒吼仍独立形成 `12000/10300` 战力、`804/1000` 兵损、`196/0` 存活和 `1000/804` 经验；陆逊合法掠夺未命中保持 `10000/10000` 核心战力与 `500/500` 核心损失，连营增伤仍独立追加目标步兵 100 并形成最终 `500/600`、存活 `500/400` 和经验 `600/500`；黄盖合法掠夺未命中不得压制黄忠后续特性，苦肉反击与老当益壮各追加 100，最终双方兵损、存活和经验均为 `600/400/600`；魏延合法主动进攻未命中保持 `10000/10000` 战力、`500/500` 兵损、`500/500` 存活和 `500/500` 经验，双方触发时间线为空且固守汉中不越过方向限制；赵云合法增援未命中时七进七出仍形成 `1500` 秒行军，龙胆不产生减损，三方战报和权威援军均为 `100/100/0`，赵云经验保持 `97`；吕蒙合法出征未命中时仅白衣急行把行军和返程缩短为 `2475` 秒，抵达后战力、兵损、存活和经验保持 `1000/10`、`0/1`、`100/0`、`1`，双方触发时间线为空。全仓 ESLint 仍有布局、地图和将领面板等既有错误，未混入本批特性治理。轮回测试夹具和临时战斗规则必须在结束时恢复隔离副本，避免执行顺序污染其他战报用例。

- 第十六项追加验收已通过 `go test ./...`、太史慈场景连续 10 次定向测试、相邻正反场景连续 5 次测试、定向 `-race` 连续 5 次测试、`web` 200 项测试与生产构建、`web-wlsg` 233 项测试与生产构建、`web` 定向 ESLint 及 `git diff --check`。本轮同时修复 `web-wlsg` 的魏蜀吴基础步兵、骑兵缺少中文映射导致特性明细暴露内部 ID 的问题。
- 甄宓破防与孙权加防的顺序敏感追加验收已通过场景连续 10 次定向测试、定向 `-race` 连续 5 次测试、`go test ./...`、`web` 201 项测试与生产构建、`web-wlsg` 234 项测试与生产构建、`web` 定向 ESLint 及 `git diff --check`。

- 已修复战斗后损失特性在“主守军与援军使用相同兵种 ID”时的战报明细覆盖问题。等势掠夺中，黄忠率 1000 名蜀步兵进攻孙权 500 名吴步兵与太史慈 500 名吴步兵，核心先让攻守联盟各损失 500；老当益壮随后对两条吴步兵来源各追加 50，真实追加总数应为 100。过去 `addLosses`、`addTargetLosses`、`reduceLosses` 的实际结果映射按兵种赋值，第二条来源会覆盖第一条，导致实际已扣 100、特性战报只显示 `+50`；现在统一按兵种累加。最终攻方为 `1000/500/500`，主守军与援军均为 `500/300/200`，黄忠与太史慈经验分别为 `600/500`，主战双方战报准确显示“老当益壮：吴步兵 +100”，三份战报与两份权威状态均使用援军损失 300。独立援军战报按既有归属规则只展示援军自身兵力与经验，不复制主将黄忠的特性时间线；`web` 和 `web-wlsg` 已同步锁定这些数值。
- 后续所有追加损失、指定兵种追加损失和减少损失特性，都必须覆盖同一兵种来自多个部队来源的情况。验收时既要核对每个来源的最终兵力，也要核对按兵种汇总的特性实际值；禁止用映射覆盖掩盖已发生的真实扣兵或返兵。
- 本项追加验收已通过核心三路径单元测试、真实 PVP 场景连续 10 次测试、相邻援军伤害与压制场景连续 5 次测试、定向 `-race` 连续 5 次测试、`go test ./...`、`web` 202 项测试与生产构建、`web-wlsg` 235 项测试与生产构建、`web` 定向 ESLint 及 `git diff --check`。
- 已修复主城赵云与同兵种援军共同防守时龙胆救援越过部队来源边界的问题。真实 PVP 中，1000 名魏步兵进攻赵云率领的 500 名蜀步兵和刘备援助的 500 名蜀步兵；无特性对照的主守军与援军各损失 `250`。龙胆固定触发后，只能把赵云主守军损失由 `250` 降为 `200`，刘备援军仍损失 `250`，守方联盟总损失为 `450`，主守军与援军最终兵力分别为 `300/250`。龙胆实际减损 `50` 只进入主战双方战报，独立援军战报不得复制主将特性；攻方、赵云、刘备经验分别按真实击杀记为 `450/500/500`。PVP 与黄巾适配层现在先按来源拆分主守军和各批援军，再让 `reinforcement_self` 范围的减损只处理所属来源，不修改稳定战斗核心。
- 战斗后损失处理必须区分三个口径：`AfterCombatResolve` 的追加伤害或减损决定真实阵亡和敌方击杀经验；`AfterBattle` 的复活、返还只改变最终净损失与存活，不回减已经发生的击杀经验；俘虏是独立的非阵亡减员，不参与龙胆减损。援军赵云的既有场景因此把攻方经验从按减损前总损失误算的 `101` 修正为实际阵亡 `81`；刘备援军原始阵亡 `100`、仁德和仁主守护后最终净损失 `40` 时，敌方经验仍以原始阵亡 `100` 为准。
- 本项追加验收已通过龙胆、援军损失和黄巾相邻场景连续 5 次测试、主将与援军龙胆真实 PVP 定向 `-race` 连续 5 次测试、`go test ./...`、`web` 203 项测试与生产构建、`web-wlsg` 236 项测试与生产构建、`web` 定向 ESLint 及 `git diff --check`。
- 已继续补齐主守将刘备与同兵种援军共同防守时的来源隔离证据。真实等势 PVP 中，1000 名魏步兵对刘备主守军 500 名蜀步兵和关羽援军 500 名蜀步兵，核心原始阵亡按来源各为 `250`。仁德只为刘备主守军复活 `125`，仁主守护再返还 `25`，主守军最终存活 `400`；关羽援军不继承主将能力，仍保持损失/存活 `250/250`。战斗记录和标准兵力行继续保留两条来源各自的原始阵亡 `250`，攻方经验按真实击杀总数记为 `500`，刘备与关羽经验也分别按攻方阵亡记为 `500`。两项刘备结果只进入主战双方时间线，独立援军战报不得复制；`web` 与 `web-wlsg` 均已按同一后端整数锁定三方卡片和中文明细。
- 该场景证明主守将 `self_army` 的 `AfterBattle` 返兵当前已经基于主守军来源损失执行，无需修改稳定核心或 PVP 结算实现。后续治理必须继续区分：龙胆类 `AfterCombatResolve` 减损会降低实际阵亡及敌方经验；仁德、仁主守护类 `AfterBattle` 返兵只增加最终存活，不修改原始阵亡和敌方击杀经验。
- 本项追加验收已通过主守将刘备、刘备攻守双向及正式援军返兵场景连续 10 次测试、主守将刘备与攻守双向场景定向 `-race` 连续 5 次测试、`go test ./...`、`web` 204 项测试与生产构建、`web-wlsg` 237 项测试与生产构建、`web` 定向 ESLint 及 `git diff --check`。
- 已修复典韦护主死战与郭嘉鬼才遗策在 PVP 平局中被误判为“战败后触发”的 BUG。旧 `AfterBattleContext` 只有 `Won` 布尔值，平局时攻守双方均为 `false`，统一条件判断把 `requiredOutcome=loss` 错误解释为 `!Won`，导致等势 1000 对 1000 掠夺中双方原始阵亡各 `500` 后，典韦错误返还 `75`、郭嘉错误返还 `50`，并污染最终兵力和战报时间线。现在战后上下文额外携带真实胜方 `attacker/defender/draw`；平局既不满足 `win`，也不满足 `loss`。PVP、NPC、黄巾、援军和轮回适配入口均传递明确胜方，未提供新字段的旧单元调用仍兼容原 `Won` 语义。
- 真实 PVP 已分别覆盖进攻主将、防守主将和援军三种归属。等势主战双方各保持 `1000/500/500`，同样兵力由主守军 500 与援军 500 组成时，两条防守来源各保持 `500/250/250`；典韦、郭嘉可以保留在参战将领拥有快照中，但两项战败返兵均不得进入旧结果或标准时间线，攻守及援军经验继续按真实阵亡记为 `500`。`web` 与 `web-wlsg` 已同步锁定平局兵力、经验、空触发区和不从拥有快照补造返兵。
- 本项追加验收已通过胜、负、平三态及 PVP、NPC、黄巾、援军、轮回相邻场景连续 10 次测试，主将与援军平局、合法战败场景定向 `-race` 连续 5 次测试、`go test ./...`、`web` 205 项测试与生产构建、`web-wlsg` 238 项测试与生产构建、`web` 定向 ESLint 及 `git diff --check`。
- 已补齐正式 `requiredOutcome=win` 特性小霸王追击的平局反向验收。PVP 与 NPC 现在都覆盖掠夺胜利触发、普通进攻无效、掠夺战败无效、掠夺平局无效四格矩阵。真实等势用例明确移除城墙等公共 Modifier 后形成 `1000/1000` 战力，双方均为出动 `100`、原始阵亡 `50`、存活 `50`，孙策获得经验 `50`；参战快照保留小霸王追击和小霸王铁骑，但追击不追加伤亡、不进入旧结果或标准时间线。`web` 与 `web-wlsg` 已同步锁定完整兵力、经验和空触发区，并与“掠夺获胜但随机未命中”场景分开验收。
- 平局测试不能只用双方人数相等作为证据。蜀国城墙等公共 Modifier 会让相同人数形成 `1000/1020` 并判定防守胜利；后续所有胜负条件测试必须同时核对最终攻防战力及后端权威 `winner`，再判断 `requiredOutcome` 是否允许触发。
- 本项追加验收已通过小霸王追击 PVP、NPC 胜负平矩阵及核心胜负条件连续 10 次测试、PVP 与 NPC 定向 `-race` 连续 5 次测试、`go test ./...`、`web` 206 项测试与生产构建、`web-wlsg` 239 项测试与生产构建、`web` 定向 ESLint 及 `git diff --check`。
- 已继续补齐典韦护主死战、郭嘉鬼才遗策在 NPC 与黄巾真实平局中的反向验收。NPC 使用攻击规则，100 人对 100 人形成 `1000/1000` 战力后双方均全灭，玩家兵力为 `100/100/0`、经验 `100`；黄巾保留魏国 3% 城墙公共加成，用 103 名来袭方对 100 名守军形成 `1030/1030`，来袭方为 `103/103/0`、守军为 `100/100/0`，守将经验 `103`。两项战败返兵都可以保留在参战快照中，但不得返兵、修改权威状态或进入旧结果与标准时间线；`web` 与 `web-wlsg` 已按两种场景的真实全损整数展示，未沿用 PVP 掠夺平局的半损口径。
- `winner=draw` 只定义胜负三态，不定义损失公式。同为平局，PVP 掠夺当前是双方约半损，而 NPC、黄巾攻击规则可让双方全灭；战报和前端必须直接使用对应玩法核心返回的出动、阵亡、存活和经验，不得按“平局”标签统一推算损失。
- 本项追加验收已通过 NPC、黄巾及相邻 PVP、援军平局三态场景连续 10 次测试、NPC 与黄巾定向 `-race` 连续 5 次测试、`go test ./...`、`web` 207 项测试与生产构建、`web-wlsg` 240 项测试与生产构建、`web` 定向 ESLint 及 `git diff --check`。
- 已修复轮回绝境仍把胜负三态压成 `won bool` 的遗漏。轮回战报虽然已经使用核心 `result.Winner` 正确显示 `draw`，但战后适配函数过去只接收“玩家是否获胜”；进攻波和防守波的 `false` 都会被函数反推为对手获胜，导致典韦护主死战、郭嘉鬼才遗策在平局中分别错误返还全损兵力的 15% 和 10%，同时污染玩家权威军队、副本战斗记录、旧结果和标准时间线。轮回战后入口现直接传递核心的 `attacker/defender/draw`，`Won` 只由真实胜方派生，不再丢失平局。
- 真实轮回攻防波现各自覆盖典韦和郭嘉，共四个事务场景。双方 100 人形成 `1000/1000` 后按当前副本攻击规则全部阵亡，玩家和敌军均保持 `100/100/0`，玩家将领经验为 `100`；拥有快照继续保留对应战败返兵特性，但返兵字段、旧结果、标准时间线和持久化副本特性结果均为空，玩家初始 500 人只剩 400 人。`web` 与 `web-wlsg` 同步锁定攻防两侧权威战力、全损兵力、经验和空触发区，不按平局标签自行推算。
- 本项追加验收已通过四个轮回平局场景连续 10 次测试、定向 `-race` 连续 5 次测试、`go test ./...`、`web` 208 项测试与生产构建、`web-wlsg` 241 项测试与生产构建、`web` 定向 ESLint 及 `git diff --check`。
- 已补齐小霸王追击作为防守主将时的角色与胜负矩阵。正式配置只有 `allowedScenes=[plunder]` 和 `requiredOutcome=win`，没有 `allowedSides`，因此“胜利”必须相对于孙策当前归属方判断，不能固定解释为进攻方胜利。孙策守城现覆盖掠夺获胜触发、普通进攻获胜无效、掠夺战败无效和掠夺平局无效四格；结合既有主动掠夺矩阵，攻守两侧都具有明确证据。
- 真实掠夺守城胜利中，100 名魏步兵以 `1000` 战力进攻孙策 200 名吴步兵的 `2000` 防御战力。核心先产生来袭方 `72`、守军 `54` 的损失，小霸王追击再对来袭方实际追加剩余 `28`，最终来袭方为 `100/100/0`、守军为 `200/54/146`，孙策经验为 `100`。双方旧结果和标准时间线都把追击归属防守孙策，特性结果只记录设计 50% 与本场实际 `+28`；权威返程、守军库存和经验与战报一致。两个前端同步锁定防守归属、战力、兵力、经验和中文明细。
- 本项追加验收已通过孙策防守矩阵连续 10 次测试、攻守两套矩阵定向 `-race` 连续 5 次测试、`go test ./...`、`web` 209 项测试与生产构建、`web-wlsg` 242 项测试与生产构建、`web` 定向 ESLint 及 `git diff --check`。
- 已补齐黄盖苦肉计作为防守主将时的合法概率未命中验收。等势掠夺中，黄忠率 1000 名蜀步兵进攻黄盖 1000 名吴步兵，移除城墙后形成 `10000/10000` 战力，核心先让双方各损失 `500`；防守黄盖的苦肉计强制未命中，不得压制进攻黄忠的老当益壮，也不得吞掉黄盖自己的苦肉反击。时间线按实际分发顺序只保留“进攻方老当益壮、防守方苦肉反击”，两项各向敌方追加 `100`，最终双方均为出动 `1000`、损失 `600`、存活 `400`，黄忠与黄盖各获得经验 `600`。苦肉计仍保留在黄盖参战拥有快照，但旧结果和标准时间线均不得出现该未命中特性；双方权威兵力、返程、战报与经验完全一致。
- 本项与既有“黄盖作为进攻方未命中”组成攻守双角色证据。`web` 与 `web-wlsg` 均锁定防守视角下的时间线顺序、攻守归属、两项实际追加损失、双方 `1000/600/400` 兵力和防守黄盖经验 `600`，不得从拥有快照补造苦肉计。本项已通过正反角色场景连续 10 次测试、定向 `-race` 连续 5 次测试、`go test ./...`、`web` 210 项测试与生产构建、`web-wlsg` 243 项测试与生产构建、`web` 定向 ESLint 及 `git diff --check`。
- 已补齐司马懿疑兵偷袭作为防守主将时的合法概率未命中验收。1000 名蜀步兵进攻司马懿 1000 名魏步兵，移除城墙后疑兵偷袭强制未命中，不产生战前伤亡；仅防守生效的谋定后发仍独立把蜀步兵单位攻击从 `10` 降为 `9`，形成 `9000/10000` 攻防战力。掠夺核心据此结算来袭方损失 `537`、存活 `463`，守军损失 `462`、存活 `538`；刘备与司马懿经验分别为 `462/537`。疑兵偷袭与谋定后发均保留在司马懿拥有快照，但双方旧结果和标准时间线只能出现归属防守方的谋定后发，并准确记录设计减攻 `10%` 与蜀步兵实际攻击 `-1`。
- 本项与既有“司马懿作为进攻方疑兵未命中”组成攻守双角色证据，证明随机战前伤亡未命中不能撤销同将领后续确定性防守减攻。双方权威返程、守军库存、战报兵力行、经验和防守视角展示完全对账；`web` 与 `web-wlsg` 均不从快照补造疑兵偷袭。本项已通过攻守两侧定向场景连续 10 次、定向 `-race` 连续 5 次、`go test ./...`、`web` 211 项测试与生产构建、`web-wlsg` 244 项测试与生产构建、`web` 定向 ESLint 及 `git diff --check`。
- 已补齐陆逊火烧联营作为防守主将时的合法概率未命中验收。1000 名蜀步兵进攻陆逊 1000 名吴步兵，移除城墙后形成 `10000/10000` 战力，掠夺核心先让双方各损失 `500`；防守陆逊的火烧联营强制未命中，不得产生按战前人数 100% 追加的伤亡，连营增伤仍独立向来袭蜀步兵追加 `100`。最终来袭方为出动 `1000`、损失 `600`、存活 `400`，守军为 `1000/500/500`，刘备与陆逊经验分别为 `500/600`。平局只表示核心胜负，不得让前端把特性追加后的非对称兵损重算为双方各半。
- 陆逊参战快照保留火烧联营与连营增伤，但双方旧结果和标准时间线只能出现归属防守方的连营增伤，并准确记录设计比例 `10%` 与蜀步兵实际追加损失 `100`；返程、守军库存、经验和战报兵力行完全一致。该场景与既有陆逊主动进攻未命中组成攻守双角色证据，`web` 与 `web-wlsg` 均不从快照补造火烧联营。本项已通过攻守两侧定向场景连续 10 次、定向 `-race` 连续 5 次、`go test ./...`、`web` 212 项测试与生产构建、`web-wlsg` 245 项测试与生产构建、`web` 定向 ESLint 及 `git diff --check`。
- 已补齐关羽、张辽、张飞三项随机战前特性在防守角色中的合法概率未命中矩阵。水淹七军、威震震慑、震慑全军均未配置 `allowedSides`，因此守城属于合法方向；三项强制未命中后分别不得产生战前真实伤亡、临时压兵或旧结果。三人的武圣破军、威震逍遥、万人怒吼则各自明确仅限 `attacker`，不能因为同将领第一项能力在防守场景合法就越界获得攻击加成。
- 三个真实等势掠夺场景均移除城墙，固定为 `10000/10000` 战力，攻守双方都是出动 `1000`、损失 `500`、存活 `500`，双方将领经验各 `500`。防守将领快照和标准参战快照完整保留各自两项拥有特性，但双方旧结果、标准时间线和特性文案全部为空；返程和守军权威库存均为 `500`。结合三项既有主动进攻未命中用例，六个方向样本证明同一将领两项能力必须分别执行概率与方向条件。`web` 与 `web-wlsg` 已同步锁定空触发区、基础战力、兵力和防守经验。本项已通过六场攻守样本连续 10 次、定向 `-race` 连续 5 次、`go test ./...`、`web` 213 项测试与生产构建、`web-wlsg` 246 项测试与生产构建、`web` 定向 ESLint 及 `git diff --check`。
- 已补齐典韦、马超、孙策三项带不同合法条件的防守随机未命中验收。典韦守城战败满足护主死战的 `requiredOutcome=loss`，但强制未命中后不得返兵，死战到底又只允许主动进攻；`10000/1000` 攻防战力下，来袭方损失 `37`、存活 `963`，100 名守军全损，双方将领经验为 `100/37`。马超面对 1000 吴步兵与 1000 吴骑兵时满足西凉突击的骑兵目标条件，但强制未命中后两类来袭兵都只承担核心损失 `12`，100 名守军全损；天神下凡仍在快照中提供 `force +20`、`attackBonus 0.4`，但攻击型被动不改变守城防御，战力保持 `20000/900`，双方经验为 `100/24`，且被动不生成触发条目。
- 孙策在 `1000/2000` 的掠夺守城中获胜，满足小霸王追击的场景与胜负条件，但强制未命中后不追加来袭方伤亡；核心兵损保持来袭方 `72`、守军 `54`，存活 `28/146`，双方经验为 `54/72`，小霸王铁骑因仅限主动进攻也不越界生效。三场双方战报、标准兵力行、返程、守军库存和经验完全对账，拥有快照保留各自两项能力，触发时间线全部为空。结合既有进攻未命中样本，本项已通过六场条件场景连续 10 次、定向 `-race` 连续 5 次、`go test ./...`、`web` 214 项测试与生产构建、`web-wlsg` 247 项测试与生产构建、`web` 定向 ESLint 及 `git diff --check`。
- 已补齐赵云龙胆救援作为主守将、孙权江东固守作为援军时的合法概率未命中证据。赵云场景使用 1000 名来袭兵对 500 名主守军和 500 名同兵种援军，形成 `10000/10000` 平局；未命中后攻方损失 `500`，主守军和援军各损失 `250`，三方经验均为 `500`。孙权场景使用 110 名来袭兵对 1 名主守军和 100 名援军，江东固守未命中后防御战力保持 `1010`，援军 `100/100/0` 全损，三方经验为 `101/97/97`。两场拥有快照均保留，但旧结果与标准时间线为空，三方战报和权威状态完全一致。
- 已补齐孙策小霸王追击作为真实援军时的完整条件矩阵。正式配置没有 `allowedSides`，所以孙策援军属于防守联盟，`requiredOutcome=win` 必须相对于守方判断。199 名孙策援军与 1 名主守军形成 `2000` 防御战力，对 100 名来袭兵的 `1000` 攻击战力；掠夺守方获胜且概率命中时，核心来袭损失 `72`，正式 10% 追击再追加 `10`，最终来袭方为 `100/82/18`。守方核心损失 `54` 全部落在孙策援军，主守军保持 `1/0/1`，援军为 `199/54/145`，三方经验为 `54/82/82`。
- 同一援军矩阵还覆盖掠夺胜利合法未命中、普通进攻胜利、掠夺战败和掠夺平局。未命中时保持基础来袭损失 `72`；普通进攻使用自身损失规则形成来袭全损 `100`、援军损失 `74`；掠夺战败为来袭损失 `54`、援军损失 `72`；平局为攻守总损失各 `50`，其中主守军 `1`、援军 `49`。四个反向场景都保留孙策拥有快照，但不生成追击结果。命中场景的三份旧结果和标准时间线均恢复 `ownerRole=reinforcement`、真实 `ownerPlayerId` 与“魏步兵 +10”，两个前端只在孙策援军卡片展示该结果，并使用含玩家归属的稳定列表 key。
- 已补齐陆逊火烧联营与连营增伤作为真实援军时的顺序及概率证据。100 名魏步兵进攻 1 名蜀步兵与 99 名陆逊援军，移除城墙后形成 `1000/1000` 平局，核心先让来袭方损失 `50`，守方联盟损失 `50`；守方损失按来源落为主守军 `1`、援军 `49`，所以主守军最终 `1/1/0`、援军最终 `99/49/50`。火烧联营命中时，按来袭步兵战前人数 100% 计算并受剩余兵力封顶，实际只再追加 `50`，来袭方最终 `100/100/0`；连营增伤此时实际增量为零，不进入旧结果或标准时间线。攻方、主守将和陆逊经验分别为 `50/100/100`。
- 火烧联营强制未命中时，连营增伤仍独立按战前来袭步兵的 10% 追加 `10`，来袭方最终 `100/60/40`，主守军与援军兵力不变，三方经验为 `50/60/60`。两种结果都恢复 `ownerRole=reinforcement`、`ownerPlayerId=helper_luxun` 和陆逊将领归属，拥有快照保留两项特性，但时间线严格只显示本场产生非零实际增量的一项。`web` 与 `web-wlsg` 已同步锁定三方兵力、援军经验、含玩家归属的稳定 key，以及“魏步兵 +50/+10”的后端实际值。本轮后端业务实现无需修改，新增的是此前缺失的真实事务与双前端验收证据。
- 已补齐司马懿疑兵偷袭作为真实援军时的命中、合法未命中及谋定后发方向隔离证据。100 名蜀步兵进攻 1 名吴步兵与 99 名司马懿援军，疑兵命中时先按来袭方战前人数 35% 造成 `35` 名真实伤亡，剩余 65 人再以 `650` 攻击战力对抗 `1000` 防御战力；掠夺核心继续产生 `42` 名来袭方损失，所以最终来袭方为 `100/77/23`。守方联盟核心损失 `35` 全部落在援军，主守军保持 `1/0/1`，援军为 `99/35/64`；攻方、主守将和司马懿经验分别为 `35/77/77`。
- 疑兵强制未命中时保持 `1000/1000` 平局，来袭方为 `100/50/50`，主守军为 `1/1/0`，援军为 `99/49/50`，三方经验均为 `50`。司马懿援军拥有快照始终保留疑兵偷袭与谋定后发，但谋定后发正式配置仅允许 `defender`，没有声明 `reinforcement`，因此命中和未命中两条路径都不得降低来袭方攻击，也不得进入旧结果或标准时间线；未命中时整个触发区为空。疑兵命中结果恢复 `ownerRole=reinforcement`、真实玩家与司马懿归属，`web` 和 `web-wlsg` 只在 `helper_simayi` 援军卡片显示“蜀步兵 +35”。本轮后端业务实现无需修改，补齐了此前只有角色映射和主将样本、没有援军真实事务的证据缺口。
- 已补齐张飞震慑全军作为真实援军时的命中、合法未命中及万人怒吼方向隔离证据。100 名魏步兵进攻 1 名吴步兵与 99 名张飞援军，震慑命中时让 50 名来袭兵本场暂不参战，剩余 50 人形成 `500/1000` 核心战力；核心实际产生来袭方阵亡 `36`、守方联盟阵亡 `27`。压制兵不属于阵亡并在战后保留，所以来袭方最终必须是 `100/36/64`，不能把压制 50 与阵亡 36 相加；守方损失全部落在援军，主守军保持 `1/0/1`，援军为 `99/27/72`。攻方、主守将和张飞经验分别按真实阵亡记为 `27/36/36`。
- 震慑强制未命中时保持 `1000/1000` 平局，来袭方、主守军和援军分别为 `100/50/50`、`1/1/0`、`99/49/50`，三方经验均为 `50`，触发区为空。张飞援军拥有快照始终保留震慑全军与万人怒吼，但万人怒吼正式配置只允许 `attacker`，援军不能因为临时映射进防守联盟而获得步兵攻击加成或生成时间线。命中结果恢复 `ownerRole=reinforcement`、真实玩家与张飞归属；`web` 和 `web-wlsg` 只在 `helper_zhangfei` 援军卡片显示“本场压制兵力：魏步兵 +50”，同时兵力行仍显示真实损失 36、存活 64。本轮后端业务实现无需修改，新增的是此前缺少的真实援军事务证据。
- 已补齐黄盖苦肉计与苦肉反击作为真实援军时的同阶段顺序和合法未命中证据。100 名黄忠蜀步兵进攻 1 名孙权吴步兵与 99 名黄盖吴步兵援军，移除城墙后形成 `1000/1000` 平局，核心双方各损失 `50`。苦肉计命中时实际压制敌方后序老当益壮 `1` 项，但不能消耗黄盖自己的苦肉反击；反击继续向来袭方追加 `10`，最终来袭方为 `100/60/40`，主守军为 `1/1/0`，援军为 `99/49/50`，攻方、主守将和黄盖经验分别为 `50/60/60`。三方时间线严格为“苦肉计、苦肉反击”，不得出现已被压制的老当益壮。
- 苦肉计强制未命中时不生成结果，也不得吞掉双方后续特性；老当益壮先向守方同兵种联盟追加 `10`，苦肉反击再向来袭方追加 `10`，最终来袭方、主守军和援军分别为 `100/60/40`、`1/1/0`、`99/59/40`，三方经验均为 `60`。主战双方时间线按“老当益壮、苦肉反击”排列，援军独立战报只展示黄盖自己的苦肉反击；拥有快照仍保留未命中的苦肉计，不得据此补造触发。
- 本轮修复战后追加伤害对同兵种多来源分别向下取整的真实 BUG。过去 1 名主守军与 99 名援军会把 10% 伤害算成 `floor(1×10%)+floor(99×10%)=9`，造成战报设计值、联盟总损失和来源持久化相差 1 人；`addLosses` 与 `addTargetLosses` 现在先按兵种总人数计算目标整数，再按各来源比例余数稳定分配并受剩余兵力封顶。验收已通过黄盖及相邻 5 组场景连续 10 次、核心取整连续 10 次、应用层与核心定向 `-race` 各连续 5 次、`go test ./...`、`web` 221 项测试与生产构建、`web-wlsg` 254 项测试与生产构建，以及 `web` 定向 ESLint。两个前端均锁定主守军 `1`、援军 `49/59`、双方追加 `10`、三方经验和真实玩家归属。
- 已补齐马超西凉突击作为真实援军时的命中、合法未命中和被动能力隔离证据。进攻方使用 50 名魏步兵与 50 名魏骑兵，对抗 1 名孙权吴步兵主守军和 99 名马超蜀步兵援军；正式兵种属性形成 `1200/883.33` 攻防战力。西凉突击未命中时，来袭步兵损失 `19`、骑兵损失 `20`，主守军无损、援军损失 `60`，最终分别存活 `31/30/1/39`；攻方、主守将和马超经验为 `60/59/59`，三方触发区为空。
- 西凉突击命中时只按敌方骑兵战前人数的 12% 追加 `6` 名骑兵损失，不得影响步兵。最终来袭步兵仍损失 `19`、骑兵损失变为 `26`，主守军与援军仍损失 `0/60`，最终分别存活 `31/24/1/39`；攻方、主守将和马超经验为 `60/71/71`。三份战报只把西凉突击归属 `helper_machao` 援军并显示“魏骑兵 +6”。天神下凡是马超固有的 `force +20` 被动，参战快照必须保留 `effective force=20` 与 40% 攻击加成，但马超位于防守援军时不改变防御战力，也绝不能进入触发时间线。
- 本轮同时修复部队映射转战斗队列时依赖 Go 映射遍历顺序的确定性 BUG。混合步骑核心损失存在 1 人取整余数时，旧实现会让该余数随机落到步兵或骑兵，继而使西凉突击前后的逐兵种损失和维护费加权经验在相同输入下漂移。`armyMapToSlice`、军队校验和模拟战斗单元构造现在统一按兵种 ID 稳定排序；真实战斗结果、权威兵力与前端战报因此可重复对账。
- 本项验收还补齐 `web-wlsg` 的援军将领快照 `effectiveStats` 类型字段，避免后端已经返回的被动后最终四维在生产类型检查中被拒绝。本轮已通过马超及相邻 7 组场景连续 10 次、新场景与稳定排序定向 `-race` 连续 5 次、测试隔离回归连续 5 次、`go test ./...`、`web` 222 项测试与生产构建、`web-wlsg` 255 项测试与生产构建、`web` 定向 ESLint 及 `git diff --check`。
- 已补齐诸葛亮奇门遁甲与卧龙谋制作为真实援军时的跨阶段组合证据。100 名黄忠蜀步兵进攻 1 名孙权吴步兵主守军与 99 名诸葛亮蜀步兵援军；奇门遁甲按正式 25% 让 25 名来袭兵仅本场不参战，形成 `750/1000` 攻防战力。核心实际损失为来袭方 `45`、防守联盟 `39`，临时困住的 25 人不属于阵亡，所以来袭方战报与返程均为 `100/45/55`；主守军保持 `1/0/1`，39 名防守损失全部落到援军。
- 卧龙谋制关闭的隔离对照中，黄忠老当益壮继续按 99 名蜀步兵援军的 10% 追加 `9`，援军最终为 `99/48/51`，攻方、主守将、诸葛亮经验为 `48/45/45`，主战时间线必须是“奇门遁甲、老当益壮”。卧龙谋制启用后实际压制老当益壮 `1` 项，援军保持核心结果 `99/39/60`，经验变为 `39/45/45`；三方时间线严格只保留“奇门遁甲、卧龙谋制”，被压制特性不得出现。两个前端均把双特性精确归属 `helper_zhugeliang`，并展示“本场压制兵力：蜀步兵 +25”与“实际压制特性数：1”。
- 本项发现并修复援军跨阶段结果先合并为无阶段映射、最后一次性追加到战报造成的时间线倒置 BUG。旧实现会把真实先发生的奇门遁甲排到进攻方老当益壮之后；现在援军分别保存战斗前、对敌结算后、自身结算后和战斗结束后结果，PVP 主战报、黄巾协防主战报及援军独立战报都按阶段合并，同时保留总结果供返兵等结算读取。本轮已通过诸葛亮及 6 组相邻场景连续 10 次、4 组阶段与协防场景定向 `-race` 连续 5 次、`go test ./...`、`web` 223 项测试与生产构建、`web-wlsg` 256 项测试与生产构建、`web` 定向 ESLint 及 `git diff --check`。
- 已补齐张辽威震震慑作为真实援军时的正式概率命中、合法未命中与威震逍遥方向隔离验收。100 名蜀步兵进攻 1 名吴步兵主守军与 99 名张辽魏步兵援军；威震震慑命中时临时压制 20 名来袭兵，形成 `800/1000` 战力，核心来袭方损失 `46`、防守联盟损失 `42`，最终来袭方为 `100/46/54`、主守军为 `1/0/1`、援军为 `99/42/57`，攻方、主守将和张辽经验分别为 `42/46/46`。强制未命中时保持 `1000/1000` 平局，三方兵力分别为 `100/50/50`、`1/1/0`、`99/49/50`，经验均为 `50`，触发区为空。
- 已补齐关羽水淹七军作为真实援军时的正式概率命中、合法未命中与武圣破军方向隔离验收。同样的 100 对 100 守城结构中，水淹七军命中先造成 35 名战前真实伤亡，剩余来袭兵形成 `650/1000` 战力，核心再产生 42 名来袭损失；最终来袭方为 `100/77/23`、主守军为 `1/0/1`、援军为 `99/35/64`，攻方、主守将和关羽经验分别为 `35/77/77`。强制未命中时恢复 `1000/1000`、三方各 `50` 经验及空触发区。两条路径都保留关羽双特性拥有快照，但仅限主动进攻的武圣破军不得修改援军防御或进入时间线。
- 张辽、关羽两组命中结果均恢复 `ownerRole=reinforcement`、真实援军玩家和将领归属；`web` 与 `web-wlsg` 分别锁定“本场压制兵力：蜀步兵 +20”和“战前真实伤亡：魏步兵 +35”，未命中时不得从拥有快照补造结果。本轮后端现有业务实现通过严格事务验收，无需修改结算代码。
- 至此正式配置中 13 项可由援军作用敌方的特性已完成真实 PVP 事务闭环，并覆盖随机项的合法命中/未命中、方向隔离、三方兵力、经验、权威援军记录和双前端战报。该结论只代表 PVP 援军对敌角色矩阵完成，不代表整个将领特性持续治理完成；下一阶段转入黄巾、NPC、副本等适配入口的交叉审计。
- 本项验收已通过张辽、关羽及相邻 4 组援军场景连续 10 次测试、4 组随机援军场景定向 `-race` 连续 5 次、`go test ./...`、`web` 224 项测试与生产构建、`web-wlsg` 257 项测试与生产构建、`web` 定向 ESLint 及 `git diff --check`。
- 已开始黄巾适配层的随机特性交叉审计，并补齐关羽水淹七军守城的正式概率命中与合法未命中。100 名魏步兵来袭 100 名蜀步兵，蜀国公共防御形成 `1020` 战力；命中时先造成 35 名战前真实伤亡，来袭方只以 `650` 战力进入核心，最终来袭方为 `100/100/0`、守军为 `100/52/48`，关羽获得 `100` 经验。未命中时保持 `1000/1020`，来袭方仍全损，守军为 `100/97/3`，关羽经验仍为 `100`；两条路径都不得触发仅限主动进攻的武圣破军。
- 已补齐张辽威震震慑守黄巾的正式概率命中与合法未命中。100 名蜀步兵来袭 100 名魏步兵，魏国公共防御为 `1030`；命中时临时压制 20 名来袭兵并形成 `800/1030` 战力，最终来袭方为 `100/80/20`、守军为 `100/69/31`，张辽获得 `80` 经验。被压制的 20 人不是阵亡，必须保留在黄巾来袭方存活数中。未命中时保持 `1000/1030`，来袭方为 `100/100/0`、守军为 `100/95/5`，张辽获得 `100` 经验；仅限主动进攻的威震逍遥始终不生效。
- 两名守将的标准快照在命中和未命中时都保留各自两项拥有特性，但命中时间线只归属防守方并显示实际“魏步兵 +35”或“蜀步兵 +20”，未命中时间线为空。玩家权威守军、战报双方兵力行、最终战力和守将经验完全对账；`web` 与 `web-wlsg` 同步锁定相同后端整数和方向。本轮现有业务实现通过严格验收，无需修改生产结算代码。
- 本项验收已通过关羽、张辽及 4 组相邻黄巾场景连续 10 次、3 组黄巾战前场景定向 `-race` 连续 5 次、`go test ./...`、`web` 225 项测试与生产构建、`web-wlsg` 258 项测试与生产构建、`web` 定向 ESLint 及 `git diff --check`。该结论只关闭关羽、张辽主守将的黄巾随机战前矩阵；其余黄巾随机特性和 NPC 适配仍继续逐项交叉审计。
- 已把张飞震慑全军纳入黄巾主守将随机战前矩阵。100 名魏步兵来袭 100 名蜀步兵，蜀国公共防御为 `1020`；命中时压制 50 人并形成 `500/1020` 战力，核心真实阵亡为来袭方 `50`、守军 `36`，最终双方兵力为 `100/50/50` 与 `100/36/64`，张飞获得 `50` 经验。未命中时保持 `1000/1020`，来袭方 `100/100/0`、守军 `100/97/3`、张飞经验 `100`。压制的 50 人必须作为存活兵保留，不能与真实阵亡相加；万人怒吼仅限主动进攻，两条守城路径都不得生效。
- 已补齐陆逊火烧联营与连营增伤在黄巾守城中的正式命中、合法未命中和战后执行顺序。200 名魏步兵来袭 100 名吴步兵，公共防御形成 `2000/1025`，核心先让来袭方损失 `77`、守军全损。火烧命中时按当前剩余人数封顶再追加 `123`，来袭方最终 `200/200/0`，陆逊经验 `200`；连营增伤此时实际增量为 0，不得生成结果。火烧未命中时，连营仍独立按战前人数 10% 追加 `20`，来袭方最终 `200/97/103`，陆逊经验 `97`；时间线只显示连营增伤。
- 张飞、陆逊的命中与未命中战报均保留两项拥有快照，但只展示本场实际产生非零变化的结果；玩家权威守军、双方标准兵力行、最终战力和守将经验完全对账。`web` 与 `web-wlsg` 同步锁定压制 `50`、火烧追加 `123`、连营追加 `20` 及对应中文明细。本轮现有生产实现通过严格事务验收，无需修改业务代码。
- 本项验收已通过张飞、陆逊及相邻黄巾场景连续 10 次、相关战前与战后场景定向 `-race` 连续 5 次、`go test ./...`、`web` 226 项测试与生产构建、`web-wlsg` 259 项测试与生产构建、`web` 定向 ESLint 及 `git diff --check`。黄巾随机组合审计仍未整体结束，下一批继续黄盖等剩余组合及 NPC 入口。
- 已补齐黄盖苦肉计与苦肉反击在黄巾守城中的命中、合法未命中及零压制口径。200 名魏步兵来袭 100 名吴步兵，公共防御形成 `2000/1025`，核心先让来袭方损失 `77`、守军全损。苦肉计命中时，因黄巾没有可拦截的后续特性，应记录设计压制数 `1`、实际压制数 `0`；随后苦肉反击仍追加 `20`，来袭方最终为 `200/97/103`，黄盖获得 `97` 经验。实际压制 `0` 是有结算含义的结果，不能被后端或前端真假值判断吞掉。
- 苦肉计强制未命中时不得生成压制结果，但也不能影响黄盖自己的苦肉反击；战力、兵力与经验保持同一真实结果，时间线只显示苦肉反击“魏步兵 +20”。命中路径时间线严格为“苦肉计、苦肉反击”，未命中路径严格只有“苦肉反击”；两条路径的黄盖参战快照均保留两项拥有特性，玩家权威守军、标准双方兵力和经验完全对账。
- `web` 与 `web-wlsg` 已同步锁定 `disabledTraitCount=0`、实际追加损失 `20`、最终来袭存活 `103`、守军全损和黄盖经验 `97`。本轮生产实现无需修改，新增的是黄巾应用层与双前端的零值和后序独立效果证据。
- 本项验收已通过黄盖、陆逊及 PVP 无目标压制等相邻场景连续 10 次、相关场景定向 `-race` 连续 5 次、`go test ./...`、`web` 227 项测试与生产构建、`web-wlsg` 260 项测试与生产构建、`web` 定向 ESLint 及 `git diff --check`。黄巾随机组合仍继续审计马超、赵云、孙权等剩余角色，并继续转入 NPC 入口。
- 已补齐马超西凉突击在黄巾守城中的命中、合法未命中与天神下凡被动隔离。200 名魏骑兵来袭 100 名蜀步兵，天神下凡只在马超快照中形成 `force +20` 和 `attackBonus=0.4`；马超是防守方，因此该攻击被动不改变本场 `2800/816` 战力，也不生成战斗触发记录。西凉突击命中时只向魏骑兵追加 `24` 名损失，来袭方最终为 `200/58/142`，马超经验为 `116`；未命中时保持核心结果 `200/34/166`，经验为 `68`，时间线为空。
- 已补齐赵云龙胆救援在黄巾守城中的合法未命中对照。200 名贪狼营来袭 100 名蜀步兵形成 `2000/1020` 战力；未命中时来袭方为 `200/76/124`、守军为 `100/100/0`，赵云经验为 `76`，时间线为空。七进七出属于行军能力，只保留在拥有快照中，黄巾即时守城没有行军过程，不能生成加速或战斗结果。既有命中路径继续锁定龙胆对守军真实减损，不能以拥有快照补造未命中的结果。
- 已补齐孙权江东固守在黄巾守城中的合法未命中。100 名魏步兵来袭 100 名吴步兵形成 `1000/1025` 战力；未命中时来袭方为 `100/100/0`、守军为 `100/96/4`，孙权经验为 `100`，时间线为空。江东号令仅限掠夺场景，黄巾守城不得生效；江东固守虽然方向合法，但未通过概率判断时也不得产生防御 Modifier 或战报条目。两项能力都必须保留在孙权拥有快照中，用于区分“拥有但不满足场景”和“合法但本次未触发”。
- 三名守将的后端严格测试及 `web`、`web-wlsg` 定向战报测试均已通过；连续 10 次、定向 `-race` 连续 5 次、`go test ./...`、`web` 229 项测试与生产构建、`web-wlsg` 262 项测试与生产构建全部通过。本轮生产结算无需修改，新增的是黄巾真实事务、被动隔离、合法未命中和双前端数值证据。黄巾与 NPC 适配层仍需继续补司马懿、典韦等遗漏分支，不能据此宣称整个持续任务完成。
- 已补齐司马懿黄巾守城的疑兵合法未命中。疑兵命中时先造成 `350` 名战前真实伤亡，谋定再把剩余来袭步兵攻击降低 `1`，形成 `5850/10300` 战力；来袭方最终 `1000/1000/0`，守军 `1000/447/553`，司马懿经验 `1000`。疑兵未命中时谋定仍独立生效，形成 `9000/10300`；来袭方仍全损，守军为 `1000/825/175`，经验 `1000`，时间线只保留谋定后发。两条路径都保留司马懿双特性拥有快照。
- 已补齐司马懿主动进攻 NPC 的疑兵命中、合法未命中与谋定方向隔离。200 名魏步兵对 100 名魏步兵，疑兵命中先造成 `35` 名真实伤亡并形成 `2000/650`，最终进攻方 `200/40/160`、NPC 全损；未命中时为 `2000/1000`，进攻方 `200/74/126`、NPC 全损。两条路径司马懿经验均为 `100`；谋定后发仅允许防守方，主动 NPC 进攻中始终不得修改战力或进入时间线。
- 已补齐典韦黄巾守城战败时护主死战的命中与合法未命中。200 名黄巾对 100 名魏步兵形成 `2000/1030`，核心让来袭方损失 `77`、守军原始全损；护主命中后返还 `15`，守军标准兵力为 `100/100/15`，未命中时为 `100/100/0`，典韦经验均为 `77`。死战到底仅限主动进攻，两条黄巾守城路径都只保留拥有快照，不得产生攻击加成或时间线。
- 已补齐典韦主动进攻 NPC 的双特性跨阶段概率矩阵。100 名魏步兵对 200 名 NPC，死战到底固定把步兵攻击增加 `4`，形成 `1400/2000`；进攻方原始全损，NPC 损失 `120`、存活 `80`，典韦经验 `120`。护主死战命中后返还 `15`，时间线严格为“死战到底、护主死战”；合法未命中时最终存活为 `0`，时间线只保留死战到底。拥有护主不能补造未发生的返兵，返兵也不能覆盖原始阵亡 `100`。
- 本批现有生产结算通过严格验收，无需修改业务代码。相关场景连续 10 次、定向 `-race` 连续 5 次、`go test ./...`、`web` 231 项测试与生产构建、`web-wlsg` 264 项测试与生产构建、`web` 定向 ESLint及 `git diff --check` 均通过。持续任务下一批继续审计其余 NPC 随机特性和聚合入口，不能把司马懿、典韦样本扩张为整个 NPC 适配层完成。
- 已补齐许褚、关羽、张辽、张飞主动进攻 NPC 时的随机战前命中与合法未命中，四名将领都使用完整正式双特性配置。许褚虎痴命中后先把 NPC 步防/骑防各降低 `2`，破敌防御再按当前属性降低 `3/2`，形成 `2000/500`，进攻方为 `200/27/173`；未命中时破敌防御仍降低 `3/3`，形成 `2000/700`，进攻方为 `200/44/156`。NPC 两条路径均全损，许褚经验均为 `100`。
- 关羽水淹命中先造成 `35` 名 NPC 战前真实伤亡，武圣破军仍把魏步兵攻击增加 `2`，形成 `2400/650`，进攻方为 `200/31/169`；水淹未命中时武圣仍形成 `2400/1000`，进攻方为 `200/57/143`。NPC 两条路径均从 100 人全损，关羽经验均为 `100`；未命中时间线只能保留武圣破军。
- 张辽威震命中临时压制 `20` 名 NPC，威震逍遥把魏骑兵攻击增加 `5`，正式骑防口径形成 `3800/640`，进攻方为 `200/15/185`，NPC 为 `100/80/20`，张辽经验 `80`；未命中时保持 `3800/800`，进攻方 `200/21/179`、NPC 全损、经验 `100`。压制的 20 人必须计入 NPC 存活，不能当成阵亡或经验。
- 张飞震慑命中临时压制 `50` 名 NPC，万人怒吼把魏步兵攻击增加 `2`，形成 `2400/500`，进攻方为 `200/21/179`、NPC 为 `100/50/50`、经验 `50`；未命中时万人怒吼仍形成 `2400/1000`，进攻方 `200/57/143`、NPC 全损、经验 `100`。四名将领的命中时间线均严格为“随机特殊特性、确定性加成特性”，未命中时只保留确定性加成；拥有快照始终保留两项能力。
- 本批现有生产结算通过严格验收，无需修改业务代码。四将场景连续 10 次、定向 `-race` 连续 5 次、`go test ./...`、`web` 232 项测试与生产构建、`web-wlsg` 265 项测试与生产构建、`web` 定向 ESLint及 `git diff --check` 均通过。NPC 随机战前矩阵本批收口；剩余继续审计马超、黄忠、陆逊、黄盖等战后随机项、孙策掠夺追击概率和扫荡聚合入口。
- NPC 战后随机矩阵已补齐马超、黄忠、陆逊、黄盖和孙策的正式双特性命中与合法未命中。测试统一使用 NPC 掠夺规则，避免 NPC 普通攻击的等势互灭规则提前清空目标，从而能够真实观察核心战损后的追加效果；这同时固定了“玩法胜负规则与特性阶段测试必须匹配”的验收方法。
- 马超两条路径均由天神下凡把 `force 0 -> 20` 并形成 40% 攻击 Modifier，固定为 `14000/10000` 战力和进攻方 `1000/382/618`。西凉突击命中时只向 1000 名魏骑兵追加 `120`，NPC 最终 `1000/737/263`、经验 `737`；未命中时保持核心 `1000/617/383`、经验 `617`。被动只保留在参战快照，不进入任一触发时间线。
- 黄忠百步命中时把 NPC 两类防御各降低 `2`，形成 `10000/8000`；核心损失 `421/578` 后老当益壮追加 `100`，最终 NPC `1000/678/322`。未命中时保持 `10000/10000`，老当仍独立形成最终 `1000/600/400`。陆逊火烧命中时在核心各损 `500` 后只追加剩余 `500` 并让 NPC 全灭，后序连营增伤因实际变化为 0 必须省略；火烧未命中时连营独立追加 `100`，NPC 最终 `1000/600/400`。
- 黄盖苦肉计命中时，NPC 没有敌方将领特性可拦截，战报如实记录设计压制 `1`、实际压制 `0`；苦肉反击仍追加 `100`，双方最终为进攻方 `1000/500/500`、NPC `1000/600/400`。苦肉未命中时只省略压制结果，反击和全部兵力、经验保持相同。孙策 200 名霸王骑在命中与未命中时都由小霸王固定形成 `15600/8000`，进攻方 `200/55/145`；合法掠夺胜利命中时追击追加 `100`，NPC `1000/821/179`，未命中时只保留核心 `1000/721/279`。两种路径都不得从拥有快照补造追击。
- NPC 扫荡聚合入口已完成最终复核：真实双城火攻会累计每场实际扣兵和 `triggerCount=2`，同一项单场属性修正不会错误翻倍；主侧战力、初始出动兵力和将领快照取第一场战前值，累计经验及最终等级只走奖励字段；典韦、郭嘉扫荡战败返兵和最终军队对账，武将校验失败则完整回滚。该入口已有真实事务、聚合函数、双前端“触发场次”和战前快照测试，无新增实现缺口。
- 本轮最终验证通过：上述五将场景连续 10 次、定向 `-race` 连续 5 次、`go test ./...`、`web` 233 项测试与生产构建、`web-wlsg` 266 项测试与生产构建，以及本批 `web` 文件定向 ESLint。全仓 ESLint 仍被 `Layout.tsx`、`Sidebar.tsx`、`DungeonTab.tsx` 等既有 7 个错误阻塞，本批没有修改这些无关文件，也没有把它们计作特性治理通过项。至此本轮短期治理矩阵收口；后续仅在新增特性、玩法或配置变化时按持续任务规则重新审计。

## 9. 后续使用方式

后续每次治理将领特性时，应先从本文档找到对应特性，再按《将领特性短期治理开发文档》的检查表逐项验收：

- 是否注册。
- 是否配置到将领。
- 是否能触发。
- 是否真实改变战斗输入、战斗结果或真实状态。
- 是否进入正确战报字段。
- `web` 和 `web-wlsg` 是否同步展示。
- 是否补充正反测试。
