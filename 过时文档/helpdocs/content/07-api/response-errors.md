# 响应和错误

API 应保持统一风格，避免每个接口返回结构不一样。

## 成功响应

常见形式：

```json
{
  "state": {},
  "result": {}
}
```

不同接口可以返回不同字段，但命名要稳定、清晰。

## 错误响应

常见形式：

```json
{
  "error": "insufficient resources"
}
```

前端会把部分英文错误映射成中文提示。

## 设计原则

- 错误码或错误文本要稳定。
- 前端不要依赖不稳定的临时字段。
- 后端不要把内部异常直接暴露给用户。
- 高危操作需要权限校验。
