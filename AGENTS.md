# Hero3 Agent Rules

1. 每个任务都使用中文回答。
2. 仓库操作遵循用户已授权的 GitHub CLI 环境；用户要求同步、提交、推送时，按当前项目远程仓库执行。
3. 项目必须持续维护完整中文 README 和 docs 文档。
4. 当用户明确说“记住这个规则”“以后按这个判断”“加入规则”时，优先把规则记录到根目录 `PROJECT_RULES.md`。
5. 后续架构、模块、目录和实现判断，必须优先参考根目录 `PROJECT_RULES.md` 中的稳定规则。
6. 如果新需求和既有规则冲突，先指出冲突点，再给出调整建议，不直接推翻旧规则。
7. Hero3 当前架构方向：模块化单体后端，核心稳定，玩法模块通过标准接口接入核心。
8. 如果产生了核心不兼容 无法接入的情况，要第一时间指出来，让用户去决定是核心是否没有设计好，核心是否不完整，去完善核心再继续往下开发。
9. 项目记忆记录在根目录 `memory/`。当用户要求“记下聊天记录”“记录记忆”“读一下记忆”时，按日期维护或读取对应记忆文件。
10. 每次提交前必须更新当天的 `memory/YYYY-MM-DD.md`，压缩记录本次对话中的关键决策、规则变化、架构共识和待办。
11. 如果开启新会话，或用户要求读取记忆，应先阅读 `AGENTS.md`、`PROJECT_RULES.md` 和 `memory/` 中最近的记忆文件，再继续判断。
12. 每天使用一份新的记忆文件，例如 `memory/2026-06-25.md`；同一天继续追加，不重复新建。
